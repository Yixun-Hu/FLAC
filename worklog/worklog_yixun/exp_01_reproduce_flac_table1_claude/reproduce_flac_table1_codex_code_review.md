# Codex code review — exp_01_reproduce_flac_table1

**N/A — no code was written for this experiment.**

exp_01 ran the unmodified release evaluation pipeline (`eval_FLAC.py` at pristine commit `0bd5da0`) via a 24-line launcher shell script (`run_exp01.sh`) that only loops the documented CLI invocation over K ∈ {1, 8} × seeds 42–46. No Python code was added or modified, so the Opus 4.8 code-writing step and the Codex review step of the SOP do not apply.

First applicable review will be exp_02 (re-application of the archived rotation tooling as reviewed commits).
