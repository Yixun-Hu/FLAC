# exp_16 — della_vanilla_repro — lab notebook (append-only)

## 2026-08-11T15:45:00-04:00 — Environment survey (della) + release-checkpoint forensics

- **Goal** — Establish every fact needed to plan the vanilla-FLAC reproduction on della before writing the plan: storage layout, dataset/weights presence, Slurm access, DINOv3 loading path, and — critically — the training budget the paper omits.
- **Command / Validation** — read-only `ls`/`sinfo`/`sacctmgr`/`grep`/`torch.load` on the login node (della9). No GPU, no writes outside the worklog.
- **Result** — status `passed`. Findings:
  - **Storage**: `/scratch/gpfs/BLANCHETTE/yh4742/FLAC/{checkpoints,models,outputs_FLAC,weights}` exist (checkpoints/outputs/weights empty). Dataset at `/scratch/gpfs/BLANCHETTE/yh4742/datasets/AcousticRooms` (`depth_map`, `metadata`, `single_channel_ir_1`, `single_channel_ir_zip`). Repo `models` → scratch symlink already in place (Yixun). Home quota 7.5/48.8 GiB used; scratch fileset 8/15 TiB (33 GiB ours).
  - **Weights**: released `FLAC.ckpt` (724 MB), `FLAC_EMA.ckpt` (258 MB), `FLAC_HAA.ckpt`, `VAE.{ckpt,safetensors}`, three AGREE `.pt` — all present under repo `weights/` (home). exp_01's ten per-seed unseen metric JSONs sit beside them.
  - **⚠️ Release training budget recovered**: `torch.load(weights/FLAC/FLAC.ckpt)` → **`global_step = 67500`, `epoch = 14`** (15 epochs × 4500 steps/epoch × batch 64 ⇒ ~288k train samples/epoch). Optimizer state confirms step 67500; `InverseLR` state `{inv_gamma: 1e6, power: 0.5, warmup: 0.99, base_lr: 5e-5, _last_lr: 4.8393e-5}` matches `FLAC_AR.json`'s scheduler config exactly. **The paper text never states the training length; the released wrapped checkpoint pins it at 67,500 optimizer steps.** This becomes the pre-registered reproduction budget.
  - **Config parity (release vs repo)**: flattened diff of `FLAC.ckpt['model_config']` vs `src/configs/model_configs/FLAC/AR/FLAC_AR.json` — architecture blocks **identical** (DiT: io 32, embed 256, depth 12, heads 8, adaLN; conditioners identical). Differences only: (a) ViT path — release used **`./Models/dinov3-vits16-pretrain-lvd1689m`** (a local dir!) vs repo `facebook/dinov3-vits16-pretrain-lvd1689m`; repo adds explicit `img_h:256, img_w:512, from_scratch:false`; (b) release carries a `training.demo` block (demo_every 50000) absent from repo; (c) metric-callback flags differ (repo enables FD+retrieval); (d) `structured_noise: false` present only in release. None touch model or optimizer math. The release training itself loading DINOv3 from a local directory legitimizes a local-path accommodation here.
  - **DINOv3 locally available twice**: `models/dinov3-vits16-pretrain-lvd1689m/` (config + model.safetensors + preprocessor) AND the shared HF cache `/scratch/gpfs/BLANCHETTE/yh4742/hf_cache/hub/models--facebook--dinov3-vits16-pretrain-lvd1689m`. Loading path in code: `src/models/conditioners.py:455-458` (`AutoModel.from_pretrained(hf_model_name_or_path)`).
  - **Slurm**: account `blanchette`, QOS `gpu-short` (1 d, ≤44 GPU), `gpu-medium` (3 d, ≤20 GPU), `gpu-long` (6 d, ≤16 GPU), `gpu-test`. Partition `gpu` (15 d limit) holds the H100 nodes (`gpu:h100:4/8` GRES seen in sinfo); `gputest` overlaps for smokes. Login node has no GPU — everything runs through sbatch.
  - **Codex reviewer unavailable on della**: no `codex`, `npm`, or `node` on PATH. Per SOP/CLAUDE.md the declared fallback reviewer is **Claude Opus 5 at max effort**, to be stated in every review by-line this experiment.
  - **exp numbering**: remote `worklog_yixun/` tops out at exp_15 → this is **exp_16**.
  - **Eval references on hand**: exp_01 (A6000, commit `0bd5da0`) reproduced paper Table 1 within ~1σ on the full unseen split; its per-seed JSONs (K1/K8 × seeds 42-46) are in `weights/FLAC/`, e.g. unseen K8 seed42: T60 8.6238 / C50 0.9687 / EDT 37.0786 / FD 0.3053 / R@1 7.1012. exp_01's noise floors: K8 T60 ±0.012, C50 ±0.003, EDT ±0.07, R@1 ±0.10.
  - **eval_FLAC output routing**: `build_output_paths` (eval_FLAC.py:148) writes metrics JSON **and** predictions `.pt` next to `--ckpt-path`'s directory. With the repo-side `weights/FLAC/` checkpoint this puts any `--store_predictions` payload in home — the calibration evals therefore run without `--store_predictions` (README includes it; deviation disclosed in the plan).
  - **Stale-doc note**: announcement 05 claims CLAUDE.md is gitignored (".gitignore:177"); on this branch CLAUDE.md **is tracked** (upstream commit `e603947` edits it; `.gitignore:176-177` now holds `weights/`, `outputs_FLAC/`). Announcement not edited (other session owns that history); noted here and in `issue_report.md` at handoff.
- **Analysis** — No blockers. The two recipe unknowns (training length, ViT source path) are both resolved by the released wrapped checkpoint itself. Remaining genuine unknowns: release RNG seed (unknowable — reproduction is statistical, not bit-exact) and H100 wall-clock throughput (measured at smoke rung).
- **Next** — git: rebase onto origin, cut `della-flac-chequity`, scaffold exp_16, write plan, fallback-reviewer plan review, user approval.

## 2026-08-11T16:03:00-04:00 — Branch + scaffold

- **Goal** — Create the della accommodation branch and the exp_16 folder per SOP.
- **Change** — none to code yet. Runtime asset: `ln -sfn /scratch/gpfs/BLANCHETTE/yh4742/datasets/AcousticRooms AcousticRooms` (gitignored runtime location, mirrors the A6000 layout). Created `/scratch/gpfs/BLANCHETTE/yh4742/FLAC/wandb/` for offline W&B.
- **Version Control** — `git pull --rebase --autostash origin check-equivariance-necessity` (clean; autostash of CLAUDE.md /init update reapplied), then `git checkout -b della-flac-chequity`. Base = origin/check-equivariance-necessity @ `e603947`.
- **Result** — `passed`. Branch live, dataset symlink resolves (`single_channel_ir_1` visible), exp_16 folder scaffolded.
- **Next** — write `plan_della_vanilla_repro.md`; run fallback plan review (Opus 5, declared); present for approval. No implementation before approval.

## 2026-08-11T16:40:00-04:00 — Plan review round 1 (fallback reviewer) + Rev 2

- **Goal** — SOP plan review before user approval.
- **Command / Validation** — Reviewer: Claude Opus 5 (declared fallback; Codex/npm/node absent on della), read-only Agent subagent with full briefing (SOP, announcements 01-05, exp_16 files, exp_01 results, touched code). Review saved verbatim: `della_vanilla_repro_opus_plan_review.md`.
- **Result** — `passed` (loop closed for the plan round). Verdict REVISE, 7 blocking + 14 non-blocking. Highest-value findings: **B1** the flac env cannot import the codebase (setuptools 83 dropped `pkg_resources`; pytest absent) — rung 0 added; **B2/B3** the "shared HF cache" was an empty stub AND the AGREE metric model is a second un-patched hub fetch — fix = populate the real cache (zero-code, covers both), resolver demoted to defense-in-depth; **B4** three prior full-budget vanilla runs already exist and would FAIL the drafted ±10% bands — bands recalibrated (C50 ±3%, T60/EDT ±8%, FD ±5%, R@k ±20%), P1@67.5k made co-reference, contribution restated as pipeline validation; **B5** corrected budget arithmetic (291,210 items / 4,550 steps/epoch; "≥67,500, snapshot adopted") and banked the epoch-14 batch-64 witness; **B6** `/AcousticRooms` symlink not gitignored; **B7** Phase-1 gate now fully pre-registered (`max(3σ_exp01, 1% rel)` absolutes; Run A demoted to descriptive; seeds 42+43). Reviewer independently reproduced lr(67500) = 4.839339184958273e-05 analytically (bit-identical to the ckpt) and verified the BN/EMA-validation-isolation mechanism.
- **Analysis** — Review caught two would-have-burned-GPU failures (B1, B2) and a factual error in my survey (B3: I saw the cache dir and assumed blobs; only `refs/` existed). All blocking findings addressed in plan Rev 2; N-findings folded in or explicitly scoped (AGREE untouched — cache covers it; sbatch kit gated by bash -n/DRYRUN as a stated scope call).
- **Next** — present Rev 2 to Yixun for approval. No implementation before sign-off.

## 2026-08-11T16:55:00-04:00 — Yixun approves Rev 2; reviewer switches back to Codex

- **Goal** — Record approval and a mid-round protocol change.
- **Result** — Yixun approved plan Rev 2 in full (both phases; no separate go/no-go before Phase 2 — the pre-registered Phase-1 gate is the go/no-go). Yixun installed codex-cli 0.147.0 mid-session and directed: use Codex for review. **Reviewer from the first code round onward: OpenAI Codex `gpt-5.6-sol` at xhigh** (`codex exec -s read-only`, env-modification explicitly forbidden in every prompt). The plan review remains attributed to the declared Opus 5 fallback.
- **Next** — rung 0.

## 2026-08-11T17:00:00-04:00 — Rung 0 complete: env repaired, cache populated, offline loads proven

- **Goal** — Clear review findings B1/B2/B3 before any code or GPU work.
- **Change** — env only (no commits): setuptools 83.0.0 → **80.10.2**, pytest **9.1.1** installed into the flac env; HF cache populated with the real DINOv3 snapshot (`114c1379…`). Storage: five FLAC + three AGREE weight files moved to `/scratch/gpfs/BLANCHETTE/yh4742/FLAC/weights/`, file-level symlinks left behind (repo `weights/` now 23 KB + JSONs). `pip freeze` snapshot committed alongside the params file.
- **Command / Validation** — `python -c "import train, eval_FLAC"` OK; offline-load proof with `HF_HUB_OFFLINE=1` on CPU: hub-id-via-cache ✓, local `models/` dir ✓, AGREE-style `device_map="auto"` ✓; **cache snapshot and `models/` weights bit-identical** (`torch.equal` all keys). Full details in `della_vanilla_repro_params_set_up.md`.
- **Result** — `passed`. All three Phase-1-blocking review findings cleared.
- **Next** — Coder round A.

## 2026-08-11T17:35:00-04:00 — Coder round A: gitignore + TDD ViT resolver (red→green)

- **Goal** — Implement plan §4a via the Opus 5 Coder subagent.
- **Change** — `.gitignore` +`/models` +`/AcousticRooms`; new `src/tests/test_vit_local_resolution.py` (8 tests); `src/models/conditioners.py` +89/−4: `resolve_vit_model_path` (priority explicit-dir → local_root arg → $FLAC_LOCAL_MODEL_ROOT → repo-root-from-`__file__` → passthrough, returns `(path, tag)`), `_vit_weights_provenance` (size + sha256-prefix, `OSError`-safe), call-site wiring logging `original -> resolved [tag]`; convnext check kept on the original string; configs and AGREE untouched.
- **Version Control** — commits `81a3c44` (gitignore), `f69a3f8` (tests, red), `dacd4d1` (impl, green). Red evidence: ImportError on `resolve_vit_model_path` collection. Not pushed yet.
- **Command / Validation** — new file 8/8 green; full suite **1021 passed, 9 skipped, 1 failed** — the failure is pre-existing (`test_yaw_aug_training.py::test_disabled_path_matches_golden`, an A6000-captured byte-hash golden that differs cross-platform; reproduced at `ddca540` in a throwaway worktree, no overlap with this round's code path). Single-process pytest gets OOM-killed on the login node (exit 137); counts obtained file-by-file and in 3 chunks, agreeing exactly. End-to-end probe: real FLAC_AR conditioning block built with `HF_HOME=/nonexistent` + offline env → resolver alone loads DINOv3 from `models/` (86,406,384 bytes, sha256 4610ad75edef83e7).
- **Analysis** — Coder deviations, all reasonable: +`os`,`hashlib` imports (spec's "no new imports" unsatisfiable as written); test 5 strengthened with a CWD decoy; test commit 255 lines (atomic new module; code commit is 89). Two latent issues logged for later: the della golden-hash failure (cross-platform, affects exp_15's test on this box) and `test_vit_gradient_checkpointing.py` under-reporting coverage here because its skip-guard checks only the HF cache. Neither blocks this round.
- **Next** — Codex round-A review (running in background), fix loop if needed, then round B (Slurm kit).

## 2026-08-11T17:50:00-04:00 — Hardware amendment (H100 → A100 80GB) + Codex-on-della mechanics

- **Goal** — Resolve two blockers: no H100 access for `blanchette`, and the round-A Codex review failing to execute.
- **Hypothesis** — (a) della H100s are QOS-gated away from this account; (b) codex-cli's bwrap sandbox cannot work on della login nodes.
- **Command / Validation** — `sacctmgr show assoc user=yh4742` → QOS `gpu-short/medium/long, gpu-test, short/medium/test/vlong` only. `sbatch --test-only --partition=pli --gres=gpu:h100:1` → "Invalid qos specification" (same for pli-lc). H100s exist only in `pli*` (80 nodes h100:8), `cryoem` (13× h100:4), `ailab*` (h200) — all inaccessible. Explicit `--partition=gpu` is itself rejected ("This is not allowed") — della auto-routes GPU jobs from GRES. Working probe: `sbatch --test-only --gres=gpu:a100:1 --constraint=gpu80 --time=3-00:00:00` → routes to partition `gpu`, would start (worst-case estimate) 2026-08-16; backfill expected to beat that. 40GB A100s (`gpu40`) excluded — README requires 80GB. `/proc/sys/user/max_user_namespaces` = **0** on della login nodes → bwrap can never create its namespace → `codex exec -s read-only` cannot run ANY local command here (first attempt burned itself discovering this, then found the branch missing from GitHub). Sandbox-off invocation was blocked by the harness permission classifier (appropriately).
- **Result** — `passed` (decisions made). **Yixun approved: Phase 2 trains on A100 80GB** (deviation = GPU arch only; disclosed everywhere; plan §4d amended). Codex review re-dispatched via its **GitHub connector** against the freshly pushed branch (`git push -u origin della-flac-chequity`, HEAD `34a1535`) — commits must be pushed before every future Codex round on this box.
- **Analysis** — Both failures are infrastructure, not bugs. The A100 deviation joins the §1 irreproducibility list (Ampere vs Hopper kernels; flash-attn already absent both sides). Codex-on-della SOP note for CLAUDE.md at handoff: push first, review via connector.
- **Next** — Codex round-A verdict → fix loop → round B (Slurm kit, now A100-targeted).

## 2026-08-11T18:25:00-04:00 — Round A CLOSED: Codex REVISE → fixes → re-verified

- **Goal** — Close the round-A loop per SOP (review → fix → re-verify → log).
- **Command / Validation** — Codex review (GitHub connector, gpt-5.6-sol xhigh, 232k tokens) verdict **REVISE**: 1 blocking (priority tests not hermetic off-della), 3 nits (exact log-format assertion; `.`/`..` basename escape — Codex predicted `facebook/..` resolves to the root's PARENT, confirmed red in stage 2; symlink cases). Saved: `della_vanilla_repro_codex_code_resolver_review.md`. Coder fix commit **`f38a804`** (+190/−69): hermetic competing-root tests via `_competing_roots` helper + `_default_local_root()` refactor (pure, behavior-identical), dot-basename guard, symlink tests, exact-substring log assertions. Red→green staged three ways (12 red on missing helper → 4 red on unguarded escape → 17 green). Planner re-verification: `test_vit_local_resolution.py` **17/17** re-run independently; guard + refactor diff inspected; end-to-end offline probe re-confirmed post-refactor (sha256 4610ad75edef83e7).
- **Analysis** — Two accepted deviations: fix commit is 259 changed lines (deletions are superseded tests replaced in place; single-commit fix loop was the instruction); bare `.`/`..` inputs keep upstream-verbatim explicit-dir behavior (no join occurs ⇒ no escape), pinned by `test_bare_dot_input_is_returned_verbatim`. Blocking finding fixed, not batched. Loop: code → review → fixes → re-verified green → logged here. **Round A closed.**
- **Next** — Round B: Slurm kit (A100-targeted per hardware amendment).

## 2026-08-11T19:20:00-04:00 — Coder round B: della Slurm kit (eval/train/submit)

- **Goal** — Implement plan §4d (as amended: A100 80GB, no explicit --partition).
- **Change** — `della_eval.sbatch` + `della_submit.sh` (+359) and `della_train.sbatch` (+208), all in the exp folder. Fail-closed gates with distinct exit codes (EXPECT_SHA+clean-tree=4, interpreter-provenance, GPU=6, NTASKS=7, arg validation=2), offline exports, DRYRUN=1 on all three, tee'd jid-suffixed logs, submit-time command recording.
- **Version Control** — commits `60edd0a` (kit 1/2), `85a928b` (kit 2/2); pushed (`18d94ad..85a928b`).
- **Command / Validation** — `bash -n` clean ×3 (shellcheck absent on della — reported, not installed); DRYRUN argv for all 5 invocation forms captured, `unseen_s42` eval argv byte-matched against the plan §5 flag list; every gate exercised live with its documented exit code; artifact paths cross-checked against `build_output_paths`/`stream_sidecar_path`; resume detection verified on a synthetic 2-ckpt tree; dry runs verified write-nothing.
- **Analysis** — Coder deviations, all accepted: (1) GPU gate requires `nvidia-smi` rc=0 + `^GPU [0-9]` rows — measured on della9 that the driver-failure prose goes to stdout (2 lines), so the specced `wc -l == 1` could pass with ZERO GPUs on a one-line-error node; (2) resume glob needs `globstar` (ckpts 3 levels deep per train.py:169) + empty-array guard (bare `ls -t` would list CWD); (3) `_jid<id>` log suffix (3 same-second eval cells would collide); (4) SMOKE=1 refuses conflicting exported overrides (no-silent-no-op doctrine); (5) submit wrapper gained its own DRYRUN. Submit's pushed-SHA interlock verified firing pre-push.
- **Next** — Codex round-B review (in flight, GitHub connector) → fix loop → close round → submit Phase-1 cells + train smoke.

## 2026-08-11T20:55:00-04:00 — Round B CLOSED: Codex REVISE (5 blocking) → e41dfec → re-verified

- **Goal** — Close the Slurm-kit round.
- **Command / Validation** — Codex review (connector, 141k tokens) verdict **REVISE**: B1 ambient-override channel (`--export=ALL` + env-tunable recipe), B2 resume glob not isolated from smoke/foreign ckpts, B3 non-atomic submit-vs-record + the tracked-record/clean-gate lifecycle conflict, B4 tee checked only post-run, B5 DRYRUN not write-free (git fetch, pycache). Saved: `della_vanilla_repro_codex_code_slurmkit_review.md`. Fix commit **`e41dfec`** (+410/−162): explicit-list `--export=VAR=…` (Planner-approved deviation from the specced `--export=NONE,VAR=…` — Slurm 25.11 man page forbids combining NONE with explicit vars AND makes NONE load a login env), literal recipe constants + GATEFAIL on ambient recipe vars, opt-in `RESUME=1` (fail-closed both directions, step<67500 validation, smoke save-dir now a sibling `exp16_vanilla_repro_smoke`), `sbatch --hold --parsable` → flock'd record append → verify → `scontrol release` (scancel on record failure), exp_11-style **content-scoped closure gate** over 13 paths (worklog/record commits can no longer kill queued jobs; demonstrated live: record-dirty + HEAD-moved ⇒ PASS, touched src/ ⇒ rc=4, committed closure change ⇒ rc=4), exec-into-tee started before the gates with pre-open check, `git ls-remote` pushed-check, `python -B` + PYTHONDONTWRITEBYTECODE everywhere, all four nits. Six-case resume matrix + write-free proof (FETCH_HEAD mtime unchanged, pycache count unchanged) in the Coder report. **Planner re-verification:** `bash -n` ×3, DRYRUN eval unseen_s42 + train full + train SMOKE=1 rerun independently — all gates green, both argvs byte-identical to plan §5.
- **Analysis** — Coder judgment calls accepted: RESUME=1-with-nothing refused; --smoke ⟂ --resume; SIGPIPE-safe discovery (no `| head -1` under pipefail). **Round B closed.** Kit is launch-ready.
- **Next** — Push, submit Phase-1 cells (unseen_s42/s43, seen_s42) + train smoke via della_submit.sh.

## 2026-08-11T21:05:00-04:00 — LAUNCH: Phase-1 calibration cells + training smoke

- **Goal** — Phase-1 calibration (gates Phase 2) + throughput pricing.
- **Acceptance criteria (pre-launch, per plan §5):** eval cells — worker runs pushed SHA `e41dfec` closure; loader prints full split (6337/17 unseen, 6217 seen); stream count check passes; clean ckpt load; unseen_s42 within the pre-registered `max(3σ_exp01, 1%)` absolute thresholds of exp_01 seed-42 (T60 ±0.086, C50 ±0.0097, EDT ±0.371, R@1 ±0.30, R@5 ±0.19, R@10 ±0.27, FD ±0.0031, Invalid T60 == 0). Smoke — ≥1 optimizer step, no OOM/NaN, reaches 300 steps with a val pass at 50, TRAINRESULT rc=0; yields steps/s for the ETA. Smoke metrics are throwaway (save-dir sibling, never enters results).
- **Command / Validation** — via `della_submit.sh` (held→record→release): jobs **12267442** (unseen_s42), **12267443** (unseen_s43), **12267444** (seen_s42), 4h limits; **12267445** (train --smoke), 1h. All PENDING at submit. Full sbatch lines + EXPECT_SHA in `della_vanilla_repro_command.md` (appended by the wrapper at submit time).
- **Next** — On completion: check EVALRESULT/TRAINRESULT lines + gate the Phase-1 thresholds; compute Phase-2 ETA from smoke rate; bring QOS/--time choice to Yixun.

## 2026-08-11T21:40:00-04:00 — Smoke PASSED (12267445): throughput priced, Phase-2 ETA fixed

- **Result** — `passed`, COMPLETED 17:46, rc=0, A100-SXM4-80GB. **Epoch bar shows 4550 steps/epoch — the exact 291,210//64 prediction, live confirmation of global batch 64.** Train rate 0.58–0.63 steps/s (1034s for 300 steps incl. 6×86s val passes; steps 20–50 window 1.6s/step). Loss 2.34→0.937 (step 300), val/avg_loss 1.19, LR matches InverseLR analytically (4.75e-5@300). No OOM/NaN. last_ckpt=<none> (smoke checkpointing off, as designed).
- **Analysis** — Phase-2 projection: 67,500/0.58 ≈ 32.3h train + 27×86s val (39m) + ~27 ckpt saves (~15m) ≈ **33.3h**; ×1.5 margin → `--time=2-06:00:00`, QOS **gpu-medium** (3d cap, N7 satisfied; no resume chain needed). Memory: 80GB card, no pressure observed at batch 64.
- **Next** — `full` integrative Codex review (dispatching now, parallel to queued evals) → Phase-1 gate on eval landings → Yixun go/no-go with this ETA → submit `train --time=2-06:00:00`.

## 2026-08-11T23:05:00-04:00 — Full review + Round C CLOSED; eval cells cancelled & resubmitted

- **Goal** — Close the pre-launch full integrative review loop; keep Phase-1 cells viable.
- **Command / Validation** — Full Codex review (connector, 183k tokens) verdict **REVISE**, both findings launch-procedural: B1 Phase-1 verdict must be a committed, wrapper-enforced artifact (exact JSON paths pinned: `metrics.{T60,C50,EDT,FD,"Invalid T60"}` + `RIR_to_GT_RIR_R@{1,5,10}`, never `RIR_to_geom_*`; all-sample aggregate — disclosed exception to the per-scene note); B2 interruption recovery must be deterministic + documented. Fixes: plan **Rev 3** correction block (Planner; commit `4873813`) — A100 consistency, gate bookkeeping, measured cluster policy **JobRequeue=0 / PreemptMode=OFF**, 6-step interruption runbook; Coder commit **`cf029e9`** — `#SBATCH --no-requeue` both drivers, **PHASE1_PASS.md interlock** (train + train --resume refuse with rc=8 unless the verdict file exists AND is tracked at HEAD; --smoke/eval exempt; fatal under DRYRUN too), commit ledger completed (17 commits, stale placeholder resolved). Planner re-verification: DRYRUN train → rc=8 refusal verbatim; DRYRUN eval unseen_s42 → passes, argv byte-identical.
- **Result** — `passed`; full-review loop closed. **Operational consequence handled:** kit files sit inside the measurement closure, so `cf029e9` invalidated the three PENDING eval cells (bound to `EXPECT_SHA=2bddbcb`) — they would have died at the closure gate (by design, zero GPU cost). `scancel 12267442-4`, pushed, resubmitted via the wrapper: **12268240 (unseen_s42), 12268241 (unseen_s43), 12268242 (seen_s42)**, all PENDING; only queue position lost, and the resubmitted cells now carry `--no-requeue`. Old smoke monitor stopped; new monitor armed on the new ids.
- **Analysis** — Two structural notes for the record: (1) kit edits and queued cells are mutually exclusive by construction (the closure gate covers the kit itself) — freeze kit changes while cells pend, or accept a cancel+resubmit cycle; (2) the launch interlock chain is now: evals land → Planner writes gate table → PHASE1_PASS.md committed at HEAD → only then can `della_submit.sh train` produce a job — plus Yixun's explicit go.
- **Next** — Await 12268240-42; evaluate the gate; PHASE1_PASS.md or STOP.

## 2026-08-12T10:00:00-04:00 — Queue tactics (Yixun-approved): eval cells resubmitted at 2h

- **Goal** — Beat the overnight queue slip (est. start drifted 07:38→13:30; 357 pending on `gpu`, our rank ~295, zero idle A100s).
- **Change** — `della_eval.sbatch` `--time` 04:00:00→02:00:00 (one line; still 8–18× exp_01's measured 6.5–15 min/run; improves backfill eligibility). Kit edit = closure change, so cells were scancel'd FIRST (12268240-42, never started, zero GPU), edit committed+pushed (`e2d77a2`), then resubmitted. Review note: batched into the next Codex round per SOP small-scripts clause; Planner-verified (bash -n, DRYRUN gates green, argv byte-identical).
- **Command / Validation** — new cells: **12287666** (unseen_s42), **12287676** (unseen_s43), **12287677** (seen_s42), 2h limits, EXPECT_SHA `e2d77a2`. Monitor re-armed. Announcement 06 logged this morning (status board every response; ask on every real choice) — this resubmission was its first applied ask.
- **Next** — Cells land → gate table → PHASE1_PASS.md or STOP.

## 2026-08-12T10:20:00-04:00 — Yixun pre-authorizes Phase-2 launch on gate pass

- **Result** — Yixun (verbatim): "Once phase-1 gate passes, go ahead and submit the training". Standing approval recorded: when ALL pre-registered Phase-1 thresholds pass (plan §5 + Rev 3 §2 JSON paths), the session writes + commits `PHASE1_PASS.md`, then immediately submits `della_submit.sh train --time 2-06:00:00` (A100-80GB, gpu-medium) without a further ask. A FAILED gate still stops everything and goes to Yixun as a diagnosis. The PHASE1_PASS interlock remains in force — the wrapper still refuses without the committed verdict file.

## 2026-08-12T18:15:00-04:00 — Queue diagnosis + two-leg race strategy (Yixun-approved)

- **Goal** — Explain the all-day eval queue wait; de-risk the training-leg queue wait.
- **Result** — Diagnosis: cells rank ~273-275 of ~275 pending on `gpu` (prio 5095 vs 11k-17k ahead). Decomposition (sprio, 12287666): fairshare **2967** + QOS 2000 (gpu-short) + age 113 + size 15. Root cause is **account-level fairshare exhaustion**: blanchette EffectvUsage 0.101 vs NormShares 0.042 (~2.4× over-share; kw4335 = 82% of the account's 166M raw usage; yh4742's own usage 0.04%). Fair-tree charges the whole account. Backfill starved by 340-deep pending queue.
- **Decision (Yixun, race option):** on Phase-1 PASS, submit BOTH a gpu-medium single leg (`--time 2-06:00:00`) and a gpu-short leg (`--time 23:30:00`, ~48k steps before timeout); first to start wins, loser scancel'd (three exclusion layers: Slurm `Dependency=singleton` on the shared job name, monitor-driven cancel ≤60 s, and the sbatch checkpoint-exists gate). gpu-short winner ⇒ planned RESUME=1 continuation per Rev 3 runbook (non-bit-exact resume disclosed in results; the gpu-medium single-leg outcome remains the preferred, cleaner artifact). Both submissions recorded in the command manifest as usual.
- **Next** — Cells (12287666/76/77, est. start ~00:32) → gate → PASS ⇒ two-leg submission, FAIL ⇒ stop + diagnosis.

## 2026-08-12T23:15:00-04:00 — PHASE 1 GATE PASS (8/8) → Phase 2 two-leg race SUBMITTED

- **Result** — Gating cell 12287666: COMPLETED 7:52, rc=0, A100-80GB-PCIe. **Gate PASS 8/8** — T60 8.6238 (Δ +0.0000!), FD 0.3053 (Δ +0.0000!), C50 +0.0001, EDT +0.0017, R@1 −0.032, R@5 +0.032, R@10 −0.063, Invalid T60 = 0. The A100/della eval pipeline reproduces exp_01's A6000 release numbers to ~4 decimals — far inside every pre-registered band. Verdict artifact `PHASE1_PASS.md` + calib metrics JSON committed (`f160659`).
- **Command / Validation** — Per pre-authorization: submitted **12313040** (gpu-medium, `--time 2-06:00:00`, prio 3783) and **12313041** (gpu-short, `--time 23:30:00`, prio 4983) — first-start-wins; loser scancel'd by the race monitor (in-script, ≤45 s; ckpt-exists gate as backstop). Note: tried `Dependency=singleton` for kernel-level exclusion, then REMOVED it — singleton blocks on sibling *termination*, not just running (a pending sibling blocks forever), which would have broken the race. As predicted, gpu-short leg carries +1200 priority over gpu-medium.
- **Next** — Race monitor arbitrates; winner trains ~33 h (short leg: to ~48k steps, then RESUME=1 continuation per runbook). Cells 12287676 (running) / 12287677 (pending) land as diagnostic/descriptive rows for `_results.md`.

## 2026-08-13T12:10:00-04:00 — Resume verification + chunked-chain decision (Yixun query 2)

- **Goal** — Verify checkpoint-resume support before designing the chunked chain (Yixun directive).
- **Hypothesis** — Resume works mechanically; the risk is *what data a resumed leg sees*.
- **Result** — `passed` (verification): (1) train.py:230 `trainer.fit(ckpt_path=…)` — full PL restore incl. optimizer/InverseLR/EMA (release ckpt structure is the proof); (2) checkpoint path is leg-independent; (3) **trap found and proven**: train loader is bare `shuffle=True`; PL 2.1 restarts the dataloader on mid-epoch resume (explicit warning, training_epoch_loop.py:155); same-seed legs draw **byte-identical permutations** (empirical: leg1(42)==leg2(42), seed 43 differs) ⇒ fixed-seed 2500-step chunking would train the same ~55% subset 27× and never see the rest. Fix needs NO new training code: per-leg `--seed 42+LEG`. Also applies to any single RESUME=1 continuation (Rev 3 runbook amended by Rev 4's seed rule). (4) Queue mechanics: `bf_max_job_user_part=3`, `bf_window=72h`, no ACCRUE_ALWAYS ⇒ chain waits serialize; QOS weights gpu-short 2000 vs gpu-medium 800.
- **Decision (Yixun):** CHUNK=7500 × 9 legs, gpu-short `--time=04:30:00`; cancel racers at chain submission. Design in plan Rev 4 (new-files-only so racers survive until the swap).
- **Next** — Coder round D (chain kit) → Codex review → 3-leg GPU probe → swap racers → chain live.

## 2026-08-13T13:00:00-04:00 — Round D CLOSED: Codex REVISE → bd9f1c1 → verified; executing the swap

- **Command / Validation** — Codex round-D review (connector) verdict REVISE: B1 fail-open squeue in both mutual-exclusion gates (blocking); N1 checkpoint-indexed reseed endorsed; N2 concurrent-wrapper TOCTOU; N3 stamp read-modify-write not truly locked; N4 spare-leg CHAINDONE branch needs a post-probe DRYRUN check. Fix commit `bd9f1c1`: SQ_RC capture pattern (Coder's own validation caught a `set -euo pipefail` interaction that silently swallowed the first cut's gate message — fixed with explicit rc capture), SEED = 42 + S/CHECKPOINT_EVERY (production: crash-retry at 62500 now seed 67 ≠ leg-start 66; probe keeps 42/43/44), flock re-exec transaction lock (`-E 99`, 10 s), stamp fully inside its own lockfile. Planner re-verification: bash -n ×2; fresh-leg DRYRUN seed 42 argv correct; seed formula at della_chain.sbatch:317; probe submit DRYRUN prints 4 held legs with afterany chain. **Round D closed.**
- **Result** — Chain kit is launch-ready. Swap executing: scancel racers 12313040/12313041 (Yixun-approved; timing = start of the probe+chain submission sequence, disclosed 12:20 board), push, probe.
