# exp_15 yaw_aug — lab notebook (append-only)

## 2026-08-10T23:32:41-04:00 — Scaffold + fact-finding + design verification with Yixun

- **Goal** — Open exp_15: train vanilla FLAC with random-yaw training augmentation on the standard AR train split; compare against a matched no-aug vanilla control on the full Table-1 eval. Scaffold the folder, gather the facts the plan depends on, verify design alignment with Yixun before planning.
- **Result** — `passed`. Folder scaffolded; `yaw_aug_yixun_query.md` written. Facts established (zero new compute, bounded reads only):
  - **Control exists.** exp_11 VANL: vanilla conditioning, rung 8×8 (8×L40 × micro 8 = eff 64 = SyncBN batch), training seed 42, 40k steps, job 3661520, commit `81ddac3`, config = exp_11's `FLAC_AR_VANCKPT.json` (sha `733ca52b…` matches the committed registry). `outputs_FLAC/exp11_VANL/FLAC_exp11_VANL/exp11_VANL/checkpoints/epoch=8-step=40000.ckpt` verified on disk (written Aug 9 23:24; the job's Slurm `FAILED` state is post-save — 16 checkpoints at the 2500 cadence are all present; 40k reached in ~10.6 h).
  - **Recipe pins** (from `fa_orbit_train.sbatch` argv block): torchrun 1 node × 8 L40, `--batch-size 8 --accum-batches 1 --num-workers 6 --seed 42 --sync-batchnorm true --precision bf16-mixed --val-every -1 --gradient-clip-val 0.0 --checkpoint-every 2500`, ViT `gradient_checkpointing: true` in the conditioner configs (post-P0: mandatory for the rung), EMA on, InverseLR(1e6, 0.5, 0.99), AdamW 5e-5/(0.9,0.999)/wd 1e-3.
  - **Rotation machinery exists and is per-sample.** `src/data/yaw_rotation.py::rotate_scene_metadata(md, alpha_rad, img_w)` — exact quantized column roll (dj = round(α·W/2π) mod W) + per-pixel 3D vector rotation + all four pose fields (`source`, `source_vit`, `context_poses`, `context_poses_vit`) rotated about z; `context_audio`/`scene`/GT pass through; original dict not mutated. Training-side hook point: `DiffusionCondTrainingWrapper.training_step` receives `metadata` as a list of per-sample dicts and calls `_compute_conditioning(metadata)` (`src/training/diffusion.py:224–239`).
  - **Train split:** `data/AR/train.json` = 243 rooms, 291,210 items (exp_07 plan: 4,550 steps/epoch at eff 64) → 40k steps ≈ 8.8 epochs.
  - **exp_14 interaction:** exp_14 (yaw_gen, plan drafted, NOT yet reviewed/implemented) specifies the `--rotate-mode random --rotate-seed` extension of `eval_FLAC.py` and `draw_yaw_offsets` in `src/data/yaw_rotation.py`. exp_15's robustness readout uses the identical spec — shared-file coordination recorded in the plan (§9).
- **Design verification (AskUserQuestion, all four recommendations accepted by Yixun):** (1) control = reuse exp_11 VANL@40k; (2) budget = match exp_11 rung, 8×8/40k/seed 42; (3) eval = Table-1 θ=0 headline + random-yaw robustness secondary, both arms, both K, 5 seeds; (4) augmentation = fresh per-visit draw, uniform over all 512 columns, physically-consistent rotation with GT RIR and context audio unchanged.
- **Analysis** — The experiment is the missing "augmentation instead of architecture" arm of the program: exp_11 gave the θ=0 orbit trend, exp_14 will give trained-arm robustness, and exp_15 supplies the classical data-augmentation baseline both must be judged against. Reusing VANL makes the marginal cost one training run (~11 h on 8×L40) plus ~42 eval cells.
- **Next** — Write `plan_yaw_aug.md`; then Codex plan review; then Yixun approval. No implementation before approval.

## 2026-08-11T00:05:00-04:00 — Plan Rev 1 → Codex plan review (REVISE) → Rev 2

- **Goal** — SOP plan-review loop before Yixun approval.
- **Change** — `plan_yaw_aug.md` Rev 1 written; Codex `gpt-5.6-sol` xhigh plan review run (`codex exec -s read-only`, prompt forbade installs/env changes); review saved as `yaw_aug_codex_plan_review.md`; plan revised to Rev 2 addressing all 12 findings (3 BLOCKING, 8 MAJOR, 1 MINOR — §11 changelog in the plan maps each to its disposition).
- **Result** — `passed` (loop closed at the plan stage). Load-bearing review facts, each independently verified against the repo before adoption:
  - **exp_14 moved concurrently while Rev 1 was being written**: its plan is now Rev 2 (190 lines, post its own Codex review) with a full assignment-integrity contract (`input_hash`/`assignment_hash`, 6,337-tuple substitution guard, `_rotrand<seed>` naming, batch/worker pins) and an explicit §9 seam split — **exp_14 owns `eval_FLAC.py` + shared yaw helpers; exp_15 owns the training-side hook**. exp_14 round 1 has already landed: `9e737a1` (`draw_yaw_offsets`/`offsets_to_radians`), `66e6ca5` (fixed-mode byte-compat contract). Rev 1's "first-lander owns" language was therefore wrong and is replaced by a hard dependency (plan §6.1).
  - **VANL's 40k checkpoint is NOT sha-pinned in exp_11's registry** (entry has no `final_ckpt_sha256` — job 3661520 ended `FAILED` post-save and the field was never backfilled). Rev 2 adds an exp_15-owned committed control-admission record + per-cell re-hash gate.
  - **exp_11's train kit now pins 100,000 steps** (`PINNED_MAXSTEPS=100000`, assert at `fa_orbit_train.sbatch:189` — the Q10 extension era). Rev 2 enumerates the 40k re-pin + guardtests.
  - **eval_FLAC defaults are unsafe** (`--frame-avg-angles` defaults to the C4 list, `--cond-autocast` defaults to `default`≠bf16; `eval_FLAC.py:589-591`). Rev 2 §4.2 pins literal argv per cell class.
  - Statistics tightened (one confirmatory family = H1 co-primaries Holm-2; EQUIV retired; mandatory scope-of-inference statement), resume determinism made counter-based/stateless, no-op audit upgraded (pin diff allowlist + golden disabled-path fixture), YAWAUG@90° demoted from gate to descriptive.
- **Analysis** — The three BLOCKING findings were all real and all verifiable in-repo; the review also caught a stale cost model (depth is 3×256×512, not 3×64×512). No finding contradicts the four Yixun-confirmed design decisions, which stand unchanged.
- **Next** — Present Rev 2 + review to Yixun for approval. No implementation before approval. After approval: TDD rounds per plan §6 (training-side first; eval blocked on exp_14's rounds closing).

## 2026-08-11T12:22:00-04:00 — Plan APPROVED by Yixun; Round 1 (training-side hook) opened

- **Goal** — Record approval and open TDD Round 1 per plan §6 (training-side code path only: factory parsing/guards, wrapper hook, tests §6.5-1…8; golden fixture captured FIRST at the pre-change state per §3.3-3).
- **Version Control** — Branch state at round open: HEAD `0f0acb2` (16 commits since our scaffold `809ece5` — exp_14 r2 kit rounds + **`2c91345` exp_11 q9: the VANL row landed with 5-seed evidence, headline "FRAME AVERAGING REVERSES under the new recipe"**). ⚠️ Discovery: the exp_14 session works **in this same checkout** (its unstaged `yaw_gen_screen_guardtests.sh` sits in this working tree and blocks `git pull --rebase`). Standing self-rules for exp_15: never stash/rebase/checkout-over the other session's files; all exp_15 commits are path-scoped (`git add` explicit paths only); pushes only when origin accepts a fast-forward, never forced.
- **Change** — none yet this entry; Coder subagent (Opus 5, max effort) tasked with Round 1. Round base SHA: `0f0acb2`.
- **Command / Validation** — Coder ordered to: commit the golden disabled-path fixture harness + captured fixture BEFORE any production edit; then red→green TDD cycles (step-seed mix → factory → wrapper hook), pytest teed to `yaw_aug_<ts>_pytest_r1.log` in this folder; no edits to `eval_FLAC.py`, `src/data/yaw_rotation.py`, exp_11/exp_14 folders, or the other session's unstaged file; no installs; CPU-tiny tests only (login node).
- **Acceptance criteria (Round 1)** — All §6.5-1…8 tests green; golden fixture test green with `yaw_aug` absent; existing test suite subsets still green; every commit < 200 changed lines; SHAs reported for `commits_yaw_aug.md`.
- **Result** — `in_progress` (Coder launched).
- **Next** — On Coder return: Codex per-round review (`yaw_aug_codex_code_r1_review.md`), blocking findings fixed, then Round 2 (arm config + control-admission recorder).

## 2026-08-11T13:00:00-04:00 — Round 1 Coder output (training-side hook) — GREEN, review pending

- **Goal** — Round 1 implementation per plan §6.2/§6.3/§6.5 (Coder: Opus 5 max effort).
- **Change** — `src/training/diffusion.py` (`_splitmix64`/`_yaw_aug_step_seed`, ctor flags, `on_fit_start` banner, `_check_yaw_aug_metadata`, `_apply_yaw_aug`, one `training_step` call site), `src/training/factory.py` (`_parse_yaw_aug_config`, fail-closed), `src/tests/test_yaw_aug_training.py` + `src/tests/fixtures/exp15_yaw_aug_disabled_golden.json` (golden captured PRE-change with the real DiT forward, digest-exact), 11 teed pytest logs.
- **Version Control** — Round base drifted to `61b464f` (concurrent exp_14 commits; disjoint). Commits: `7062c00` (golden fixture, pre-change) → `72bab56` (cycle 1 step-seed) → `8c4ac4b` (cycle 2 factory) → `f8983de` (cycle 3 wrapper hook) → `28f66d5` (logs). Not pushed (per round protocol; push after review closes).
- **Command / Validation** — Full red→green record in `yaw_aug_2026-08-11_*_pytest_r1.log`: reds at 12-33-32 (9F), 12-35-33 (24F), 12-38-25 (28F); final combined **397 passed / 0 failed** (12-44-13). Existing suites (yaw symmetry, invariant conditioning, dispatch, train flags, exp_14 snapshot) all green.
- **Result** — `passed` (implementation); round OPEN until Codex review + fixes close it.
- **Analysis** — Coder findings the kit round must absorb: (1) **`--accum-batches 1` is now load-bearing** — PL `global_step` counts optimizer steps, so accumulation >1 would give identical yaw draws to every micro-batch in a window; add an explicit kit gate. (2) Flash-attn is availability-gated, not device-gated — any CPU tiny-forward must null `src.models.transformer.flash_attn_func`. (3) `self.global_step`/`global_rank` proxy to 0 with no Trainer (testability) and are true under DDP; PL restores `global_step` on resume ⇒ resume-exactness holds with world size + micro pinned. (4) Banner is `on_fit_start`/rank-0, exactly once, grep-able. (5) Strict block-equality config gate is consistent with the parser (unknown keys rejected). (6) Coder deviations 1–6 recorded verbatim in its report; to be adjudicated by the Codex review (esp. real-DiT golden, >200-line test commits, constructor defense-in-depth guards).
- **Next** — Codex r1 code review (`yaw_aug_codex_code_r1_review.md`); blocking findings fixed before Round 2.

## 2026-08-11T13:25:00-04:00 — Codex r1 review: REVISE (2 MAJOR, 2 MINOR, 1 NIT); fix loop opened

- **Goal** — Per-round review of commits `7062c00`…`28f66d5` (SOP: round closes only after blocking findings are fixed and re-verified).
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_code_r1_review.md`. Findings: **(1) MAJOR** — torch 2.7.0 CPU `Generator.manual_seed` uses only the low 32 bits, so the 63-bit step-seed collapses; reviewer *empirically verified* 10 effective-seed collisions over the exact 40,000×8 (step,rank) domain (e.g. (526,2) ≡ (10156,7) produce identical offset streams). Fix: keyed collision-free 32-bit bijection over (step,rank) before `manual_seed` + regression tests (torch high-bit alias demo + full-domain distinctness). **(2) MAJOR** — pose guard `if key in md` silently permits missing pose fields (partial rotation instead of fail-closed) and 0-d poses raise `IndexError` not `ValueError`. Fix: require all four `POSE_KEYS`, type/ndim checks, per-key deletion/scalar/non-tensor tests, device assertions. **(3) MINOR** — banner needs `flush=True` (FIFO-buffered stdout could hide it from the launch log gate). **(4) MINOR** — ctor defense-in-depth coerces (`bool()`/`int()`) instead of rejecting; validate raw args. **(5) NIT** — golden regeneration must require an explicit capture SHA (writer currently records "unknown"). All six Coder deviations ACCEPTED (dev-5 with bookkeeping correction: strict numstat puts three commits >200 lines incl. tests; SOP "generally" — no rewrite). Accum-hazard adjudicated: kit-gate approach CONFIRMED sound.
- **Analysis** — Finding 1 is scientifically negligible (10 repeated 8-sample offset vectors among 320k) but the decorrelation contract must be true, and the fix is cheap and structural (bijectivity ⇒ zero collisions). No finding touches the disabled path, the golden fixture's validity, or the factory's literal pre-change call (all adjudicated Pass).
- **Next** — Coder fix loop (same agent, full context): findings 1–5; then re-verify (tests + golden green), log closure, push, open Round 2.

## 2026-08-11T13:40:00-04:00 — Round 1 CLOSED (fixes applied, re-verified, Planner-verified)

- **Goal** — Close the r1 loop per SOP (fixes → re-verify → log with SHAs).
- **Change** — Fix commits: `076e11a` (F1: keyed 32-bit bijection `fmix32(v ^ k_lo) ^ k_hi`, `v=(step<<12)|rank`, fail-closed domain asserts step<2^20/rank<2^12; full 320,000-cell distinctness test + torch high-bit-alias documentation test + reviewer collision-pair regression), `8a33234` (F2: all four POSE_KEYS required, type/ndim before shape[-1], per-key deletion/scalar/non-tensor tests, device assertions), `a10e409` (F3 banner flush=True; F4 ctor validates raw args, no coercion; F5 golden writer demands explicit capture SHA), `aa3a66f` (fixture-safety hardening), `2e9b887` (logs).
- **Command / Validation** — Red→green per fix in `yaw_aug_2026-08-11_*_pytest_r1fix.log`; combined final **437 passed / 0 failed** (13-13-48). Planner spot-verified both MAJOR fixes in the diffs (bijectivity construction and the required-pose guard). Golden fixture byte-identical to capture commit `7062c00` (sha `ae95e096…`).
- **Result** — `passed`; **Round 1 CLOSED**. Incident on record (Coder-reported, resolved): during the F5 red phase the then-unguarded writer briefly overwrote the fixture; caught pre-stage, restored from `7062c00`; the accidental re-capture at fully-fixed code matched **byte-identically** on every digest — unplanned extra confirmation the disabled path never moved. Test hardened so this fails loudly in future.
- **Analysis** — The r1 net state: counter-based draws now collision-free by construction over the armed domain; schema guard fail-closed on all four poses; banner FIFO-safe; ctor strict. Nothing touched the eval seam or the disabled path.
- **Next** — Push r1; open Round 2 (arm config `FLAC_AR_YAWAUG.json` + §6.5-9 config-diff test; control-admission recorder §6.4 + §6.5-10 tests + the real committed record).
