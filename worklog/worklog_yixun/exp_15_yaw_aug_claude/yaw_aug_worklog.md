# exp_15 yaw_aug — lab notebook (append-only)

## 2026-08-10T23:32:41-04:00 — Scaffold + fact-finding + design verification with Yixun

- **Goal** — Open exp_15: train vanilla FLAC with random-yaw training augmentation on the standard AR train split; compare against a matched no-aug vanilla control on the full Table-1 eval. Scaffold the folder, gather the facts the plan depends on, verify design alignment with Yixun before planning.
- **Result** — `passed`. Folder scaffolded; `yaw_aug_yixun_query.md` written. Facts established (zero new compute, bounded reads only):
  - **Control exists.** exp_11 VANL: vanilla conditioning, rung 8×8 (8×L40 × micro 8 = eff 64 = SyncBN batch), training seed 42, 40k steps, job 3661520, commit `81ddac3`, config = exp_11's `FLAC_AR_VANCKPT.json` (sha `733ca52b…` matches the committed registry). `outputs_FLAC/exp11_VANL/FLAC_exp11_VANL/exp11_VANL/checkpoints/epoch=8-step=40000.ckpt` verified on disk (written Aug 9 23:24; the job's Slurm `FAILED` state is post-save — 16 checkpoints at the 2500 cadence are all present; 40k reached in ~10.6 h).
  - **Recipe pins** (from `fa_orbit_train.sbatch` argv block): torchrun 1 node × 8 L40, `--batch-size 8 --accum-batches 1 --num-workers 6 --seed 42 --sync-batchnorm true --precision bf16-mixed --val-every -1 --gradient-clip-val 0.0 --checkpoint-every 2500`, ViT `gradient_checkpointing: true` in the conditioner configs (post-P0: mandatory for the rung), EMA on, InverseLR(1e6, 0.5, 0.99), AdamW 5e-5/(0.9,0.999)/wd 1e-3.
  - **Rotation machinery exists and is per-sample.** `src/data/yaw_rotation.py::rotate_scene_metadata(md, alpha_rad, img_w)` — exact quantized column roll (dj = round(α·W/2π) mod W) + per-pixel 3D vector rotation + all four pose fields (`source`, `source_vit`, `context_poses`, `context_poses_vit`) rotated about z; `context_audio`/`scene`/GT pass through; original dict not mutated. Training-side hook point: `DiffusionCondTrainingWrapper.training_step` receives `metadata` as a list of per-sample dicts and calls `_compute_conditioning(metadata)` (`src/training/diffusion.py:224–239`).
  - **Train split:** `data/AR/train.json` = 243 rooms, 291,210 items (exp_07 plan: 4,550 steps/epoch at eff 64) → 40k steps ≈ 8.8 epochs.
  - **exp_14 interaction:** exp_14 (yaw_gen, plan drafted, NOT yet reviewed/implemented) specifies the `--rotate-mode random --rotate-seed` extension of `eval_FLAC.py` and `draw_yaw_offsets` in `src/data/yaw_rotation.py`. exp_15's robustness readout uses the identical spec — shared-file coordination recorded in the plan (§9).
- **Design verification (AskUserQuestion, all four recommendations accepted by Yixun):** (1) control = reuse exp_11 VANL@40k; (2) budget = match exp_11 rung, 8×8/40k/seed 42; (3) eval = Table-1 θ=0 headline + random-yaw robustness secondary, both arms, both K, 5 seeds; (4) augmentation = fresh per-visit draw, uniform over all 512 columns, physically-consistent rotation with GT RIR and context audio unchanged.
- **Analysis** — The experiment is the missing "augmentation instead of architecture" arm of the program: exp_11 gave the θ=0 orbit trend, exp_14 will give trained-arm robustness, and exp_15 supplies the classical data-augmentation baseline both must be judged against. Reusing VANL makes the marginal cost one training run (~11 h on 8×L40) plus ~42 eval cells.
- **Next** — Write `plan_yaw_aug.md`; then Codex plan review; then Yixun approval. No implementation before approval.

## 2026-08-11T00:05:00-04:00 — Plan Rev 1 → Codex plan review (REVISE) → Rev 2

- **Goal** — SOP plan-review loop before Yixun approval.
- **Change** — `plan_yaw_aug.md` Rev 1 written; Codex `gpt-5.6-sol` xhigh plan review run (`codex exec -s read-only`, prompt forbade installs/env changes); review saved as `yaw_aug_codex_plan_review.md`; plan revised to Rev 2 addressing all 12 findings (3 BLOCKING, 8 MAJOR, 1 MINOR — §11 changelog in the plan maps each to its disposition).
- **Result** — `passed` (loop closed at the plan stage). Load-bearing review facts, each independently verified against the repo before adoption:
  - **exp_14 moved concurrently while Rev 1 was being written**: its plan is now Rev 2 (190 lines, post its own Codex review) with a full assignment-integrity contract (`input_hash`/`assignment_hash`, 6,337-tuple substitution guard, `_rotrand<seed>` naming, batch/worker pins) and an explicit §9 seam split — **exp_14 owns `eval_FLAC.py` + shared yaw helpers; exp_15 owns the training-side hook**. exp_14 round 1 has already landed: `9e737a1` (`draw_yaw_offsets`/`offsets_to_radians`), `66e6ca5` (fixed-mode byte-compat contract). Rev 1's "first-lander owns" language was therefore wrong and is replaced by a hard dependency (plan §6.1).
  - **VANL's 40k checkpoint is NOT sha-pinned in exp_11's registry** (entry has no `final_ckpt_sha256` — job 3661520 ended `FAILED` post-save and the field was never backfilled). Rev 2 adds an exp_15-owned committed control-admission record + per-cell re-hash gate.
  - **exp_11's train kit now pins 100,000 steps** (`PINNED_MAXSTEPS=100000`, assert at `fa_orbit_train.sbatch:189` — the Q10 extension era). Rev 2 enumerates the 40k re-pin + guardtests.
  - **eval_FLAC defaults are unsafe** (`--frame-avg-angles` defaults to the C4 list, `--cond-autocast` defaults to `default`≠bf16; `eval_FLAC.py:589-591`). Rev 2 §4.2 pins literal argv per cell class.
  - Statistics tightened (one confirmatory family = H1 co-primaries Holm-2; EQUIV retired; mandatory scope-of-inference statement), resume determinism made counter-based/stateless, no-op audit upgraded (pin diff allowlist + golden disabled-path fixture), YAWAUG@90° demoted from gate to descriptive.
- **Analysis** — The three BLOCKING findings were all real and all verifiable in-repo; the review also caught a stale cost model (depth is 3×256×512, not 3×64×512). No finding contradicts the four Yixun-confirmed design decisions, which stand unchanged.
- **Next** — Present Rev 2 + review to Yixun for approval. No implementation before approval. After approval: TDD rounds per plan §6 (training-side first; eval blocked on exp_14's rounds closing).

## 2026-08-11T12:22:00-04:00 — Plan APPROVED by Yixun; Round 1 (training-side hook) opened

- **Goal** — Record approval and open TDD Round 1 per plan §6 (training-side code path only: factory parsing/guards, wrapper hook, tests §6.5-1…8; golden fixture captured FIRST at the pre-change state per §3.3-3).
- **Version Control** — Branch state at round open: HEAD `0f0acb2` (16 commits since our scaffold `809ece5` — exp_14 r2 kit rounds + **`2c91345` exp_11 q9: the VANL row landed with 5-seed evidence, headline "FRAME AVERAGING REVERSES under the new recipe"**). ⚠️ Discovery: the exp_14 session works **in this same checkout** (its unstaged `yaw_gen_screen_guardtests.sh` sits in this working tree and blocks `git pull --rebase`). Standing self-rules for exp_15: never stash/rebase/checkout-over the other session's files; all exp_15 commits are path-scoped (`git add` explicit paths only); pushes only when origin accepts a fast-forward, never forced.
- **Change** — none yet this entry; Coder subagent (Opus 5, max effort) tasked with Round 1. Round base SHA: `0f0acb2`.
- **Command / Validation** — Coder ordered to: commit the golden disabled-path fixture harness + captured fixture BEFORE any production edit; then red→green TDD cycles (step-seed mix → factory → wrapper hook), pytest teed to `yaw_aug_<ts>_pytest_r1.log` in this folder; no edits to `eval_FLAC.py`, `src/data/yaw_rotation.py`, exp_11/exp_14 folders, or the other session's unstaged file; no installs; CPU-tiny tests only (login node).
- **Acceptance criteria (Round 1)** — All §6.5-1…8 tests green; golden fixture test green with `yaw_aug` absent; existing test suite subsets still green; every commit < 200 changed lines; SHAs reported for `commits_yaw_aug.md`.
- **Result** — `in_progress` (Coder launched).
- **Next** — On Coder return: Codex per-round review (`yaw_aug_codex_code_r1_review.md`), blocking findings fixed, then Round 2 (arm config + control-admission recorder).

## 2026-08-11T13:00:00-04:00 — Round 1 Coder output (training-side hook) — GREEN, review pending

- **Goal** — Round 1 implementation per plan §6.2/§6.3/§6.5 (Coder: Opus 5 max effort).
- **Change** — `src/training/diffusion.py` (`_splitmix64`/`_yaw_aug_step_seed`, ctor flags, `on_fit_start` banner, `_check_yaw_aug_metadata`, `_apply_yaw_aug`, one `training_step` call site), `src/training/factory.py` (`_parse_yaw_aug_config`, fail-closed), `src/tests/test_yaw_aug_training.py` + `src/tests/fixtures/exp15_yaw_aug_disabled_golden.json` (golden captured PRE-change with the real DiT forward, digest-exact), 11 teed pytest logs.
- **Version Control** — Round base drifted to `61b464f` (concurrent exp_14 commits; disjoint). Commits: `7062c00` (golden fixture, pre-change) → `72bab56` (cycle 1 step-seed) → `8c4ac4b` (cycle 2 factory) → `f8983de` (cycle 3 wrapper hook) → `28f66d5` (logs). Not pushed (per round protocol; push after review closes).
- **Command / Validation** — Full red→green record in `yaw_aug_2026-08-11_*_pytest_r1.log`: reds at 12-33-32 (9F), 12-35-33 (24F), 12-38-25 (28F); final combined **397 passed / 0 failed** (12-44-13). Existing suites (yaw symmetry, invariant conditioning, dispatch, train flags, exp_14 snapshot) all green.
- **Result** — `passed` (implementation); round OPEN until Codex review + fixes close it.
- **Analysis** — Coder findings the kit round must absorb: (1) **`--accum-batches 1` is now load-bearing** — PL `global_step` counts optimizer steps, so accumulation >1 would give identical yaw draws to every micro-batch in a window; add an explicit kit gate. (2) Flash-attn is availability-gated, not device-gated — any CPU tiny-forward must null `src.models.transformer.flash_attn_func`. (3) `self.global_step`/`global_rank` proxy to 0 with no Trainer (testability) and are true under DDP; PL restores `global_step` on resume ⇒ resume-exactness holds with world size + micro pinned. (4) Banner is `on_fit_start`/rank-0, exactly once, grep-able. (5) Strict block-equality config gate is consistent with the parser (unknown keys rejected). (6) Coder deviations 1–6 recorded verbatim in its report; to be adjudicated by the Codex review (esp. real-DiT golden, >200-line test commits, constructor defense-in-depth guards).
- **Next** — Codex r1 code review (`yaw_aug_codex_code_r1_review.md`); blocking findings fixed before Round 2.

## 2026-08-11T13:25:00-04:00 — Codex r1 review: REVISE (2 MAJOR, 2 MINOR, 1 NIT); fix loop opened

- **Goal** — Per-round review of commits `7062c00`…`28f66d5` (SOP: round closes only after blocking findings are fixed and re-verified).
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_code_r1_review.md`. Findings: **(1) MAJOR** — torch 2.7.0 CPU `Generator.manual_seed` uses only the low 32 bits, so the 63-bit step-seed collapses; reviewer *empirically verified* 10 effective-seed collisions over the exact 40,000×8 (step,rank) domain (e.g. (526,2) ≡ (10156,7) produce identical offset streams). Fix: keyed collision-free 32-bit bijection over (step,rank) before `manual_seed` + regression tests (torch high-bit alias demo + full-domain distinctness). **(2) MAJOR** — pose guard `if key in md` silently permits missing pose fields (partial rotation instead of fail-closed) and 0-d poses raise `IndexError` not `ValueError`. Fix: require all four `POSE_KEYS`, type/ndim checks, per-key deletion/scalar/non-tensor tests, device assertions. **(3) MINOR** — banner needs `flush=True` (FIFO-buffered stdout could hide it from the launch log gate). **(4) MINOR** — ctor defense-in-depth coerces (`bool()`/`int()`) instead of rejecting; validate raw args. **(5) NIT** — golden regeneration must require an explicit capture SHA (writer currently records "unknown"). All six Coder deviations ACCEPTED (dev-5 with bookkeeping correction: strict numstat puts three commits >200 lines incl. tests; SOP "generally" — no rewrite). Accum-hazard adjudicated: kit-gate approach CONFIRMED sound.
- **Analysis** — Finding 1 is scientifically negligible (10 repeated 8-sample offset vectors among 320k) but the decorrelation contract must be true, and the fix is cheap and structural (bijectivity ⇒ zero collisions). No finding touches the disabled path, the golden fixture's validity, or the factory's literal pre-change call (all adjudicated Pass).
- **Next** — Coder fix loop (same agent, full context): findings 1–5; then re-verify (tests + golden green), log closure, push, open Round 2.

## 2026-08-11T13:40:00-04:00 — Round 1 CLOSED (fixes applied, re-verified, Planner-verified)

- **Goal** — Close the r1 loop per SOP (fixes → re-verify → log with SHAs).
- **Change** — Fix commits: `076e11a` (F1: keyed 32-bit bijection `fmix32(v ^ k_lo) ^ k_hi`, `v=(step<<12)|rank`, fail-closed domain asserts step<2^20/rank<2^12; full 320,000-cell distinctness test + torch high-bit-alias documentation test + reviewer collision-pair regression), `8a33234` (F2: all four POSE_KEYS required, type/ndim before shape[-1], per-key deletion/scalar/non-tensor tests, device assertions), `a10e409` (F3 banner flush=True; F4 ctor validates raw args, no coercion; F5 golden writer demands explicit capture SHA), `aa3a66f` (fixture-safety hardening), `2e9b887` (logs).
- **Command / Validation** — Red→green per fix in `yaw_aug_2026-08-11_*_pytest_r1fix.log`; combined final **437 passed / 0 failed** (13-13-48). Planner spot-verified both MAJOR fixes in the diffs (bijectivity construction and the required-pose guard). Golden fixture byte-identical to capture commit `7062c00` (sha `ae95e096…`).
- **Result** — `passed`; **Round 1 CLOSED**. Incident on record (Coder-reported, resolved): during the F5 red phase the then-unguarded writer briefly overwrote the fixture; caught pre-stage, restored from `7062c00`; the accidental re-capture at fully-fixed code matched **byte-identically** on every digest — unplanned extra confirmation the disabled path never moved. Test hardened so this fails loudly in future.
- **Analysis** — The r1 net state: counter-based draws now collision-free by construction over the armed domain; schema guard fail-closed on all four poses; banner FIFO-safe; ctor strict. Nothing touched the eval seam or the disabled path.
- **Next** — Push r1; open Round 2 (arm config `FLAC_AR_YAWAUG.json` + §6.5-9 config-diff test; control-admission recorder §6.4 + §6.5-10 tests + the real committed record).

## 2026-08-11T14:20:00-04:00 — Round 2 Coder output (arm config + control admission) — GREEN, review pending

- **Goal** — Round 2 per plan §3.3-1/§6.4/§6.5-9,10.
- **Change** — `FLAC_AR_YAWAUG.json` (byte-copy of exp_11 `FLAC_AR_VANCKPT.json` + exactly the `training.yaw_aug` block; byte-level diff test), `yaw_aug_record_control.py` (TDD, 10 tests) + the real committed `yaw_aug_control_admission.json`, new tests `src/tests/test_yaw_aug_arm_config.py` / `test_yaw_aug_record_control.py`.
- **Version Control** — Commits `075b787` (arm config) → `d2c11db` (recorder) → `389d0d8` (embedded-config binding) → `970110f` (real record + logs) → `b9de6c4` (final log). Not pushed until review closes.
- **Command / Validation** — Red→green per deliverable in `yaw_aug_2026-08-11_*_pytest_r2.log`; final **456 passed / 0 failed** (13-28-13); golden disabled-path still green. Real record: ckpt sha `1095f493…` (723,922,539 B, sha reproduced across two independent streaming passes), `global_step` 40000 / epoch 8, EMA family `diffusion_ema.ema_model.` **210 keys**, config file sha = registry pin `733ca52b…`, **embedded `model_config` equal to `FLAC_AR_VANCKPT.json`** (canonical sha `2023ccc6…`).
- **Result** — `passed` (implementation); round OPEN until Codex review closes.
- **Analysis** — Coder findings for the eval-kit G4 gate: (1) PL embeds the full training config as top-level `model_config` — the admission gate should re-validate it canonically (strongest binding available, proves the ckpt was TRAINED with the pinned config); (2) `weights_only=True` works on this ckpt under torch 2.7 — the gate can use the safe loader; (3) **EMA covers the DiT only** (210 keys vs online 1066 = conditioner 561 / pretransform 295 / model 210) — a whole-model EMA assert would falsely fail; correct invariant: EMA count == online `diffusion.model.*` count; EMA eval = EMA'd DiT + online conditioner/VAE, identical for both arms, to be stated in the write-up; (4) transparency: the first real record predates the embedded-config binding and was deleted BEFORE staging (never committed/used), re-run produced the committed record — both logs committed.
- **Next** — Codex r2 review (`yaw_aug_codex_code_r2_review.md`); then Round 3 (launch kit).

## 2026-08-11T15:05:00-04:00 — Codex r2 review: REVISE (1 BLOCKING, 3 MAJOR, 2 MINOR); fix loop opened

- **Goal** — Per-round review of `075b787`…`b9de6c4`.
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_code_r2_review.md`. Findings: **(1) BLOCKING** — recorder TOCTOU (checkpoint loaded, then separately re-opened for stat/sha — a swap between opens records facts from file A with the hash of file B; config likewise) AND an unsafe `weights_only=False` fallback (never ran here per the transcript, but a fail-closed admission tool must not contain it). **(2) MAJOR** — EMA detection fail-open (prefix fallback, single-tensor acceptance); the proposed count-equality G4 invariant insufficient — require exact suffix-set equality `diffusion.model.*` ≡ `diffusion_ema.ema_model.*` + shapes/dtypes + inventory digest. **(3) MAJOR** — comparisons not type-strict (`1==1.0`, `True==1`, `int("40000")`); compare canonical bytes/hashes; step must be a non-boolean int == 40000. **(4) MAJOR** — the "byte-level" arm-config test is text-level (`read_text` normalizes CRLF); artifact itself verified correct by raw git diff. **(5) MINOR** — no-overwrite is check-then-write, not O_EXCL. **(6) MINOR** — no test pins the real committed record (456-green does not protect the evidence artifact). Extension (embedded-config binding) ACCEPTED — it implements what plan §3.3-1 actually asks; deletion incident ACCEPTED as a transparent pre-admission correction (evidence chain verified clean: both transcripts + sole record entered together in `970110f`, same ckpt sha in both).
- **Analysis** — All six findings are real; 1–4 gate closure, 5–6 join the same fix cycle. The admission record must be REGENERATED under the corrected recorder in an explicit review-fix commit (git history preserves the superseded `970110f` artifact; no history rewrite).
- **Next** — Coder fix loop; then re-verify, close r2, push, open Round 3 (launch kit).

## 2026-08-11T16:05:00-04:00 — Round 2 CLOSED (fixes applied, record regenerated, Planner-verified)

- **Goal** — Close the r2 loop per SOP.
- **Change** — Fix commits: `30d5065` (F1: no unsafe fallback; fd-pinned hashing via os.pread on an open descriptor + device/inode/size/mtime_ns identity before and after the safe load; config read once as bytes), `7e7c512` (F2: EMA must mirror online DiT — suffixes+shapes+dtypes; `online_model_key_count`; `ema_inventory_sha256`), `a3719f9` (F3: type-strict JSON domain + canonical-bytes comparison + non-bool-int step), `1e7ef90` (F5 O_CREAT|O_EXCL|O_NOFOLLOW; F4 genuine byte-level arm diff + CRLF negative control), `ae7a924` (record REGENERATED under corrected recorder: git rm + fresh run + transcript; superseded record preserved in history at `970110f`), `f51578c` (F6: committed record pinned by test, cross-refs read from exp_11 registry at test time), `d33d0a2` (final log).
- **Command / Validation** — Red→green per fix in `yaw_aug_2026-08-11_*_pytest_r2fix.log`; final **485 passed / 0 failed** (13-57-04). Planner spot-verified F1 in the diff. Regenerated record: **ckpt sha UNCHANGED `1095f493…`** (three streaming passes, two code paths), step 40000, strict EMA mirror PASSED on the real checkpoint (210 ≡ 210, exact suffixes/shapes/dtypes), `ema_inventory_sha256 68dc5ef5…`, embedded canonical == file canonical `2023ccc6…`.
- **Result** — `passed`; **Round 2 CLOSED**.
- **Analysis** — G4 gate contract now fully re-derivable from the checkpoint alone (recompute sha, step, EMA inventory digest, embedded-vs-file canonical config hash — never trust the record's booleans). The EMA invariant for the gate: suffix/shape/dtype equality vs `diffusion.model.*` (210), never the 1066 all-`diffusion.*` count.
- **Next** — Push r2; open Round 3 (launch kit per plan §6.6).

## 2026-08-11T17:00:00-04:00 — Round 3 Coder output (launch kit) — GREEN locally, review pending

- **Goal** — Round 3 per plan §6.6 + §3.3-2/5: the training launch kit. NO Slurm submissions (verified: the guardtest suite self-greps for sbatch and passes).
- **Change** — Commits `a4bbe86` (verbatim exp_11 kit copies, cmp-verified) → `90f2c3c` (all enumerated deltas: exp15 identity, 40k re-pin + assertion, semantic config gate incl. yaw_aug literal block + NEW ViT-img_w==yaw_aug-img_w check, accumulation gate, banner watcher + PYTHONUNBUFFERED, pin-diff allowlist gate + `yaw_aug_pin_allowlist.txt`, VANL-manifest env cross-check, argv parity, registry with final_ckpt_sha256 at completion, exit class 8 = banner absent) → `b71ca45` (guardtests re-aimed; caught 2 launcher bugs incl. restart-cap ordering) → `9fa7067` (set -u fix) → `548d5db` (transcripts).
- **Command / Validation** — Guardtests **98 passed / 0 failed** (`yaw_aug_2026-08-11_14-21-08_guardtests_r3.log`; sections A–Q incl. budget-pin both directions, restart cap, accum gate, 10 mutated configs through the real gate, allowlist real-mode fatal on planted file, banner watcher rejects wrong-seed/wrong-img_w banners). DRYRUN argv verbatim in the Coder report — Planner eyeballed vs plan §3.2: matches (40000/8×8/accum1/seed42/SyncBN/bf16-mixed/ckpt-2500, YAWAUG identity).
- **Result** — `passed` (implementation); round OPEN until Codex review closes.
- **Analysis** — Load-bearing new facts: (1) **train.py and defaults.ini are UNCHANGED since the control's commit `81ddac3`** — the aug arm runs literally the entry point VANL ran; production diff = factory/diffusion/yaw_rotation + tests only, allowlist-verified by an independent fnmatch guardtest. (2) Structural deviations (6, all reasonable): arm-agnostic exp_11 helpers invoked-not-copied but drift-gated; init-hash gate N retired in favor of the stronger byte-level config proof + ViT pin; `--extension` mode dropped (stricter contract retained); unreachable restart-cap line spool-tested; exit class 8 added; the kit writes the registry completion fields exp_11 never backfilled.
- **Next** — Codex r3 review; then the SOP integrative `full` review of the whole exp_15 diff; then ladder (DRYRUN done → SMOKE on the rung → launch).

## 2026-08-12T09:45:00-04:00 — Overnight reconciliation: r3 review lost, branch rebased, exp_14 campaign landed, exp_11 re-active

- **Goal** — Re-establish ground truth after the overnight gap before resuming the r3 review → full review → smoke → launch pipeline.
- **Result** — `passed` (reconciliation):
  1. **The r3 Codex review never completed** — its background task died with the idle session (no output artifact). Rerun required; no round state lost (r3 commits intact).
  2. **SHA drift from the shared checkout's rebase:** the other session's `git pull --rebase` rewrote our unpushed r3 commits. Map (old → new): `a4bbe86`→`50a526d` (verbatim copies), `90f2c3c`→`155d57a` (deltas), `b71ca45`→`0dd59a9` (guardtests), `9fa7067`→`889898d` (set -u fix), `548d5db`→`5829ef2` (transcripts). All five now pushed (origin contains `5829ef2`). Worklog r3 entry's old SHAs are superseded by this map.
  3. **exp_14's campaign ran and landed overnight** (its session): Z rows published to `model_comparison.md` incl. the **VANL baseline row** (announcement-04 regen at `8499030`, one-pin `e8ca26e`, G1–G4 ALL PASS, G5 exact reproduction). Two facts exp_15 must absorb: (a) our §5 external-check reference rows now exist; (b) **exp_14 r3-fix4 corrected the per-scene aggregation to 10 ROOM FAMILIES (not 17 rooms)** — exp_15's collector (§6.8) must adopt the same grouping, and the plan's "17 rooms" phrasing refers to the split, not the aggregation unit.
  4. **exp_11 is ACTIVE** (the tracker's earlier "closed 2026-08-10" was another session's misread, per its rewritten row): five 100k extension legs PENDING (jobs 3684149–53, 8×L40 each). ⚠️ Its first extension legs (3680875–78) **were killed by test-only commits tripping the content-scoped launch gate**; the gate now excludes `src/tests` (`2b75036`, Codex-approved). Standing implication for exp_15: worklog/exp_15-kit commits are safe while exp_11 legs pend; any future `src/` (non-test) commit is NOT — coordinate first.
  5. **L40 pool saturated**: 32 nodes all in mixed allocation; exp_11's five legs pend on Priority. exp_15's smoke + 24 h training job will contend with Yixun's own exp_11 legs → queueing policy escalated to Yixun (ask-don't-assume rule, CLAUDE.md 2026-08-11).
- **Next** — Rerun Codex r3 review (corrected SHAs); tracker row refresh; Yixun's queueing decision; then full review → smoke → launch per the standing authorization.

## 2026-08-12T10:20:00-04:00 — Codex r3 review (rerun): REVISE — 3 BLOCKING, 3 MAJOR, 1 MINOR, 1 NIT; fix loop opened

- **Goal** — Per-round review of the launch kit (`50a526d`…`5829ef2`), rerun after the lost 08-11 invocation.
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_code_r3_review.md`. Blockers: **(1)** SMOKE accepts wrong topologies AND writes the production registry — the required smoke→launch sequence would abort the real INITIAL as already-registered (DRYRUN exits before that block, so 98 guardtests never exercised it); **(2)** completion writing is not round-2-rigorous (mtime-picked ckpt, no fd-pinning/safe-load/step/config/EMA verification, `final_step` from MAXSTEPS not the ckpt, registry-write failure only warns); **(3)** runtime closure omits `data/AR/train.json` and the drift `git status` can fail open (stderr discarded, exit ignored). Majors: **(4)** exp_15 has the same over-broad pending-job closure exp_11 just fixed in `2b75036` (src/tests in the closure — a test-only commit would abort our queued job); port the content-scoped binding; **(5)** banner gate: substring match (`seed=420` passes), no banner-before-step ordering, class 8 over-assigned (masks OOM/infra); **(6)** VANL-manifest hash and parse are two separate opens (same snapshot-binding class as r2 F1). Minor: guardtest spool harness ignores cp/sed failures + stray `-e` (transcript has 2 sed errors; the 98 PASS count itself verified genuine). Nit: pre-rebase SHA in a comment. Deviations: 1–4 ACCEPTED, 5–6 ACCEPT-INTENT/REVISE-IMPLEMENTATION. Also confirmed clean: 40k re-pin complete, argv parity catches dropped SyncBN/changed precision, copy byte-identity via git blob IDs, allowlist anchoring correct and real-mode fatal.
- **Analysis** — F1 would have burned the smoke AND stranded the launch; F4 is the operational lesson from exp_11's killed legs applied to us. All findings are pre-compute catches — nothing invalidates rounds 1–2.
- **Next** — Coder fix loop (findings 1–8); regenerate guard/DRYRUN evidence; then the integrative `full` review; then smoke → launch (Yixun queue policy 2026-08-12: submit both when the ladder passes).

## 2026-08-12T11:15:00-04:00 — Round 3 CLOSED (all 8 findings fixed, re-verified, Planner-verified)

- **Goal** — Close the r3 loop.
- **Change** — Fix commits `bf4c861` (F1–F6, F8 in the kit) → `e65fe8d` (F7 + guardtest sections R/S/T/U) → `efbf9f8` (worktree cases → DRYRUN; drift advisory names offending paths) → `3676223` (GUARD_TAG param) → `72c536c` (clean transcripts).
- **Command / Validation** — Guardtests **145 passed / 0 failed, no SKIPs, clean transcript** (`yaw_aug_2026-08-12_10-42-44_guardtests_r3fix.log`; 47 new cases: smoke isolation R, completion audit S, closure/content-binding T incl. throwaway-worktree proofs that a test-only commit does NOT abort and a src/training commit DOES, banner/manifest U). Pytest **495 passed** (golden regression included). Fresh DRYRUN argv verified identical to plan §3.2. Planner spot-verified: smoke registry namespace switch + `:(exclude)src/tests` content closure + runtime-surface list (incl. `yaw_aug_record_control.py`, now imported by the completion gate).
- **Result** — `passed`; **Round 3 CLOSED**. Completion audit reaches round-2 rigor BY IMPORT (`snapshot_checkpoint`/`canonical_bytes`/`summarize_ema`/`canonical_sha256` from the reviewed recorder); exit classes 8 (banner) and 9 (completion audit) documented. Coder deviations 1–4 this loop all reasonable (data/AR superset closure = exp_11 parity; DRYRUN-mode worktree cases because real mode ignores test hooks BY DESIGN; class 9; smoke exercises the registry write path in its own namespace).
- **Next** — Push; integrative `full` review (SOP, last gate before compute); then smoke → 40k INITIAL per the standing authorization + queue policy.

## 2026-08-12T11:50:00-04:00 — Integrative full review: NO-GO (4 findings block smoke) + ref-move incident + F5 deferral

- **Goal** — Record the `full` review verdict, the F4 incident + resolution, and open the fix loop.
- **Result** — Review saved as `yaw_aug_codex_full_review.md` (verdict **NO-GO**): **F1 (BLOCKING)** completion audit imports helpers from the live checkout at end-of-run (late-binding — a mid-run edit could alter admission or reclassify an 11 h run); fix = snapshot recorder/config/classifier/wandb-helper into a run-owned dir at start, hash-recorded, use only copies. **F2 (BLOCKING)** plan §7 rung 4 (bounded real-AR readback) never executed — must run + tee + log before smoke. **F3 (MAJOR)** smoke not storage-light (ckpt every 10 steps ⇒ ~3×724 MB + distorted rate) and no promotion record; fix = ckpt interval beyond smoke max-step + smoke acceptance record (rate/VRAM) + production submitted only after it passes (sequenced, not queued independently — refines the queue policy within Yixun's intent). **F4 (BLOCKING)** live HEAD ≠ pushed pin — **RESOLVED** (see incident). **F5 (MAJOR)** eval kit §6.7–6.8 absent ⇒ this review is scoped "training-launch full"; **deferral recorded here**: eval implementation gets its own TDD/review rounds + a second integrative review before any eval submission (plan already sequenced it so — §6.1). F6 (restart registry entry) + F7 (allowlist count comment, pytest argv recording) fold into the fix loop. Review also confirms: all cross-round seams pass; no quiet H1-bias path found.
- **Analysis — INCIDENT (mine):** attempting F4 I moved the branch ref to `b1bcd38` after verifying safety against a state that had gone stale — the exp_14 session had pull-rebased and made two local commits (`7d36c94`,`dd77e2b`) in the interval; my `update-ref` orphaned them. Classifier blocked my restoration attempts; escalated to Yixun; before intervention was needed the exp_14 session self-healed (pull-rebase recovered its pushed `0919257`; its worktree still held the newer content, which it re-committed as `867ff95`+`5d69849` and CLOSED exp_14; verified: the three `fa_drawshare` files at HEAD are byte-identical to the orphaned tip; its closure also swept two untracked exp_15 red-phase guardtest transcripts, disclosed in `bd450d5`, verbatim). **Standing lesson (binding on this session): NEVER move refs in this shared checkout; reconcile only via pull-rebase at verified-quiet moments or via pushed state; verification and action are not atomic while another session writes.** Classification: operator error (mine), zero data loss, resolved.
- **Version Control** — HEAD = origin = `bd450d5` (pushed). Fix-loop base = `bd450d5`.
- **Next** — Coder fix loop (F1, F2 readback, F3, F6, F7); regenerate guard/DRYRUN evidence; re-run the integrative review scoped to the fixes; then smoke → acceptance record → production INITIAL.

## 2026-08-12T12:25:00-04:00 — Full-fix loop complete (F1/F2/F3/F6/F7); rung 4 EXECUTED and PASSED; scoped re-review opened

- **Goal** — Close the NO-GO findings.
- **Change** — Commits `18b9cf9` (rung-4 readback, written AND run), `9e9b2b1` (run-owned code snapshot for all end-of-run tools + hashes in manifest; storage-light smoke + `yaw_aug_smoke_acceptance.json` + fail-closed submitter promotion gate incl. SMOKE_WAIVER path; restart provenance; allowlist count), `423d449` (guardtest sections V/W/X, 34 cases), `52530a0` (clean transcripts). Pushed; HEAD == origin == `52530a0`.
- **Command / Validation** — **Rung 4 (plan §7): PASSED** — 24 real AR samples through `create_dataloader_from_config`/`acousticroom_train.json` (17.2 s CPU, `yaw_aug_2026-08-12_12-00-25_readback.log`): depth exactly (3,256,512) float **float64** finite; four poses float32 trailing-dim-3 finite; equirectangular consistency before AND after rotation; d∈{1,128,511} rotations equal an independent reference (atol 1e-4), z-row exact, (d, W−d) round-trip exact, pose norms/z preserved; context_audio/padding_mask/scene untouched. Real-data facts: depth float64 vs poses float32 (rotation exact in both — the matrix is built in the input dtype); K=8 context poses (8,3); `num_workers=1` forced by the factory's hardcoded `persistent_workers=True`. Guardtests **179 passed / 0 failed** (V: snapshot binding incl. mutated-live-copy proof; W: smoke storage/acceptance/promotion incl. refused-without-record and waiver-logged; X: restart provenance). Pytest 495 (argv recorded). Fresh DRYRUN argv unchanged. New exit class 10 (smoke failure) documented.
- **Result** — `passed`; F4 resolved (pushed pin = HEAD); F5 deferral stands. Round open pending the scoped re-review (full review's mandate).
- **Next** — Scoped Codex re-review of `18b9cf9..52530a0` → on GO: params/command artifacts + acceptance criteria → SMOKE submission → acceptance PASS → production INITIAL (Yixun queue policy + smoke-promotion gate).

## 2026-08-12T13:55:00-04:00 — Scoped re-review: F1/F2/F6/F7 CONFIRMED CLOSED; F3 needs one more cycle (NO-GO)

- **Goal** — Verify the full-fix closures.
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_rereview.md`. Confirmed closed: F1 (all late tools snapshot-bound, hashes in manifest, mutated-live-copy proof), F2 (rung 4 evidence accepted), F6, F7, storage-light mechanism, waiver bounding, pin `79c585f`. Remaining: **(1) BLOCKING** — the production promotion gate greps for the substring `"verdict": "PASS"` instead of parsing the record: malformed/nested/stale text promotes; record not bound to commit/rung/ngpu/max_steps; manifest records the path, not the record's hash. **(2) MAJOR** — the acceptance producer can PASS with `peak_vram_mb: null` (no runtime emits the searched marker), a prior PASS at the fixed path can survive a write failure, and PASS publishes before the full epilogue (late failures set class 7 after PASS exists). **(3) MINOR** — the "float64 exact" phrasing overstates: `azimuth_rotation_matrix` builds float32 coefficients upcast to the input dtype; the plan-level invariant (≤1e-4) holds; claim to be amended with the observed max error. **Correction to the 12:25 entry:** the depth-rotation exactness claim is "dtype-preserving, within 1e-4 of an independent float64 reference", not bit-exact-in-float64.
- **Next** — Focused Coder cycle (kit files only, no src/): parse-and-bind promotion gate; producer atomic-publish with real VRAM evidence and prior-record invalidation; readback max-error reporting; guardtests for every new refusal; then final gate verification → smoke.

## 2026-08-12T15:20:00-04:00 — F3 cycle closed (Planner-verified); SMOKE launch — pre-registered acceptance criteria

- **Goal** — Close the last gate finding and submit the smoke (ladder rung 6).
- **Change** — F3 commits `7578a3b`/`1a3e0e4`/`5368108` (gate parses+binds after SHA resolution — the old position bound empty strings, Coder-caught; producer supersede/VRAM-sampler/atomic-publish-last; readback max-errors: ≤1.9e-6 all families, plan invariant 1e-4 comfortably met). Guardtests **221/0**. Planner-verified per SOP (re-review not repeated: 42 targeted guardtests incl. the producer↔gate compatibility proof). Launch artifacts committed (`bd6d8b9` = launch pin; params_set_up says 5368108 — superseded by the artifact commit itself, content closure identical).
- **Acceptance criteria (SMOKE, judged against — not vibes):** job starts under sbatch at EXPECT_SHA `bd6d8b9`; every start gate green; `yaw_aug ENABLED img_w=512 seed=42` exact line before step evidence; ≥30/30 steps, no OOM/NaN; ZERO checkpoints written; acceptance record atomically published with verdict PASS: banner_seen, steps_completed, rate ≥ 0.945 steps/s (PL last-reported; warmup-ramp caveat on record), peak VRAM ≤ rung floor, torchrun/tee/wandb/preflight ok, exit class 0. FAIL ⇒ triage infra-vs-bug per SOP before any resubmission; NO production submission without a parsed+bound PASS record (gate enforces).
- **Acceptance criteria (PRODUCTION, restated from plan §8):** as plan §8 with EXPECT_SHA = HEAD at its submission (= `bd6d8b9` if unchanged); commit freeze between smoke and production submission (the record binds _meta.commit to HEAD — any interleaved commit fail-closes the gate; disclosed, retry path = fresh smoke or documented waiver, Yixun consulted if it comes to a waiver).
- **Result** — `launched` (smoke submitted; job id in `yaw_aug_command.md`).
- **Next** — Monitor smoke → on PASS record: submit production INITIAL immediately → training ~10.6 h → completion audit → registry → results phase (eval kit rounds follow per the F5 deferral).

## 2026-08-12T16:05:00-04:00 — Smoke FAIL (rate check only) → triage: measurement-design artifact → Yixun waiver → PRODUCTION LAUNCH

- **Goal** — Adjudicate smoke 3685989 and launch.
- **Result** — Smoke record: verdict FAIL on exactly `rate_at_least_0.9x_VANL` (0.2 it/s vs floor 0.945); ALL other checks PASS (banner OK, 30/30 steps, zero checkpoints, peak VRAM 3,277 MB ≪ floor, torchrun/tee/wandb/preflight ok, exit class 0; wall 762 s).
- **Analysis (infra-vs-bug: measurement design, not a model defect):** PL's displayed it/s is a running average from process start. **VANL's own training log reads 0.37 it/s @step 30, 0.81 @300, 0.89 @1000** — the floor 0.945 (0.9× whole-run 1.048) is unreachable by ANY short smoke; VANL would have failed its own gate. Like-for-like caveat recorded: aug arm 0.2 vs VANL 0.37 at step 30 (ratio 0.54) — weak slowdown signal, confounded by node/NFS-cache variance; resolved by the post-hoc windowed check below. Windowed references from VANL's log: steps 100→300 = 200/212 s = **0.943 it/s**; steps 300→1000 = 700/747 s = **0.937 it/s**.
- **Decision (Yixun, AskUserQuestion 16:00 EDT):** WAIVE the rate check and launch now; verify throughput post-hoc on the production run. **Pre-registered post-hoc criterion:** windowed rate (100→300) ≥ 0.849 (=0.9×0.943) and (300→1000) ≥ 0.843 (=0.9×0.937); breach ⇒ scancel + triage (authorized by the chosen option).
- **Command / Validation** — Production INITIAL submitted with `SMOKE_WAIVER` citing this entry (reason logged in the submission manifest); EXPECT_SHA = HEAD `bd6d8b9`. Job id in `yaw_aug_command.md`.
- **Result** — `launched`.
- **Next** — Watcher: start gates → banner → windowed rates @300/@1000 (abort criterion) → completion (~10.6 h) → completion audit/registry → results phase.

## 2026-08-13T14:20:00-04:00 — Chain round Coder output (chunked legs + self-chaining) — GREEN, review pending

- **Goal** — Implement plan §12 (Rev 3 amendment): LEG_STEPS-chunked chain with self-chaining epilogue.
- **Change** — Commits `51a8c34` (chain mode: per-leg budgets, leg-aware completion audit, self-chaining epilogue) → `9944863` (guardtest sections Y/Z/Z2, 46 cases) → `84c382b` (suite no-mutation fix + SKIP-on-dirty-closure) → `7dda3e2` (clean-worktree fixes) → `a542ea9` (transcripts).
- **Command / Validation** — Guardtests: clean worktree **256/0** (2 structural SKIPs), main checkout **245/0** (11 SKIPs where the other session's mid-edit closure files legitimately dirty the tree); union: every case green somewhere; both transcripts committed. DRYRUN argv verified: INITIAL 0→2500 (`--max-steps 2500`, time 1:30:00), RESTART 2500→5000 (`--ckpt-path` + `--max-steps 5000`), monolithic path byte-compared to the committed reference. Chain stall = exit class 12, `next_leg_command` recorded in the manifest BEFORE the first attempt, `CHAIN STALLED:` on stdout for the Monitor.
- **Result** — `passed` (implementation); round OPEN pending Codex review. Coder deviations (all reasonable, review to adjudicate): every leg records its audited boundary in a new registry `legs` list (stronger provenance than endpoint-only); two-run guardtest pattern; suite hardened against mutating the tracked acceptance record.
- **Analysis** — Monolith 3687499 est. start moved EARLIER (19:16 tonight); after these commits push it will abort fail-closed at start (accepted per amendment). If the chain is running first, I scancel it proactively to not burn the slot.
- **Next** — Codex chain-round review → fixes → push → submit chain INITIAL → cancel monolith when leg 1 RUNNING → Monitor supervises the chain.

## 2026-08-13T15:45:00-04:00 — Chain review: NO-GO (5 BLOCKING, 3 MAJOR) — two independent leg-2 killers caught pre-launch

- **Goal** — Adjudicate the chain round.
- **Result** — `fix_ready`. Review saved as `yaw_aug_codex_chain_review.md`. Blockers: **(1)** inherited ckpt preflight demands manifest max_steps == the leg's (2500 ≠ 5000) — every RESTART leg rejected at admission; **(2)** `WANDB_RESUME=must` + changed max_steps/ckpt_path — the exact exp_11 failure (their jobs 3684149/50) fixed there in `3847212`, which our copies predate; **(3)** successor submitted before final per-leg verdict (classes 7/8 can condemn a parent whose child already exists) and without `afterok` (lock race); **(4)** submission/recovery not idempotent (manifest mutated after sha-registration; replay double-submits; crash windows orphan the chain; `legs` list lacks tip continuity); **(5)** the standing waiver's post-hoc rate floors (0.849/0.843) cited but never evaluated — must gate leg-2 advancement. Majors: LEG_STEPS tunable but time pin fixed (blessed 10000-step leg in a 1:30 window); CHAIN=0 argv comparison self-referential (HEAD vs HEAD) + `legs`/`CHAIN END` leak into monolith mode; SKIP accounting false (union does NOT cover the real-mode allowlist case; literal SKIP counts 3/18 not 2/11). **PL 2.1 resume core adjudicated CORRECT** (exact leg budgets, boundary saves, no fenceposts — verified against installed PL source). Deviations: 1 accept-in-principle/revise (hash-chained legs), 2 pattern-accepted/evidence-rejected, 3 accepted. Environment notes: monolith 3687499 (est. 23:40) will fail its content gate harmlessly; no registry exists yet; stale smoke lock has no active flock; branch ahead 6/behind 4 — reconcile before push and regenerate exact-pin evidence.
- **Analysis** — The review prevented a certain leg-2 stall (two independent admission killers) and a possible double-submit corruption. All findings are wrapper-level; the scientific core (resume-exact draws, PL semantics) is confirmed.
- **Next** — Coder fix loop (all 8); reconcile + push; RERUN the chain review (reviewer mandate); then launch.

## 2026-08-13T20:05:00-04:00 — Chain re-review: 6/8 closed; F4 transaction + 2 items remain (NO-GO); monolith cancelled

- **Result** — `fix_ready`. Re-review saved (`yaw_aug_codex_chain_rereview.md`). CLOSED: F2 (W&B lineage, readback verifies the fresh id), F3 (functional no-submit + afterok), F5 (rate gate, 0.943 reproduction), F6, F7 (golden verified against 44df1a2 blob), F8 (311/311 union, STRICT sound). REMAINING: **(1) BLOCKING/F4** — the flock covers single helper calls, not the intend→squeue→sbatch→publish transaction (double-submit window); squeue failure fails OPEN; post-crash adoption can adopt a child whose afterok parent is non-successful (never releases); recovery command not state-aware; parent_ckpt_sha256 unsupplied. **(2) MAJOR/F1** — INITIAL mode must be asserted by manifest AND registry (currently either); chain flag/cap/leg not recorded for validation; submit-time reader still unchecked. **(3) MAJOR new** — FINAL_RECORD published before terminal chain classes 12/13 (a rate-breach job durably records rc=0 while exiting 13).
- **Change** — **Monolith 3687499 scancel'ed** (guaranteed-abort since the closure moved past its pin `bd6d8b9`; dead weight in the queue; per the amendment's fallback rationale). exp_11 interaction adjudicated disjoint (namespaces/locks/registries).
- **Next** — Final targeted fix loop (transactional submit under one lock; fatal scheduler-query failure; terminal-state-checked adoption; state-aware recovery op; both-must-agree identity + chain metadata; post-advancement terminal record) → verification → launch.

## 2026-08-13T20:45:00-04:00 — Coder seat change mid-loop (Opus usage limit) — Fable fork continues

- **Goal** — Continue the chain-fix2 loop after the Opus 5 Coder subagent hit its session usage limit mid-work.
- **Result** — Implementation commit `e204961` (all three re-review findings: single-transaction submit, identity completeness, post-advancement terminal record) LANDED before the cutoff; `yaw_aug_train_guardtests.sh` left mid-edit (+238/−52). **Seat change (role-attribution rule): the Coder seat is now filled by a Claude Fable 5 fork of the main session** (Opus seat limited out; Fable is the higher tier — separation of duties preserved: the fork acts as Coder, the main session remains Planner, Codex remains Reviewer). The fork finishes the guardtest adaptation + verification runs.
- **Next** — Fork completes suite + transcripts → Planner verification of the closure (the re-review's three findings are narrow and the functional tests are decisive) → launch chain INITIAL.

## 2026-08-13T17:50:00-04:00 — chain-fix2 loop complete (Fable-fork Coder); full green; final scoped verification before launch

- **Result** — `passed`. Commits `e204961` (Opus, pre-limit: transact-submit single-lock transaction, identity completeness, terminal record) + `f4671f3` (fork: **IMPLEMENTATION BUG caught by new guardtest AD on first execution** — the chain preflight's `manifest_fields()` parser dropped the `chain/leg_steps/cap` line, so every real RESTART would have been refused at admission; one-line fix) + `562be70` (guardtest completion: PID-scoped `wait` fixing a bash≥5.1 process-substitution deadlock that had hung four runs across sessions; AF drives the REAL terminal-record writer; stub signaling fix) + `aa6d3b1` (evidence). Suites: **main 321/0 (18 env-skips), STRICT worktree 333/0 (6 env-skips), union 337/337 SATISFIED**. Race/stranded/replay functional evidence verbatim in the transcripts (exactly-one-submission under concurrency; failing squeue refuses; dependency-stranded child NOT adopted, nothing resubmitted, helper never cancels; SUBMITTED replay returns the same jid; parent sha persisted). The 3 earlier STRICT failures = worktree runner missing the flac conda env (fixed properly; gate untouched; NOT skip_env).
- **Seat notes** — Coder seat: Opus 5 (to `e204961`) → **Claude Fable 5 fork** (from `f4671f3`), per the 20:45 entry; flagged per the role-attribution rule.
- **Login-node hygiene flag (not ours to kill):** the other session's 15:38 guardtest trio (PIDs 265588/265601/265610) is hung at the pre-fix AE `wait` deadlock and will never exit; its four partial logs sit uncommitted in the exp folder. Surfaced to Yixun.
- **Next** — Scoped Codex verification of `e204961`+`f4671f3`+`562be70` (the transaction implementation has not itself seen the Reviewer) → on GO: submit chain INITIAL.

## 2026-08-13T19:15:00-04:00 — chain-fix3 complete (4/4 closed, no new defect); final GO check

- **Result** — `passed`. Commits `c922148` (dead-child refusal with corpse record + token rotation, rc 5 → class 12; unconditional both-source identity incl. mandatory manifest_sha256 and the 0/2500/2500/2500 pins; terminal-write failure = class 14 with the queued-successor stranding surface + exact recovery; manifest_fields complete for all 38 keys) + `3cbdc90` (guardtest section AG, 27 cases) + `dfaf810` (evidence). Suites: **main 347/0, STRICT 359/0, union 363/363 SATISFIED**. Race/stranded core re-verified verbatim. No new implementation bug this cycle — first clean-on-first-contact cycle; convergence.
- **Next** — Push; final scoped Codex pass on `c922148`+`3cbdc90` only; on GO: submit chain INITIAL, arm the chain monitor, done for the night.

## 2026-08-14T10:45:00-04:00 — chain-fix4 complete (3/3, full green); final GO check

- **Result** — `passed`. Commits `b1314b1` (sacct State%-40 + canonical_state prefix normalization with ambiguous-truncation-fatal; SUBMITTED-replay checks the recorded successor's actual state and routes dead children through the corpse path within one locked transaction; parser completeness) + `b149f86` (evidence). Suites: **main 361/0, STRICT 373/0, union 376/376 SATISFIED**. Recovery compose verified literally (3-stage: submit → scancel+replay buries corpse and submits fresh exactly once → replay ALREADY_SUBMITTED). Scheduler-read audit: all state reads width-pinned in the state helper; submitter has zero scheduler calls; launcher's one squeue reads %l (no hazard). Coder seat: Fable fork (continuing; Opus limit).
- **Next** — Push; final Codex GO check on `b1314b1`; on GO: chain INITIAL up.

## 2026-08-14T11:20:00-04:00 — GO (zero findings) → CHAIN INITIAL LAUNCHED: job 3695627 (leg 1: 0→2500)

- **Result** — `launched`. GO check #2: ZERO findings (`yaw_aug_codex_chain_go.md`). Chain INITIAL submitted at pin `fb4fc74` (= origin), job **3695627**, 1:30:00, `CHAIN=1 LEG_STEPS=2500`, standing waiver cited (in-chain rate gate enforces the floors at leg 1). Intent manifest `yaw_aug_submission_YAWAUG_1786718640213583894-a5cd327e.txt`.
- **Acceptance criteria (leg 1, judged against):** starts at EXPECT_SHA fb4fc74; all start gates green; banner exact-line before step evidence; 2500/2500 steps; boundary ckpt `*step=2500.ckpt` audited (sha/step/config/EMA via snapshot-bound recorder); registry INITIAL entry + chain metadata written; rate gate PASS (0.849/0.843 windows) → leg 2 submitted via transact-submit with afterok; terminal record published. Any failure surfaces its class (8/9/10/12/13/14 taxonomy) + CHAIN STALLED where applicable.
- **Next** — Chain monitor armed (state-file + squeue + registry tips). Then: results phase after 40k; eval kit rounds per the F5 deferral.

## 2026-08-15T13:45:00-04:00 — Chain progress 7,500/40,000; rate gate PASSED (waiver discharged); four-doc handoff refresh (Fable 5 → Opus 5)

- **Goal** — Monitoring checkpoint + mandatory handoff refresh on Yixun's `/model` switch to Opus 5 (1M).
- **Result** — Chain self-driving as designed: leg 1 (job 3695627, 0→2500, started 08-15 05:19, 42:45) COMPLETED+audited → leg 2 (3698903, 2500→5000, 12:20, 46:07) COMPLETED+audited → leg 3 (3700165, 5000→7500) RUNNING. Boundaries 2500/5000 = SUBMITTED in the chain state with their successor jids; checkpoints `epoch=0-step=2500.ckpt`, `epoch=1-step=5000.ckpt` on disk. **RATE GATE PASS — window 100→300 = 1.005 steps/s vs floor 0.849, i.e. ABOVE VANL's own windowed 0.943.** The standing 2026-08-12 waiver's post-hoc obligation is therefore **DISCHARGED**, and the smoke's apparent slowdown (0.2 vs 0.37 at step 30) is confirmed a running-average artifact + node noise, exactly as triaged. Scientific bonus: **the augmentation is throughput-free** — a per-sample metadata roll on 3×256×512 costs nothing measurable against the DiT step.
- **Analysis** — Backfill behavior validates the chunking decision: leg 1 started 05:19 (vs Slurm's 04:01 next-day estimate for the 24 h monolith), and legs 2→3 chained with a ~1-minute gap. Pace ≈ 2,500 steps / 2.8 h wall including gaps ⇒ 13 legs remain ⇒ 40k ≈ 2026-08-16, gap-dependent. Cluster remains saturated (0 idle nodes, ~286 queued; our account also runs `vae_*` ×3, the `exp00_train` array, and exp_11's chunk legs).
- **Change (handoff protocol, model change trigger)** — All four docs refreshed: `HANDOFF.md` (header + a full "exp_15 chain LIVE" in-flight section: leg table, self-drive mechanics, stall taxonomy + idempotent recovery, closure-commit embargo, ETA, post-40k eval deferral), `master_experiment_tracker.md` (exp_15 row → TRAINING LIVE with the review-round history and the rate-gate result), `issue_report.md` (new exp_15 section: closure embargo, never-hand-submit-a-leg, waiver discharged, eval-kit deferral is deliberate, chained-vs-monolithic disclosure owed, my ref-move incident + the never-move-refs rule, the other session's hung PIDs), `CLAUDE.md` (new "Chunked chain training" section — PL resume exactness, accum-1 requirement, W&B resume trap, single-transaction submission, sacct truncation, never-adopt-dead-children, afterok, disclosure duty).
- **Next** — Chain continues autonomously to 40k under the monitor. Then: completion audit + `final_ckpt_sha256`, then the eval phase's own TDD/review rounds (deferred by record) before any eval cell.

## 2026-08-15T14:45:00-04:00 — Chain at 12,500/40,000; eval-kit build opened in parallel (Yixun-approved, incl. a recorded plan deviation)

- **Chain** — legs 4 (7500→10000, job 3700245) and 5 (10000→12500, job 3700853) COMPLETED+audited; boundaries 10000/12500 SUBMITTED; leg 6 (12500→15000, job 3701434) queued. Compute per leg steady at 42–46 min; recent queue gaps ≈ 1 min (favorable backfill).
- **Decisions (AskUserQuestion, both recommendations accepted):** (1) build the eval kit NOW in parallel with training rather than after 40k — the kit's build + its mandated review rounds are otherwise serial delay on the critical path; eval-kit files sit OUTSIDE the training closure, so commits cannot abort pending legs (empirically proven across this chain's worklog commits). (2) **PLAN DEVIATION, recorded:** plan §6.7 names exp_11's screen kit as the adaptation base (it was written before exp_14 landed); we instead adapt **exp_14's kit** (`yaw_gen_screen.sbatch`, `yaw_gen_screen_submit.sh`, `yaw_gen_submit_grid.sh`, `yaw_gen_collect.py`, `exp14_validate_cell.py`), which is review-closed and proven end-to-end on its 106-cell campaign AND already implements exactly the contract exp_15 adopted (input_hash/assignment_hash, 6,337-tuple substitution guard, `_rotrand<seed>`, validate-before-skip dedup, 10-room-family aggregation, batch/worker campaign pins). Rationale: least new code, least new review surface, no re-implementation of machinery already proved.
- **Standing constraint unchanged (review mandate):** the eval kit gets its own TDD + per-round reviews AND a second integrative review **before any eval cell is submitted**. Building now does not authorize submitting.
- **Next** — Coder round E1: eval kit adaptation (screen sbatch + submitters + guardtests). Then E2: collector + tests. Then integrative review. Eval submission only after 40k + that review.

## 2026-08-15T19:45:00-04:00 — Eval round E1 complete (kit built, 154/0); chain at 12,500 with leg 6 queued in a backfill gap

- **Change** — Eval kit built by adapting exp_14's proven kit (approved deviation): commits `1deb10e` (verbatim base copies) → `2daec8a` (42-cell grid + recomputing admission gate) → `24a1bcd` (driver + single-cell and wave submitters) → `5e86f19` (guard suite + DRYRUN transcript + admission fail-fast). New files: `exp15_validate_cell.py` (torch-FREE, for login-node classification), **`exp15_admit_ckpt.py`** (new, torch-heavy deep gate run per cell on the compute node — reuses the round-2 recorder primitives by import), `yaw_aug_screen.sbatch`, `yaw_aug_screen_submit.sh`, `yaw_aug_submit_grid.sh`, `yaw_aug_screen_guardtests.sh`.
- **Command / Validation** — Guardtests **STRICT 154 passed / 0 failed / 0 skipped**; union 154/154 covered (a new case enforces unique ledger names — 4 collisions found and renamed). DRYRUN grid = exactly **42 cells** (tbl 20 / rrob 20 / vctl 2; 21 per arm), asserted equal to the registered grid cell-for-cell, nothing submitted.
- **Admission gate behavior verified in both states:** TODAY (`arms.YAWAUG.final_ckpt_sha256` still null — the chain's final leg writes it) the gate fails closed with a message naming the state and the legs recorded so far; VANL is already admissible (sha `1095f493…`, step 40000). With a synthetic complete registry: ADMITTED both arms, and REFUSED with named reasons for wrong embedded step, broken EMA mirror, a checkpoint trained without `yaw_aug`, mismatched sha, self-contradictory registry, tampered manifest, checkpoint outside the recorded location. A static case proves no record's `checks` booleans are ever trusted.
- **Analysis — findings the review must focus on (Coder's own list, all substantive rather than cosmetic):** (1) `eval_FLAC.py` records `frame_avg_angles` only under `fa_invariant`, so under vanilla the record reads `null` — the manifest now carries BOTH the effective value and the literal argv flag, each checked against the artifact that witnesses it; (2) the deep gate is a separate module so the 42-cell validator stays torch-free; (3) the arm axis is not an orbit — `EVAL_ORBIT` is now a hard refusal, replaced by `TRAIN_YAW_AUG` asserted against the checkpoint's EMBEDDED `training.yaw_aug`; (4) rotation token moved mid-name per plan §6.7, `parse_eval_name` round-trip verified on all 42; (5) renderers moved out of shell (torch-free validator makes one source of truth possible); (6) production-root lock moved to entry; (7) env namespace `YAW_EVAL_*` with leftovers from the training/exp_14 kits refused outright; (8) exp_11 shared-store thaw/delete cases deliberately de-scoped while a chain is in flight; (9) the "did we touch exp_11/exp_14" check now asks *did WE move anything* and discloses concurrent third-party changes (another session writes there continuously).
- **INCIDENTAL SCIENTIFIC FINDING (worth keeping):** `FLAC_AR_YAWAUG.json` minus `training.yaw_aug` canonicalises to **exactly** VANL's config hash `2023ccc6…` — i.e. the matched-control claim ("identical recipe except the augmentation") is now machine-checkable, not merely asserted.
- **Chain** — 12,500 banked (5 audited boundaries, 5 checkpoints); leg 6 (3701434) PENDING in a backfill gap since ~14:45. Login node under heavy load (~39) and its user slice near its memory ceiling — keep login-node work bounded.
- **Next** — Codex review of E1 (SOP: round closes before E2 starts) → E2 collector + tests → integrative review → eval only after 40k.

## 2026-08-16T09:00:00-04:00 — Chain 30,000/40,000 (4 legs left); E1 review REVISE (1 BLOCKING); fix+E2 round opened

- **Chain** — the backfill drought broke overnight: boundaries 15000→30000 all audited and chained back-to-back (jobs 3701434, 3704157, 3704368, 3704480, 3704630, 3704731, 3704835). **12 audited boundaries, 12 checkpoints, 30,000/40,000 banked.** Leg 13 (30000→32500, job 3705236) PENDING. Yixun's queue decision (2026-08-15: "leave it alone and wait") vindicated — no intervention was needed.
- **E1 review** — `yaw_aug_codex_code_evalr1_review.md`. **BLOCKING:** `exp15_validate_cell.py` accepts ANY nonempty metrics dict / per-scene payload, so a cell with missing keys, wrong casing, booleans, or NaN/Inf classifies VALID and gets SKIPPED by the grid's dedup — "validate-before-skip" was procedural but not semantically safe; the guard suite even codified the weakness with lowercase partial fixtures. **MAJOR:** the single-cell submitter only *uses* the campaign pin if present (the wave path correctly refuses without it) — the planned V/probe single launches could therefore run unpinned. **MINOR:** the neighbour "did WE move anything" check infers attribution from filename text — rejected as proof. **NIT:** the admitter's docstring overstates descriptor consistency. **Verified sound:** verbatim base identity by git blob hash; the grid is exactly T20+R20+V2 with unregistered combinations rejected; every path renders the §4.2 literal argv incl. `--cond-method vanilla` for both arms; the dual frame-angle representation is sound (the flag is parsed from the same argv array actually appended to `eval_FLAC.py`); G4 recomputes everything and trusts no recorded boolean; the 6,337/`input_hash`/`assignment_hash`/`_rotrand<seed>`/batch-64/workers-4 contract is enforced; imports and DRYRUNs cannot submit or mutate training state. 9/10 rethinks ACCEPTED (incl. the new `exp15_admit_ckpt.py` module as a justified deviation).
- **Note for the ladder:** the reviewer flags that the driver's own DRYRUN deliberately runs the heavy checkpoint gate, so it is NOT login-node-cheap — do not invoke it under current load.
- **Next** — one Coder dispatch: E1 fixes (verified + committed first) then E2 (collector + tests), reviewed together as a consolidated round (SOP allows batching small work); then the mandated integrative review before any eval cell.
