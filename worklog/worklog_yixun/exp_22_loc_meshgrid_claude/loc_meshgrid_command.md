# exp_22 loc_meshgrid — launch command record (SOP §37)
All runs from this checkout (`mae-cab-lab-server`, 2×A6000). Historical launches (probe sweep, P1 shards+merge, R1 report, controls, v2/v3 probes, r9r/r9u measurements) are preserved verbatim in the scratchpad scripts mirrored in the teed logs beside this file (`loc_meshgrid_probe_sweep.log`, `loc_meshgrid_p1_full.log` + shard logs, `loc_meshgrid_r1_report_P1.log`, `loc_meshgrid_r1_controls.log`, `loc_meshgrid_r1_offgrid_v{2,3}.log`, r9r/r9u measurement logs) — each log's head carries the exact argv. From probe v4 onward, launch provenance is mechanical: `--emit-launch-record` writes argv/git-SHA/host/GPU-UUIDs at launch and the driver refuses a canonical run without a matching `--launch-record`. v4 record: `outputs_loc/exp22/r1_offgrid_probe_P1_v4/launch_record.json` (emitted below, from a clean tree).
