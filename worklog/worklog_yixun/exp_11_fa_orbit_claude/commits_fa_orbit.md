# Commits — exp_11_fa_orbit

Base: `b9e38ce` (exp_10 model_comparison rows). Branch: `check-equivariance-necessity`.

| Order | SHA | Summary |
|---|---|---|
| 1 | `f8d18d3` | scaffold + plan Rev 2 (post-Codex REJECT plan review, 14 findings addressed) |
| 2 | `b1c1198` | round 1 — arm configs `FLAC_AR_BF_C{4L,8,16,32}.json` (orbit + no-grad-ckpt) + TDD tests `test_exp11_orbit_configs.py` and the Cn parametrizations in `test_invariant_conditioning.py` |
| 3 | `91cfc0e` | round 1 fixes (Codex REJECT b1c1198: 2 BLOCKING + 2 NIT) — strict `is True`/`is False` gc-leaf assertions + falsy-`0` regression test, averaging test parametrized over C8/C16/C32, duplicate-key/NaN-rejecting JSON loader, orbit tests 175 s → 14 s |
| 4 | `43a4d5b` | plan Rev 3 (approved fast-recipe amendment: no ViT grad-ckpt, micro×N=64 rungs, P0 profiling stage) + round-1 review-loop artifacts |
| 5 | `e566513` | round 2 — P0 profiling kit: `p0_profile.sbatch` (paired 10/30-step cell, mem_probe-derived gates, UUID-bound VRAM poll, `P0RESULT` line), `p0_submit_matrix.sh` (rung × orbit matrix + `spot` cells), `p0_collect.py` + TDD `test_exp11_p0_collect.py` (pairing, steps/s, attribution columns, `p0_report.md`) |
| 6 | `ec0250d` | round 2 fixes (Codex REJECT e566513: 7 BLOCKING + 2 NIT) — torchrun DDP + world-size/rank-placement gates (PL would have run world size 1 under `--ntasks=1`), in-fit step-10/30 timing via new `p0_runner.py` (single job per cell, no wall-time pairing), manifest-bound collection (runid/sha/jobid/config_sha, every expected cell reported, derived tables withheld + nonzero exit unless all-OK), cell-derived config map with a semantic orbit/grad-ckpt gate, sequenced util/power poller with per-tick completeness + stop-file lifecycle, exact-{VAN,C4L,C8} fit gating with AMBIGUOUS detection, worker-pair mode |

| 7 | `60764c1` | round 2 fixes v2 (Codex re-review of ec0250d: 6 BLOCKING + 2 NIT) — **FA1 attribution control** `FLAC_AR_BF_FA1.json` (single-angle orbit: same cylindrical pose path as C4L/C8, one ViT pass) with the fit moved to the exact {FA1, C4L, C8} set and VAN demoted to a separate vanilla contrast; manifest `mode` + exact provenance binding (maxsteps/mb/ngpu/workers, ns+random run id, no-clobber publish, run-specific report path); poller evidence mandatory (finite util/power per tick, `pollcsv_sha256` hash-verified, in-window ticks per UUID); per-cell Slurm time limits for the C16/C32 spots; worker pair in ONE manifest keyed `(cell, workers)`; MAXSTEPS pinned to exactly 30; config-map hook honoured only outside a job |

| 8 | `8d53691` | round-2 review-loop artifacts (P0 kit reviews + worklog closure) — main session |
| 9 | `aa4bc18` | P0 smokes live-validate the kit; C4L_32x2 no-ckpt OOM (real bound), FA1_32x2 1.01 steps/s — main session |
| 10 | `7e617a2` | P0 matrix launched (13 cells, jobs 3638637-49) + command record — main session |
| 11 | `72a8114` | round 3 — arm training launcher `fa_orbit_train.sbatch` (torchrun rung-parameterized 16x4/8x8, 32x2 barred by the measured OOM; arm→config map + semantic orbit gate; commit/drift, wandb, VRAM and world-size gates incl. a 300 s early-abort watchdog; exp_10-style INITIAL/RESTART lineage; dual-tee + atomic manifest; DRYRUN argv-parity vs exp_07), `assert_arm_configs_exp11.py` (exp_07's DINOv3-pin + init-identity checks re-pointed at the four arms), `fa_orbit_train_guardtests.sh` (33 cases, all green) |

| 12 | `5557974` | consolidated round — grad-ckpt pivot for all arms (no-ckpt OOM-infeasible for C8+), C4L byte-identical to exp_07 B-F, P0 poller `%.6g` precision fix, launcher round-3 fixes (pins, deep restart preflight, run lock, tested exit taxonomy, env/W&B gates, SMOKE mode, 52-case guard suite) |
| 13 | `0df4103`, `4e84485` | Q3/Q4 records, plan Rev 4 note, C32 authorization — main session |
| 14 | `abbff5a` | NEW-1a — `FLAC_AR_VANCKPT.json` (canonical + the two grad-ckpt leaves) + deep-diff test: the canonical manifest could never pass the post-pivot gate |
| 15 | `c89d05a` | NEW-1b/NEW-2/NEW-5 — P0 kit pivoted (VAN→VANCKPT, CKPT4 retired everywhere, matrix now 12 cells), OUTPUT_ROOT literal under Slurm, poller comment corrected and the 2 s liveness bound restored |
| 16 | `983a7ff` | launcher residuals — flock ownership (B3), fail-closed manifest-commit binding (B2), pip-freeze/final-tee/preflight-transcript durability (B5), exported W&B entity + post-run run-identity verification (B7), intent manifest before sbatch (NEW-3), safe FIFO (NEW-4) |
| 17 | `d1477c1` | fresh evidence — 72-case guard log and 78-case pytest log committed; ledger updated |
| 18 | `1479304` | **batched-orbit `invariant_conditioning`** (Q5) — same maths, reordered execution: rotated variants stacked along the batch dim into ≤ `FRAME_AVG_MAX_FWD_SAMPLES` = 64-sample chunks (P0 measured orbit cells at 28–34% GPU util, i.e. latency-bound), split back per angle, accumulated in the same ascending order; `_orbit_average_loop` preserved as the equivalence reference; equivalence tests over C ∈ {4,8,16,32} × B ∈ {1,2,3} (+ fp64 case) and the call-count test restated as a sample-count contract |
| 19 | `d4164e8` | equivalence probe kit — `fa_orbit_equiv_probe.py` (real DINOv3 stack from the C32 arm config + real train-split samples; batched vs the library's own loop reference in fp32 and bf16 autocast for C4/C32; pre-registered fp32 rel ≤ 1e-6, bf16 rel ≤ 2e-3; one `EQUIVPROBE` line) and `fa_orbit_equiv_probe.sbatch` (1× L40, 10 min, p0-style commit/drift/GPU gates) |
| 20 | `8094d60` | batched-orbit review F1/N8/N9 — train-mode DINOv3 draws its random RoPE rescale **per forward**, so batching makes a chunk's angles share a draw: reframed as a **disclosed recipe change** (identical averaging arithmetic, different augmentation schedule, applied identically to all arms incl. C4L; historical rows = legacy-loop). New train-mode contract tests (draws == 1 + n_chunks, seed determinism, gradient flow), exact chunk-plan + mask-sentinel tests, `B > cap` rejected, `ORBIT_EXECUTION` added |
| 21 | `3075294` | F7 — eval provenance: `orbit_execution`, `frame_avg_fwd_cap`, `source_sha` in `build_metrics_record`/`build_predictions_meta` (+ tests); no CLI or behaviour change |
| 22 | `deeaddf` | F2–F5 — probe redesigned: eval-mode equivalence is the GATE (dual normwise + max-abs bounds, B8 × C{4,8,16,32} plus the B64/B1 eval schedules), train-mode is grad-enabled QUALIFICATION (finite outputs/grads required, divergence recorded), deterministic direct dataset indexing with recorded sample ids, NaN-proof metrics, fail-closed sbatch (all provenance statuses, both pipeline stages, exactly one strictly parsed `EQUIVPROBE` line, DINO pin gate) + `test_exp11_equiv_probe.py` |
| 23 | `b117acb`, `10c41e1` | batched-round evidence log + ledger; whitespace fixup |
| 24 | `d40f125` | batched re-review fixes — **NEW-1** the wrapper could never succeed (`PROBE_RC=…; TEE_RC="${PIPESTATUS[1]}"` — the first assignment resets `PIPESTATUS`, `set -u` then killed it with exit 127), fixed by an atomic array capture and proven by a new shell guard test that extracts and runs the wrapper's real block (10 cases); **NEW-2** CUDA required (no silent CPU/fp32 masquerading as the bf16 qualification), `device=` emitted and wrapper-checked; **NEW-3** exact `idx:relpath` record identities (the old ones were eight identical `Cafe` labels) with distinctness enforced in probe and wrapper; **NEW-4** the stochastic contract test now models the real per-(forward × coordinate) DINO draw topology and the gradient test isolates the orbit's contribution; **NEW-5** bf16 deviations in the `EQUIVPROBE` line; **NEW-6** vanilla rows are `orbit_execution='n/a'`; **prior-7** `batch_size`/`n_samples` in the metrics record and `orbit_execution`/`frame_avg_fwd_cap`/`source_sha` added to the exp_02 comparator's guarded keys |
| 25 | `a3b8497` | probe fix after job 3646616 — train-mode qualification is **C4 only** (train-C32 OOMed at 42.3 GiB inside the loop reference: one 46 GB L40 cannot hold both paths' C32 grad graphs, a shape that only exists distributed at micro-8 × 8 ranks; C32's train-path memory/throughput comes from the 8×8 P0 spot cell), per-path memory hygiene in `run_cell` (detach → `del` → `empty_cache` between sides), cell count 14 → 13 everywhere, and the wrapper gains its own `--gres/--cpus/--mem/--time` directives so it is self-sufficient (the first two attempts ran GPU-less because only the submit line carried them) |
| 26 | `705e308` | **B=1 breach root-caused (job 3646626)** — not a batching bug: the probe had copied train.py's global `set_float32_matmul_precision('medium')`, which turns on TF32 matmuls, and a 1-row GEMV does not take the reduced-precision GEMM path while the batched 3–31-row matmul does, so the "fp32" gate compared two DIFFERENT precisions. The breach band (3.5e-4…5.4e-4) is exactly TF32's unit roundoff 2^-11 = 4.88e-4. CPU discriminators (angle→slice mapping via a roll-encoding conditioner, and same-record B=1 vs B=8) are exact, excluding the semantic hypotheses. Fix: the gate runs in true fp32 (`highest`, cuDNN TF32 off), only the train cell keeps `medium`; precision is recorded per cell and in the result line |
| 27 | `4077b45` | gate bound **ADJUSTED AFTER MEASUREMENT** (job 3646634, pending reviewer sign-off) — `mm=highest` confirmed the TF32 diagnosis (B=1 fell to ~1.3e-6) and exposed ordinary shape-dependent fp32 summation-order noise: 6/24 gated cells at 1.099e-6…1.979e-6, every `max_abs` ≤ 7.987e-6. Expected scale √384·2⁻²⁴ = 1.17e-6, i.e. exactly where the envelope sits. `rel_norm` 1e-6 → **5e-6** (2.5× headroom; still ~70× below the TF32 defect band and ~5.3 orders below an O(1) slice error), `max_abs` unchanged at 1e-5; justification block + test docstring record the history |
| 28 | `71054cf` | pinned-path SMOKE (job 3646734) classified 7 despite fully green training — fixed the three launcher defects it exposed: the W&B readback searched `$WANDB_DIR/wandb` while PL's default `save_dir='.'` (train.py:165) overrides `WANDB_DIR` and the run landed in `$REPO/wandb`, so the readback now locates it by the collision-proof run id across both roots (new `fa_orbit_wandb_readback.py`, exactly-one-match, 15 pytest cases, replayed against the real 3646734 run dir); a stray decorative `--------` line was being executed (`command not found`); and the preflight-transcript path is now taken from `scontrol show job … StdOut` instead of guessed from the `#SBATCH` pattern (`SLURM_JOB_NAME` is unset in-job), fail-closed into class 7 |
| 29 | `a40c733` | `exp11_validate_rows.py` + 37 TDD cases — fail-closed row/cell validator (plan §5, Yixun Q7): exact orbit per arm, own-checkpoint at the claimed step, K↔dataset-config, cfg 1.0 / 1 step / bf16 / EMA, expected seeds exactly once, and the exp_11 execution provenance so legacy-loop rows can never be averaged with batched ones |
| 30 | `e0da592` | `fa_orbit_screen.sbatch` — one driver for the futility-gate screens (plan §4) and the C4 backfill (plan §3): arm→config/orbit/checkpoint-tree map, exactly-one-checkpoint rule, ckpt-embedded-config identity gate, pinned protocol (full unseen split, EMA, cfg 1.0, 1 step, bf16), plan-§4 eval-name schema, sidecar for the validator and one `SCREENRESULT` line; `fa_orbit_screen_guardtests.sh` (24 cases) + `load_ckpt_config` in the preflight module |
| 31 | `a6f203a` | drift gates scoped to CODE surfaces — the four arms are running and Slurm appends to their **tracked** `slurm_train_*.out` logs continuously, so the folder-wide pathspec would have aborted every screen (and every restart/probe) on a live-log write; the screen, probe, launcher and submitter now check `train.py defaults.ini src eval_FLAC.py` plus the exp_11 `*.json/*.py/*.sbatch/*.sh`, leaving configs, drivers and validators fully covered |
| 32 | `967f5f9` | round-4 B1/B2/B3 — `eval_FLAC` proves the protocol in its own record: `resolve_weights_source` (EMA vs online, resolved not asserted), the runtime protocol (dataset/seed/cfg/steps/eval-name/batch/actual item count/device/weights source), and an injective rotation suffix (`rot5p625`) so R3's 5.625° can no longer collide with 5° |
| 33 | `734fadc` | round-4 B3/B4/B6 — validator hardened: strict JSON (duplicate keys, NaN), all six finite metrics, `rotate_deg == 0`, exact `build_output_paths` filename, real commit shas, mandatory sidecar fields, record↔sidecar agreement, EMA + full-split proven from the record; **registered contracts** (futility/table/r3) replace caller-chosen seeds; one checkpoint/config/evaluator identity per cell; hashes recomputed under `--verify-hashes` |
| 34 | `3e9043b` | round-4 B1/B4/B5/B6 + NIT 1/2 — screen driver requires EMA weights in the checkpoint before spending a GPU, `CELL=screen|conf` enforces the contract's seeds/K, the validator runs as a **gate** before `SCREENRESULT`, the backfill is bound to a new audited `c4_backfill_manifest.json` (path + sha256 + seed), the drift gate is recursive again minus live artifacts, and the unregistered 10k backfill cell is rejected |
| 35 | `53550db` | round-4 B5/B7 — `gen_model_comparison.py` refuses to emit an exp_11 row whose cell fails validation (renders **BLOCKED**, fails closed) and every fa row now discloses `legacy-loop` vs `batched`, with the header stating they are not interchangeable; the table itself is deliberately not regenerated (the exp_10 @67.5k JSONs are missing from disk) |
| 36 | `339a125` | re-review items 5+6 — generator generation moved behind `main()` (its own tests imported it and rewrote the frozen table); a confirmatory row no longer claims `validated=table` (it cannot be validated alone) |
| 37 | `abb90a5` | re-review items 1+4a — every evaluator field mandatory and type-checked (absent ≠ pass), exactly-six metric keys, batch/device validated, `source_sha == sidecar.commit`, 64-hex vs 40-hex hash typing; R3 cells keyed by the five registered rotations instead of seeds |
| 38 | `c295c09` | re-review item 2 — arm checkpoints bound to their launch manifests (arm, config hash, canonical run dir) and validator containment moved from substring to realpath |
| 39 | `ced7f53` | re-review items 3+4b+7 — generator recomputes hashes in table validation, two-K outer gate for exp_11 table updates, and the **deferred label migration**: labels stay as published while the exp_10 endpoint JSONs are off-machine, with a loud header note saying every untagged `fa eval` row is legacy-loop; the migration happens automatically on the first regeneration after that evidence returns |
| 40 | `d273b15` | live-failure fixes from the backfill screens (jobs 3649599/3649600, both rc=6 — validation refused correctly): (a) the registered metric set was the TABLE subset, so every genuine row failed — the TRUE 11-key emission set is now pinned from job 3649599's own record with the six table metrics required as members; (b) **ITEM 8 — worktree-pinned measurement execution**: `fa_orbit_measure_worktree.sh` creates/reuses/prunes `.measure_worktrees/<sha>`, the screen driver and probe wrapper read ALL code from that pinned tree while writing outputs to the main tree, and the commit gate binds the WORKTREE's HEAD — so `source_sha` always equals the pinned SHA and **development and measurement are decoupled: no more commit freezes** |
| 41 | `b23fccd` | GO-check NO-GO closure. **ASSETS (the crasher):** `fa_orbit_measure_worktree.sh` links `AcousticRooms` and `weights` into every pinned worktree at the main tree's own targets — a fresh worktree holds only TRACKED files, so the eval's relative `AcousticRooms` / `weights/AGREE/AGREE_fullAR.pt` did not exist and a pinned screen would have died at startup; the screen now stats each required asset before spending a slot. **LIFECYCLE:** fixed-count pruning (which could delete a QUEUED job's tree) is replaced by LEASES — `.leases/<jobid>` written under an `flock` before `sbatch` returns and promoted to the real id, validated by the job at start, released by a trap at exit; a tree is prunable only with zero live leases (stale ids double-checked against `squeue`) and never the tree being handed out. New `fa_orbit_screen_submit.sh` makes pin+lease+submit atomic. **IMMUTABLE LAUNCH BINDING:** the launch manifest lives under gitignored outputs and is mutable, so it is now bound to a committed `arm_launch_registry.json` by sha256 plus job, mode INITIAL, launch uuid, commit, rung, max_steps, config/VAE/P0 hashes, save-dir and seed 42 — the seed used to be printed, not checked. **TABLE:** the two-K gate is transactional (aborts without writing; `--allow-partial-exp11` still renders WITHHELD, never numbers), `build_header()` is actually called by `main()` so the LABEL-MIGRATION-DEFERRED note reaches the file, and `--repo-root` resolves evidence/validator/output against the MAIN tree from any cwd. Also: MEASURE_ROOT must be a detached, managed worktree of THIS repo under `.measure_worktrees/`, and the sidecar records a repo-relative model-config path. |
| 42 | `e650d6a` | GO re-check 2. **LIFECYCLE (the not-closed one):** every store operation — create, lease, release, prune — now takes ONE store-wide `flock` (`.store.lock`), so a sweep can never enumerate leases while a submission writes one; the pin→lease and sbatch→promote races are gone entirely because submission is `sbatch --hold` → job id known → lease written → `scontrol release`, with the held job CANCELLED if either step fails (no orphan, no placeholder); and a nonzero `squeue` is no longer read as job-absent — only Slurm's explicit `Invalid job id` proves absence, everything else (controller down, timeout) keeps the lease and logs. Found while testing: `git worktree remove` leaves the directory behind when our symlinks/leases defeat its cleanup, and because that leftover sits inside the main checkout `git -C` on it answers for the MAIN repo — a stale directory looked like a reusable pinned tree. Identity is now verified (`.git` present, top-level == itself, same common dir) and half-removed entries are cleared. **ASSETS identity:** `AGREE_fullAR.pt` pinned by sha256 `3a13243d…` and gated in the screen exactly as the VAE is in the launcher; the `AcousticRooms` target is pinned literally in the helper, so a repointed main-tree symlink aborts instead of silently swapping the corpus. **CLI:** `main()` parsed `[]` instead of `sys.argv`, so `--repo-root` was accepted and then discarded — every command-line run used the default root; fixed, with a subprocess test from a foreign cwd against real synthetic evidence (it also exposed that the validator's repo root must be importable, else the gate fails closed for the wrong reason). **Validator:** the sidecar's repo-relative `model_config` was opened against the ambient cwd and reported 'not readable' from any pinned worktree; it now resolves against `REPO`/`OUTPUT_ROOT_BASE`, tested from a foreign cwd with the wrong-hash case still rejected. |
| 43 | `253658f` | GO re-check 3. **LOCK SPAN:** locking each operation separately still left the gap the review named — tree prepared, lock dropped, lease written — so a sweep landing in it deleted a brand-new unleased tree. `fa_orbit_screen_submit.sh` now re-execs itself under a new helper subcommand `--with-lock`, holding `.store.lock` across prepare → `sbatch --hold` → lease → validate → `scontrol release`; fd 8 is inherited so nested helper calls re-enter the same lock instead of deadlocking, the marker is only trusted when `/proc/self/fd/8` really is the lock, and the helper no longer unlocks on exit (that would drop the caller's lock). `add_lease` refuses a target whose worktree identity is not `valid`, and the submitter cancels the held job on any failure. Proven RED: with the span disabled the guard case reports `tree DELETED`, submit rc=5. **SAFE CLEANUP:** worktree identity is now three-valued — a git/NFS error is `indeterminate`, never `invalid` — and recursive deletion requires ALL of: definitively invalid identity, provably unleased (every lease id answered by a SUCCESSFUL squeue), and a name of exactly 40 hex characters; one `remove_entry` is the only code path that deletes. Stale leases are now reaped opportunistically rather than depending on which lease the loop saw first. **REGRESSION GUARD:** `gen_model_comparison.py` aborts WITHOUT writing if any published row would turn non-numeric, lose seeds, or disappear — `--allow-row-regression` permits a deliberate retraction and names the rows in both stderr and the file. Run against the real table today it correctly refuses, citing the two exp_10 @67.5k rows. |
| 44 | `2e189fe` | GO re-check 4 — the two remaining measurement mechanics. **(1) LOCK PROOF AT THE OUTER ENTRY:** `FA_ORBIT_STORE_LOCK_HELD` is an ordinary environment variable, and verifying it inside the helper was too far downstream — by then the submitter had already prepared a tree. `fa_orbit_screen_submit.sh` now proves the lock at its outer entry, before any transaction step: fd 8 must resolve (`readlink -f /proc/self/fd/8`) to the store lock, or the run is REFUSED; only an unset marker re-execs into `--with-lock`. Both sides must resolve NON-EMPTY, because two unresolvable paths compare equal — the same defect is fixed in the helper's `lock_already_held`. RED: without it, all three forged-marker cases ran a full submission UNLOCKED (rc=0). **(2) AMBIGUOUS OUTCOMES RETAIN THE LEASE:** the lease is now validated BEFORE `scontrol release` (while a bad lease still only costs a cancellation), and no error path drops a lease — a failed release, or a scancel that fails or whose result is uncertain, keeps it and leaves reaping to the squeue-proven reaper. The screen's unlocked EXIT-trap `rm -f` fallback is gone: an unlocked unlink can drop a lease mid-sweep, and the sweep then deletes a live job's worktree. Retaining costs one held tree; dropping costs a running job its code. **(3) REMOVAL FAILURE:** for a REGISTERED worktree git is the authority — if `git worktree remove` declines, forcing `rm -rf` would delete the checkout while git's admin files still point at it, so the entry is left in place (RED: `tree DELETED`). Plus the reviewer's six guard cases verbatim, all verified RED; 85/85 screen guard cases, 192 pytest. |
| 45 | `c27af15` | Conditional-GO closure — **CAMPAIGN FREEZE**, the reviewer's one remaining criterion: make deletion mechanically impossible, not merely unlikely. `--freeze` / `--thaw` / `--frozen` (all under the store lock, both mutators logging) manage `.measure_worktrees/.campaign_freeze`. While it exists EVERY deletion path in the helper is inert: explicit `--prune` skips with a warning, `remove_entry` — the single function that deletes — refuses before it can reach an `rm`, `git worktree prune` is suppressed (metadata pruning is still pruning), and the preparation path's implicit half-removed-entry cleanup ABORTS with 'thaw and clean it manually' instead of clearing. Preparation still creates and reuses trees, and stale lease FILES are still reaped — files only, never trees. `fa_orbit_screen_submit.sh` prints the freeze state in its preflight and REFUSES to submit without it, so the campaign condition enforces itself rather than resting on operator discipline. Nine guard cases (explicit prune blocked, git-worktree-prune suppressed, implicit preparation cleanup blocked, remove_entry guarded before any delete, no unguarded `rm -rf`, thaw restores collection, freeze contends on the store lock, submission refused without the freeze, preflight reports it); the four behavioural ones verified RED. 94/94 screen guard cases, 192 pytest. **The store is left THAWED: run `--freeze` before the first submission — submissions refuse until you do.** |
| 46 | `a5a8c91` | Measurement-kit extension: the two remaining pre-registered cell types. **CELL=r3** (plan §4) takes `ROTATE_DEG` from the registered set {0, 5.625, 11.25, 22.5, 45} (anything else aborts), passes `--rotate-deg` to eval_FLAC, keeps the arm's OWN orbit, and is seed 42 / K8 / step 40000 only. **CELL=cross** (R2 mechanism / D2) takes `EVAL_ORBIT` ∈ {4,8,16,32} and evaluates the checkpoint under an orbit it was NOT trained on — the ckpt-identity gate still proves the checkpoint IS the arm (embedded angles == TRAIN_ANGLES, unchanged), while the eval runs EVAL_ORBIT and the filename's aN follows it; a cross cell whose eval orbit equals the arm's training orbit is refused, because that is what screen/conf already are. Both cell types get INJECTIVE eval names (`..._r3_rot5p625_s42_K8`, `..._cross_a16_S40000_s42_K8`) via a `rot_token` renderer extracted from `build_output_paths` — five R3 rows previously shared one name. **Legacy D2**: the audited backfill manifest gains the step-40000 entry (sha256 `5319feb4…`), and ARM=C4BACKFILL STEP=40000 is registered for CELL=screen and CELL=cross only. Validator: a new `cross` contract keyed by EVAL ORBIT with a per-arm registered set (every orbit but its own), never table-admissible, requiring the sidecar to record BOTH orbits; R3 rows must have a record rotation matching their name. **Two live findings**: (a) `weights/` became a TRACKED directory when the exp_01 metric JSONs were force-added (7543476), so worktree preparation could no longer symlink it — assets are now provisioned at LEAF granularity (AcousticRooms, weights/AGREE, weights/FLAC/VAE.safetensors), which unblocked worktree creation at HEAD; (b) this guard suite thaws the store to exercise its deletion cases, which is unsafe during a live campaign — it now detects an active freeze and SKIPS those cases instead. 97 passing guard cases (deletion cases skipped, campaign live), 200 pytest. |
| 47 | `2d19bbf` | Morning fix round. **(1) EXCLUDE=** argument on the submitter → explicit `sbatch --exclude`; **root cause corrected**: the `--with-lock` re-exec does NOT drop the environment (measured: `SBATCH_EXCLUDE` survives it intact) — `SBATCH_EXCLUDE` simply **is not a Slurm input variable** (58 documented, none for `--exclude`; the lookalike `SBATCH_EXCLUSIVE` is a different option), so sbatch ignored it silently and NO batch of the campaign ever had exclusion in effect. A set `SBATCH_EXCLUDE` is now refused with a pointer to the argument. **(2) Default screen-log names** carry CELL, the rotation (`_rot5p625`) or eval-orbit (`_a16`) token and the job id — six same-second cells give six distinct names, and the old form collides exactly for two R3 rotations — so `LOG=` never needs overriding (all three overnight path incidents came from overrides); the names stay in LOGDIR with the `_screen.log` suffix the drift gate exempts. **(3) Live transcripts untracked**: the arm launcher `git rm --cached`s its own Slurm transcript at launch (tolerates absence, records the outcome in the manifest, closure documented in the header), and C32's live transcript is untracked here. **C16 clobber CONFIRMED** — mechanism is descriptor detachment, not a blob reset: all four transcripts froze at the identical mtime 02:04:07 across a rebase cycle, C16 stops at Epoch 5 of a run that reached 40000, and C32 (still running) is frozen on disk while its untracked tee'd copy grows. 114/114 guard cases, 200 pytest. |
| 48 | `_(this commit)_` | Halt closure (Yixun ruling: no semantic change on AGREE/src/metrics vs the C4L SHA — a packaging gap). **(1)** `weights/FLAC/VAE.ckpt` joins the helper's leaf assets (the AGREE config chain loads it; the worktree had VAE.safetensors but not this) and is pinned by content in the screen, sha256 `6198ff0d…`, exactly like AGREE and the launcher's VAE. **(2)** `PIN_SHA=<40-hex>` on the submitter: the helper prepares/reuses the tree AT that commit, EXPECT_SHA follows it, and a new intent manifest records job/arm/cell/pin/expect/measure-root/exclude/lease; a non-40-hex or unknown commit is refused. Freeze and lease semantics untouched. **Also found and fixed while verifying at the real pin:** the driver imported `eval_FLAC.rot_token` unconditionally, so ANY cell at a pre-r3 pin died at the render gate for a token only R3 uses — the same shape of failure that killed the C8 conf block. It is now computed only for `CELL=r3`. Verified live at `0c6e9ff`: screen, conf and cross cells all pass gates A–E with `VAE.ckpt pin OK`. 121/121 guard cases, 200 pytest. |

Notes: a commit cannot contain its own SHA (exp_07's amend lesson — never amend a SHA
into the commit that carries it), so each round's hash is reported with its output and
backfilled by the next exp_11 commit (row 7 backfilled here).
