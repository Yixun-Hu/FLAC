# exp_21 implementation contracts (r1) — from approved plan Rev 2

Binding for the Coder; plan Rev 2 sections are normative — these pins add implementation specifics. TDD in `src/tests/test_mappingA_*.py`; path-scoped commits "exp_21 r1: <desc> (TDD cycle N)"; ledger `commits_raf_mappingA.md`; Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>. Fences: this worktree only (`~/codespace/exp-21-raf-mapping-a`); never touch `~/codespace/FLAC` or `~/codespace/exp-19-raf-finetune`; no installs, no GPU, synthetic fixtures only; real corpus read-only.

## A. `data/RAF/mappingA_common.py`
- `cluster_placements(groups) -> clusters` — complete-linkage over tx-group rx-centroids, cap 0.05 m, no transitive chaining; deterministic ordering; medoid template per cluster (plan §2 M2 rules).
- `match_mics(template_rx [36,3], group_rx [36,3]) -> assignment` — Hungarian 36×36; unique; per-match displacement; FAIL the group if p95 > 0.01 m, any > 0.02 m, or ambiguity margin < 3× (next-nearest-mic distance / matched displacement); returns displacements for the record.
- `select_targets(placement_poses, seed) -> pose` — stable-hash-uniform (sha256 over room/placement-id/pose-key), documented estimand.
- `stable_item_context(...)` — per-item deterministic K=8 (exp_19 machinery), excluding every group sharing target xyz.

## B. `data/RAF/prepare_mappingA.py`
CLI `--raf-root --output-dir --split-dir --rooms --seed 0 --n-placements 16 --k 8 --readback-record --non-canonical`.
1. Correspondence + eligibility (≥9 passing source-xyz-distinct groups) → FPS over validated placement centroids → 16/room → 36 mic-slots × 1 hash target = 1,152 items.
2. **Audio union**: enumerate exact target+context capture set (~10,368); amplitude audit on raw resampled peaks (×3 no-clip ≤0.999; nothing < −60 dBFS post-scale; finite/nonzero) — ANY violation aborts with a measured report (Yixun stop-and-ask; never drop items); resample 48k→22,050 float32 ×3; read-back verification; files shared with the Mapping-H publication are reused byte-identically with provenance (id, hash, exp_19 generation).
3. Outputs: `data/RAF/mappingA_eval.json` (+ splits-record with correspondence stats, displacement distributions, target/context distance distributions, exclusions) + runtime `mappingA_metadata.json` (item → mic slot, rx_target_p + raw RAF rx height, tx*_p, per-context {capture id, tx_j_p, rx_j_p}, depth file, displacements).
4. **Static manifest validator** (plan §3): importable + CLI-run at publish; all M5 conditions.
5. Publication: DISJOINT roots (`<output>/mappingA/<Room>/…`, split files under `data/RAF/mappingA_*`), marker kinds `mappingA_prepare`/`mappingA_depth` via extended `publish.py` (new kinds + registered identities per plan §4; H/A composition tests: H→A, A→H, republish, injected crash — both flavors stay valid).

## C. `data/RAF/render_depth.py` listener mode
`--positions-from mappingA`: renders at `rx_target_p` (1,152 maps, dedup identical positions if any); QA parameterized with independent raw RAF rx height (nadir gate), transmitter endpoints for the recorded sightline diagnostic, listener-position provenance label; same miss cap 0.0025 / mask-derived QA / no-flipud; canonical identity = plan §4 list.

## D. `src/configs/dataset_configs/custom_metadata/RAF_A_md.py`
AR_md semantics with the plan §3 EXACT formulas (per-context own-rx); `md` contract identical shapes to RAF_md (target [1,10240], context audio [8,1,9600], source [3], source_vit [1,3], context_poses [8,3], depth [3,256,512]); provenance int64 `context_capture_ids` + `sample_target_id`; deterministic eval draws; mandatory `mappingA` publication verification (test-only opt-out constant, no env var); real-conditioner pass test under vanilla AND fa_invariant + C₄ rotation-invariance test on Mapping-A metadata.

## E. Config + stats
`src/configs/dataset_configs/RAF/eval/raf_mappingA.json` (id "RAF", RAF_A_md, deterministic true, expected 1,152). Per-placement metric collection: a small offline aggregator `data/RAF/mappingA_stats.py` consuming per-scene + per-item records (or retained per-item metrics via the stream sidecar) implementing plan §6: within-placement aggregation, room-stratified cluster bootstrap, paired placement-level randomization for arm contrasts; hand-checkable tests.

## F. Cycle plan (guide, adapt sensibly)
1 clustering+matching; 2 target/context selection; 3 union enumeration + amplitude audit; 4 resample/write + provenance; 5 manifest+validator+records; 6 publish.py flavor kinds + composition tests; 7 listener render mode; 8 RAF_A_md core; 9 RAF_A_md gate + conditioner/C4 tests; 10 config + stats aggregator; 11 integration on synthetic fixture end-to-end.

## Amendment 1 (r2 dispositions for Codex N1-N9, 2026-08-22 ~01:50)
All in-boundary; adopt as specified, with these registrations:
- **N1**: listener mode selects `mappingA_depth` + its complete identity (n_maps derived, positions_from, readback digest); canonical-equivalent end-to-end test where the RENDERER-produced marker passes RAF_A_md.
- **N2**: Mapping-A split root REGISTERED as `data/RAF_mappingA/` (disjoint; `raf_mappingA.json` updated); composition tests re-run over the ACTUAL CLI default topology.
- **N3**: amplitude audit records BOTH full-waveform and first-10,240-sample peaks; crop silence fails; recheck at write; marker distinguishes `amplitude_derivation_target: 0.75` from `clip_ceiling: 0.999`; delayed-impulse negative test.
- **N4**: CLI locates the Mapping-H publication, requires its generation, passes both into every room's write_union; claimed shared files must be covered by that generation's manifest.
- **N5**: `correspondence_sha256` = digest of the FULL committed record — **pinned NOW: f2da911b5de82e7914a0cf234c0f0713051880784a4a71c163fa92b377288da4** (the record exists and was independently recomputed by the reviewer); the exp_19 readback digest pinned exactly (9288181b…); canonical publication REFUSES while the audio-union digest is still a placeholder (pin-after-run, as established); pointer/prepare/depth identities cross-checked.
- **N6**: validator recomputes xyz identity + receiver displacement from poses; full schema without defaults; context group/slot attested against correspondence evidence; canonical expected_items from the REGISTERED constant (16×36×2), not self-count; config expected_items wired into the runtime stream gate.
- **N7**: eval per-item ingestion surface — `eval_FLAC.py` (this branch only) gains an additive RAF-gated `--record-per-item` sidecar (item id, per-metric values); `mappingA_stats.py` ingests it, requires exact item×seed equality, enforces 2 rooms × 16 placements, bootstraps paired placement differences within room, emits the registered contrast interval + separately-labelled seed SD. AR-path byte-identity + snapshot suites green (the exp_19 R3 discipline).
- **N8**: `rx_target_height_raf_m` persisted from raw `rx_xyz[RAF_UP_AXIS]` (exp_19 pattern); wrong-gauge test with untouched raw height.
- **N9**: rigid-array residual computed + recorded; per-slot correspondence evidence digested; duplicate receiver coordinates rejected; matched==second==0 treated as ambiguous. (Readback record regeneration required ⇒ the N5 pin updates to the regenerated record's digest in the same commit that regenerates it — one authority, no stale pin.)

## Amendment 1.1 (r2 deviation dispositions, 2026-08-22 ~02:30)
Registered: (1) shared-audio claim = CONTENT identity (rate, length, exact float32 samples; file+audio digests recorded) — byte identity is unattainable because libsndfile stamps a timestamp into every float-WAV PEAK chunk (measured, offset 60); (2) up-front scalar-agreement gate on the Mapping-H publication accepted; (3) MATCH_ALGORITHM_VERSION mappingA-correspondence-2 registered; (4) rigid residual RECORDED not gated (passing groups ≤1.6 µm — a gate would be redundant; revisit only if a future corpus shows drift); (7) per-item paired contrasts cover T60/C50/EDT/L1_STFT/mrL1/Env only (FD/recall are distribution-level and in any case unavailable for RAF); (8) loader-key fix accepted; (9) test-fixture pointer additions accepted. Workflow note (5/6): non-canonical dry run → Planner pins audio_union_sha256 → canonical run; Mapping-A evals must pass --record-stream.

## Amendment 2 (r3 dispositions for Codex P1-P3, 2026-08-22 ~03:20)
- **P1**: Mapping-A runtime default DERIVED as `<mappingH-dir>/mappingA`; explicit `--output-dir` must be a proper disjoint child (equal/ancestor roots refused BEFORE any survey/write); composition tests exercise the refusal and both real CLI invocations at their true defaults.
- **P2**: validator retains authoritative per-slot assignment evidence through validation; MATCH_SCHEMA gains the N9 evidence-digest + rigid-residual fields (finite, required); enforce `target/context rx_row == assignment[group_key][mic_slot]`; the reviewer's probe case (slot 0, row 23) becomes a committed negative test.
- **P3**: arm identity derived from sidecar provenance (ckpt sha, cond_method, config digests, publication generation, stream identity); all non-seed protocol fields constant within an arm; registered mode requires exactly seeds 42–46; cross-arm shared-identity comparison enforced.
- Residual note adopted: the N1 e2e test upgraded to consume an UNTOUCHED canonical-renderer marker (synthetic canonical-shaped fixture, no hand edits).

## Amendment 3 (r4 dispositions for Codex Q1-Q3, 2026-08-22 ~05:05)
- **Q1**: protected rooms derived from the Mapping-H publication POINTER (all its rooms), not the A-run's --rooms subset; the reviewer's probe (EmptyRoom-only run writing into <H>/FurnishedRoom) becomes a committed negative test.
- **Q2**: the sidecar/arm identity tuple gains every recorded protocol control: frame_avg_angles, frame_avg_fwd_cap, cond_autocast, batch_size, steps, cfg_scale, are_lambda, rotation protocol fields, orbit execution/source provenance, AND both publication generations (prepare + depth — RAF_A_md records both). assert_paired requires all of them shared across arms (seed-only variance within an arm; 1-step-vs-8-step probe becomes a negative test).
- **Q3**: distinct labels enforced; identities retained positionally; a registered identity-to-label registry for P1/YAW/BF/BV/finetuned asserted labels (ckpt sha ↔ label), refusing a mismatched assertion.

## Amendment 3.1 (r4 dispositions, 2026-08-22 ~05:45)
Accepted: (1) `finetuned` = the RAF finetune `6dfc2b2e…` (correct; the rcal HAA control is not an arm); (2) `source_sha` STAYS FATAL in the cross-arm shared set — operational rule: the entire sweep (all arms, all seeds) runs from ONE commit, no commits between evals; (3) per-arm-free fields as implemented (cond_method etc. — the contrast requires them to differ); (4) contrast_report positional arm_identities shape; (5) the Q1 regression-visibility comment assertion.

## Amendment 4 (amplitude-policy decision, Yixun-approved 2026-08-22 ~07:55)
The pre-authorized stop fired: 2/5,184 EmptyRoom union captures clip at x3 (max admissible 2.0401; zero silence/non-finite violations). DECISION: **Mapping A publishes its complete union at x2.0** — the registered formula re-derived over ITS union: min(one-sig-digit(0.75/max A-train-support peak), clip-clamp over ALL A-union written peaks) which must equal 2.0 (asserted pre-write, same identity discipline as exp_19). NO byte-reuse from Mapping H (provenance records the capture overlap and both scalars); the H-scalar-agreement gate is REPLACED by a registered cross-mapping scale disclosure (recorded in the A marker, splits record, and every results artifact: A audio is x2.0, H is x3.0; cross-mapping absolute mrL1/Env comparisons are unlicensed — they already were per M6). Registered identity: amplitude_scalar 2.0 for the mappingA flavor. Within-A contrasts unaffected; T60/C50/EDT level-independent.

## Amendment 4.1 (role-aware silence gate, Yixun-approved 2026-08-22 ~09:45)
Measured basis committed in mappingA_amplitude_window.json (d077ea0): no global scalar window exists (lift-all needs ≥8.7414, clip-free needs ≤2.0401); the silence hazard is TARGET-ONLY (line-verified: SampleDataset substitutes on post-crop target silence; RAF_A_md loads contexts with no amplitude test). REGISTERED: the audit's silence gate applies to TARGET-role captures at the published scalar; context sub-threshold captures are RECORDED per item (id, crop peak, dBFS) — not fatal. Clip gate unchanged over ALL written files. Scalar stays x2.0 (target window [0.545, 2.040]; quietest target −48.7 dBFS, 11.3 dB margin; zero clips). Disclosure obligations: the 19 affected items (all FurnishedRoom p008) listed in the splits record + results; a labelled with/without-p008 SENSITIVITY row accompanies every cross-arm contrast (primary row keeps p008 — real measurement conditions, identical across arms).

## Amendment 4.2 (listener-map near-field, Yixun-approved 2026-08-22 ~14:30)
Measured basis: 4/1,152 listener maps (EmptyRoom p052 slots 17/22/26/27) see scanned structure at 0.029–0.125 m — the capture rig in the photogrammetry; renders geometrically correct. REGISTERED: for LISTENER maps the scale-plausibility MINIMUM is a recorded diagnostic (per-map min depth + attribution recorded); the MAXIMUM side stays fatal (oversized scale = wrong units/gauge). Per-item disclosure joins the near-silent-reference disclosure; the labelled sensitivity row excludes BOTH flagged sets (p008's 19 + these 4 = "minus-flagged" row) beside the primary. Any NEW min-side flag in future renders is recorded and disclosed the same way.
