# exp_19 r5 Coder report (Opus 5 subagent, 2026-08-20 ~03:25 EDT) — archived by the Planner; deviations ACCEPTED

r5 complete: all six r4-pass in-boundary findings closed; **486 passed, 1 opt-in skip** (+23); metric-consumer suites green.

Commits: F1/T3 `9728e98` (runtime-tree `raf_publication.json` pointer covered by its own manifest; RAF_md mandatory `verify_combined_publication` over ACTUAL roots; `RAF_REQUIRE_PUBLICATION` env var REMOVED — only `_RAF_MD_TEST_MODE` from the test loader, with a test asserting no os.environ lookup exists in the module; malformed pointer fails closed with a readable reason, avoiding SampleDataset's substitution loop); F5/T5 `9e64a73` (`tx_height_raf_m` raw RAF Y published per group, hard-required by the render; end-to-end candidate-gauge CLI test transforming mesh AND poses: pinned gauge passes, identity gauge aborts publish); F4/T7 `c7b29f3` (raw mask is the source; the hostile zero-miss forge now fails); F2+F3/T2+T4 `d48d708` (seed 0 in the prepare identity — a seed-999 probe previously passed; `CANONICAL_RENDER_PARAMS` validated pre-I/O and written into the depth marker; completeness internal; duplicates reported; marker provenance validated); F6 `a738fa1` (bearing/vertical delta split); ledger `4fa8305`.

Deviations accepted: (1) synthetic contract tests run the loader in test mode; the gate has nine dedicated tests on a properly published synthetic tree; (2) `groups_metadata.json` gained `tx_height_raf_m` — pre-r5 runtime trees are rejected with "re-run prepare_data" (no canonical trees exist yet, so no impact); (3) four r4 tests rewritten as mask-disagreement cases; (4) Coder process-hygiene note re pkill self-match (no repo damage); (5) residuals 1–3 untouched per Amendment 7.
