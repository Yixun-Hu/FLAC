REQUEST-CHANGES

1. S1 — blocking — [readback_audit.py:332](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:332), [test_raf_prepare_data.py:124](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_prepare_data.py:124). The publish gate accepts any schema-1 JSON containing `verdict.passed=true` and two nonempty strings. The tests explicitly prove the bypass using `rooms={}`, the superseded `wxyz` pin, and no audit measurements. It neither authenticates the pinned record (`e879768f…`) nor requires the Amendment-4 pins, required rooms, audit thresholds, matching RAF root, or canonical split parameters. Fix: bind canonical publication to the pinned record hash/content and exact `(X,Z,Y)`/`xyzw` adjudication, validate all required measurements and root/rooms, and separate synthetic/noncanonical test mode from the production gate.

2. S2 — blocking — [render_depth.py:229](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:229), [render_depth.py:413](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:413), [render_depth.py:470](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:470). Canonical rendering accepts arbitrary `--max-miss-rate`; passing `0.05` makes 5% inpainting “within cap.” QA trusts that boolean rather than independently enforcing the registered 0.1%, so excess misses can be published and hidden behind passing QA. Fix: require the canonical value to equal or remain below `DEFAULT_MAX_MISS_RATE`, recompute rate/count consistency in QA against the fixed cap, and permit larger values only in explicitly tainted noncanonical output.

3. S3 — blocking — [publish.py:110](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/publish.py:110), [prepare_data.py:994](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:994), [render_depth.py:448](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:448). R7 remains non-transactional. Files are replaced individually while the previous manifest stays visible; failure on the kth replacement leaves mixed generations with an apparently committed manifest. Runtime and split roots are then committed separately, as are the two depth rooms. Existing failure tests stop before commit and never exercise these windows. Fix: implement rollback-safe directory/set publication or invalidate the old attestation before replacement and publish one generation-bound commit marker only after all roots/rooms succeed. Inject failures during `os.replace` and between commits.

4. S4 — blocking — [metric_callback.py:294](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:294), [metric_callback.py:328](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:328), [metric_callback.py:448](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:448). R8 is not fully closed. `pred_stft_id`/`ref_stft_id` contain the whole batch but are appended to every item’s scene accumulator, so every room’s `L1_STFT` contains other rooms and the macro test is tautological. Also, RAF calls without `scene` silently return per-item metrics and omit invalid count/rate. The normal scene-supplied invalid count/rate and RAF-only key gating are otherwise correct. Fix: use the indexed STFT for each scene, update the global metric once correctly, and fail closed when RAF scene attribution is absent or incomplete. Test independent unequal-room numeric oracles for every macro metric.

5. S5 — blocking — [render_depth.py:321](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:321), [render_depth.py:394](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:394), [test_raf_render_depth.py:391](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_render_depth.py:391). R6’s “landmark” check is circular: it compares the rendered farthest ray with the farthest vertex of the same transformed mesh, so a consistently wrong signed horizontal gauge still passes. AR/HAA scale references remain `null`, and `scale_plausible` is excluded from `passed`. The tests mirror this construction rather than use independent landmarks or receivers. Fix: gate on registered RAF-world landmarks/receiver bearings and actual AR/HAA reference statistics, persist that evidence in the pinned record, and include applicable scale/bearing checks in publication success.

6. S6 — should-fix — [prepare_data.py:795](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:795), [prepare_data.py:847](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:847). Statistics labeled “train supports only” include entries with role `support`, which are validation-only supports in canonical metadata. Thus the reported derivation set is contaminated by validation data; an injected `scale` is not proven to have been derived from that set at all. Fix: derive from the exact trained-support ID union only, keep validation supports separate, and require derivation provenance before applying a non-null scalar.

7. S7 — should-fix — [test_raf_metrics.py:290](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_metrics.py:290), [test_raf_metrics.py:305](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_metrics.py:305). R10’s implementation correctly follows `x.device`/`x.dtype`, and a read-only CPU bf16-autocast probe succeeded, but the registered autocast behavior is not tested: current tests run float32/double outside autocast. Fix: add CPU bf16-autocast coverage and, when CUDA is available, a non-default-device autocast case.

8. S8 — nit — [raf_common.py:28](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/raf_common.py:28), [raf_common.py:159](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/raf_common.py:159), [render_depth.py:163](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:163). Documentation still calls quaternion order unverified, the gauge a candidate, and says every miss aborts, contradicting Amendment 4 and current inpainting behavior. Fix the stale contract text.

Read-only audit note: static collection confirms exactly 333 test cases. I did not execute pytest because its temporary-file behavior would violate the mandatory read-only constraint. The ported owned files are byte-identical to `localization-exp@e23c45e`; only the declared post-port metric changes differ.