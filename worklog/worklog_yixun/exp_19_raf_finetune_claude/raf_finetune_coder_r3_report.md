# exp_19 r3 Coder report (Opus 5 subagent, 2026-08-20 ~00:30 EDT) — archived by the Planner; all deviations ACCEPTED

r3 complete on `raf-finetune-exp`: S1–S8 all closed; **393 passed, 1 deliberate opt-in skip** (`RAF_TEST_CUDA=1` gates the non-default-CUDA autocast test on this shared-GPU box; verified passing when enabled); metric-consumer suites green individually (32/112/42/8/81).

Commits: S4 `dbae7f2` (per-item STFT indexing — global value unchanged in exact arithmetic, pinned vs single-room references; RAF fails closed on absent/malformed scene labels; independent two-callback macro oracles); S1 `2897b2b` (gates bound to record sha256 e879768f… + exact pins + full measurement blocks + raf_root match; bypass-now-raises test; `--non-canonical` taint threaded through all artifacts); S2 `3253c2f` (`resolve_miss_cap` lower-only in canonical; QA re-derives verdict from preimages); S3 `6288d8a` (`PublishTransaction`: validate→invalidate attestations→swap→manifests→ONE generation-stamped commit marker last; marker-first verification; failure injection during `os.replace` and between roots); S5 `0c96b72` (circular landmark demoted to diagnostics; `rx_sightline_check` from tracked pose files gates publication — y/z-transpose test proves sensitivity; HAA scale references measured from real maps 0.50–11.55 m, `scale_plausible` gates when a reference exists); S6 `c7882e6` (scalar derivation = trained-support ids only, id-set sha256 provenance required); S7 `cf439b5` (CPU bf16-autocast + opt-in CUDA case); S8 `de2075a` (docs match Amendments 4–5); ledger `43a825e`.

Planner acceptance notes: deviation 1 (CUDA opt-in) accepted for this box; 2 (unit-level cap test, unreachable end-to-end behind the S1 gate) accepted; 3 (×3 HAA band) accepted; 4 (0.10 m + 1-px slack) accepted; 5 (marker-per-transaction locations) accepted; 6 (taint not inside loader-shape-pinned metadata JSONs) accepted; 7 noted.
