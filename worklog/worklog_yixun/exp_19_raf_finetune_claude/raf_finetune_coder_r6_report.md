# exp_19 r6 Coder report (Opus 5 subagent, 2026-08-20 ~04:40 EDT) — archived by the Planner; deviations ACCEPTED

r6 complete: all five Codex-r5 in-boundary findings closed; **511 passed, 1 opt-in skip** (+25); metric/loader consumer suites green after the dataset.py change (32/8/42/112/81 + test_are_anchor 76, test_invariant_conditioning 52, test_finetune_cond 43).

Commits: F2 `889e6e4` (`RAFPublicationError` + two-line explicit re-raise in `src/data/dataset.py` — the ENTIRE upstream diff is +19 lines; raised only by RAF_md (grep-test); true production-path tests via `create_dataloader_from_config`; narrowness proven — ordinary bad captures still substitute); F1 `bf40e78` (canonical-required, pointer output_dir == loaded tree BY INODE, combined verification always canonical=True; both named negatives); F3 `b0367b3` (complete-payload identity verification owned by the verifier `publish.py`, producers import the single copy; missing AND smuggled keys fail; digest required everywhere); F5 `5f5c37c` (mask_verified-True required for every pass incl. zero-miss; declarations demoted to `*_declared`, disagreement fails); F4 `0408941` (canonical miss cap EXACTLY DEFAULT_MAX_MISS_RATE, r5 allowance revoked; `--rx-sightline-receivers` wired/recorded/identity-checked); ledger `37ac7cb`.

Accepted deviations: exception looked up via sys.modules (hook exec'd by path); production tests index `loader.dataset[0]` (persistent_workers forbids num_workers=0; the dataset object IS what training indexes); identities moved to publish.py with producer aliases; render identity now six keys (pre-r6 markers correctly rejected; none exist). **Flagged for Yixun:** upstream footgun — a corrupted SUPPORT capture ⇒ unbounded substitution loop (pre-existing `SampleDataset` behavior, documented in a test, untouched).
