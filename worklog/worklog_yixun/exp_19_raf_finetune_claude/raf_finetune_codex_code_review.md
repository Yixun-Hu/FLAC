REQUEST-CHANGES

1. R1 — blocking — [prepare_data.py:331](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:331), [prepare_data.py:385](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:385). `--allow-nonuniform` admits every malformed group into FPS. The real 72-capture group `2d3514a210ef3296` contains 36 exact duplicate `(tx, rx)` pairs and falls into reserve only accidentally under the current ordering; the registered rule requires it to be ineligible. A different corpus/order could select it as 12/60 or 72-val, breaking counts and task geometry. Filter FPS eligibility to exactly-36 groups, narrowly validate and force the known 72 group to reserve with an exclusion reason, reject all other anomalies, and assert unique/tolerance-atomic `(tx pose, rx)` positions before splitting.

2. R2 — blocking — [prepare_data.py:360](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:360), [raf_test.json:7](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/RAF/eval/raf_test.json:7). The promised canonical artifacts do not exist: `data/RAF/` contains no `train_base.json`, `val_base.json`, `test_base.json`, `raf_splits_record.json`, or amplitude audit, so all three dataset configs are currently unrunnable. The additional one-group-per-room HAA-parity diagnostic is also absent: only train/test, val, and reserve roles exist, producing 40 rather than the planned 42 depth maps. Generate, hash, verify, and commit the canonical files; add a distinct diagnostic role/support/test manifest and include it in runtime metadata/depth rendering.

3. R3 — blocking — [RAF_md.py:68](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/RAF_md.py:68), [eval_FLAC.py:432](/home/yixunhu/codespace/FLAC/eval_FLAC.py:432). RAF exposes actual `context_capture_ids`, but the stream recorder ignores them and fingerprints `context_poses`. Thus the C7 fix does not reach its consumer, and near-duplicate RAF receivers remain indistinguishable. Prefer and strictly validate the int64 ID tensor when present, include room-qualified zero-padded IDs in the stream tuple, verify its length against `context_poses`, bump the fingerprint schema, and add an end-to-end RAF stream test.

4. R4 — blocking — [raf_common.py:28](/home/yixunhu/codespace/FLAC/data/RAF/raf_common.py:28), [raf_common.py:159](/home/yixunhu/codespace/FLAC/data/RAF/raf_common.py:159), [FLAC_RAF_finetune.json:187](/home/yixunhu/codespace/FLAC/src/configs/model_configs/FLAC/RAF/FLAC_RAF_finetune.json:187). The code still labels quaternion order and the gauge as unverified/candidate, while enabling headline T60 unconditionally. No implemented gate performs the approved onset-vs-distance regression, acquisition-delay decision, crop-vs-full T30 audit, amplitude comparison, or final gauge pinning. Add a reproducible readback command/artifact and make canonical preparation/evaluation refuse to proceed unless it passes; enable/report T60 only after the registered truncation gate resolves.

5. R5 — blocking — [render_depth.py:116](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:116), [render_depth.py:159](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:159), [RAF_md.py:79](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/RAF_md.py:79). `depth_qa` accepts any two-dimensional shape, the CLI permits arbitrary dimensions, while the loader hard-codes `256×512`. A non-default render can therefore pass QA and later fail inside metadata loading, triggering silent sample substitution. Enforce exactly `(256,512)` for canonical RAF, or record dimensions and consume them dynamically. Also validate cached depth shape, float32 dtype, finiteness, and positivity at load time.

6. R6 — blocking — [render_depth.py:102](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:102), [test_raf_render_depth.py:90](/home/yixunhu/codespace/FLAC/src/tests/test_raf_render_depth.py:90). The six-wall expected literals are independent of the production ray generator and do catch row flips, but only the synthetic half of C3/C13 was delivered. There is no real-mesh camera-containment check, room-bound check, front/left landmark bearing, receiver-bearing check, or RAF-vs-AR/HAA depth-scale decision. Consequently the synthetic oracle cannot pin whether the candidate external-world gauge is semantically correct. Add fail-closed real-mesh QA and persist its results before canonical maps are accepted.

7. R7 — blocking — [prepare_data.py:472](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:472), [prepare_data.py:612](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:612), [prepare_data.py:739](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:739), [render_depth.py:181](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:181). WAVs, metadata, splits, audits, depth maps, and QA files are published directly and sequentially. A late bad file, NAS interruption, or disk-full event leaves a plausible mixture of old and new artifacts; reruns can overwrite audio before replacing the old split. Stage a complete run under a unique temporary directory, validate all outputs, atomically replace files/directories on the same filesystem, and publish a hash manifest/commit marker last. Add failure-injection tests.

8. R8 — blocking — [metric_callback.py:298](/home/yixunhu/codespace/FLAC/src/metrics/metric_callback.py:298), [RT60.py:37](/home/yixunhu/codespace/FLAC/src/metrics/modules/RT60.py:37), [test_raf_metrics.py:55](/home/yixunhu/codespace/FLAC/src/tests/test_raf_metrics.py:55). RAF’s 9,600-sample routing and FD guard are present, but equal-room aggregation is not: the top-level result remains a per-item mean, while `by_scene` is merely exposed. Equal canonical room counts hide this defect. Moreover `"Invalid T60"` is the mean of 0/1 flags—a rate—not the promised count. Return explicit RAF macro-room metrics and both invalid count/rate, leaving AR/HAA outputs unchanged; test unequal room sizes and multiple invalid samples with numeric expectations.

9. R9 — blocking — [test_raf_md.py:418](/home/yixunhu/codespace/FLAC/src/tests/test_raf_md.py:418). The claimed “real batched conditioner pass” never occurs. This test stops after `SampleDataset` plus `collation_fn`; it does not invoke `MultiConditioner`, any configured conditioner, or `get_conditioning_inputs`. Add a batched pass through the actual model-conditioning configuration—using lightweight injected backbones if necessary—and assert all produced token/global shapes and dtypes. This is the consumer-level contract required by C10.

10. R10 — should-fix — [FLAC_RAF_finetune.json:192](/home/yixunhu/codespace/FLAC/src/configs/model_configs/FLAC/RAF/FLAC_RAF_finetune.json:192), [test_raf_metrics.py:21](/home/yixunhu/codespace/FLAC/src/tests/test_raf_metrics.py:21), [l1_stft_multires.py:41](/home/yixunhu/codespace/FLAC/src/metrics/modules/l1_stft_multires.py:41). The RAF config enables multires-L1, but its update is deliberately excluded from testing and hard-codes `.cuda()`. CPU evaluation fails, and `--device cuda:1` can create a device mismatch when both GPUs are visible. Create the Hann window on `x.device` with a compatible dtype and exercise the exact enabled RAF metric set.

11. R11 — should-fix — [test_raf_configs.py:22](/home/yixunhu/codespace/FLAC/src/tests/test_raf_configs.py:22). The clone whitelist is not structurally complete. `_flatten` emits no entry for empty dictionaries/lists, so a stray empty key can ride in undetected; dotted/bracketed JSON keys can also collide with generated paths. Compare recursive node paths, container types, lengths, and leaves, or deep-copy the HAA template and apply an explicit patch before exact equality. Also assert every referenced split file exists.

12. R12 — should-fix — [render_depth.py:73](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:73), [render_depth.py:177](/home/yixunhu/codespace/FLAC/data/RAF/render_depth.py:177). Every map reconverts the legacy mesh and rebuilds a `RaycastingScene`/acceleration structure. The real OBJ files are roughly 212–215 MB each, so doing this approximately 40–42 times is an avoidable real-scale bottleneck. Convert and add the mesh once per room, reuse the scene for every camera, and benchmark the canonical run.

13. R13 — blocking — [prepare_data.py:612](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:612), [prepare_data.py:631](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:631). The amplitude artifact records only in-memory, all-selected-item statistics. It never reads written FLOAT WAVs back, measures round-trip error, separates train-support statistics from test/val data, compares RAF with HAA/AR, or records/applies the approved fixed-scalar decision. Zero-valued files also serialize non-standard `-Infinity`. Read back every output, record strict-JSON values, compute role-specific distributions, and derive any scalar solely from train supports before applying it uniformly.

14. R14 — should-fix — [prepare_data.py:394](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:394), [prepare_data.py:409](/home/yixunhu/codespace/FLAC/data/RAF/prepare_data.py:409), [RAF_md.py:178](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/RAF_md.py:178). Amendment D5 is implemented and direct self-context is excluded, but all 12 support RIRs are also validation targets. Validation is therefore transductive and mixed: support targets draw from 11 candidates, while the other 24 draw from 12, unlike HAA’s context-only support partition. Make the 12 supports context-only and validate on the remaining 24, or explicitly rename/document this as a non-HAA-parity monitoring set and never use its metric inferentially.