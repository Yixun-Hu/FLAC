REQUEST-CHANGES

1. T1 — blocking — [readback_audit.py:401](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:401), [readback_audit.py:421](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:421), [readback_audit.py:439](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:439). Problem: S1 has a TOCTOU bypass. The JSON is parsed, then the path is reopened for hashing, then reopened again for provenance. A swapped file/symlink can therefore supply forged parsed content while the second open supplies the pinned bytes. The fallback content check only tests block presence, onset `passed`, and a nonempty T60 resolution—not every sub-verdict. `raf_root` is also only compared with `abspath`, which authenticates a pathname rather than the corpus behind a replaceable symlink/mount. Fix: read bytes once from one descriptor, hash those exact bytes, parse those bytes, and carry that authenticated digest into provenance; validate all measurement verdicts and bind canonical publication to a corpus manifest rather than only a lexical root.

2. T2 — blocking — [prepare_data.py:924](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:924), [prepare_data.py:955](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:955), [render_depth.py:654](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:654). Problem: canonical mode does not enforce the canonical rooms or split parameters. A run using only `FurnishedRoom`, arbitrary `n_groups`/`n_train`, sampled cross-checking, or `--allow-nonuniform` can still publish artifacts and a marker carrying `"canonical": true`. This also permits a canonical Furnished-only render, bypassing EmptyRoom’s supposedly unconditional S5 gate. Fix: validate the exact canonical room set and registered preparation parameters before staging; any deviation must require `--non-canonical` and be included in the marker identity.

3. T3 — blocking — [prepare_data.py:1007](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:1007), [prepare_data.py:1015](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:1015), [dataset.py:29](/home/yixunhu/codespace/exp-19-raf-finetune/src/data/dataset.py:29), [RAF_md.py:31](/home/yixunhu/codespace/exp-19-raf-finetune/src/configs/dataset_configs/custom_metadata/RAF_md.py:31). Problem: noncanonical taint is readily laundered. The loader-consumed split JSONs, metadata, WAVs, and depth arrays contain no taint, and neither dataset construction nor `RAF_md` verifies a publication marker. Placing noncanonical output at the configured paths makes it indistinguishable to training/evaluation; the same omission means a crash-produced mixed tree is consumed despite having no valid marker. Fix: add a mandatory RAF startup gate that verifies canonical preparation and depth markers, their exact roots/rooms, pinned readback digest, and artifact hashes before constructing the dataset; keep noncanonical artifacts out of canonical paths or put enforceable provenance in a loader-consumed manifest.

4. T4 — blocking — [publish.py:114](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/publish.py:114), [publish.py:205](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/publish.py:205), [publish.py:260](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/publish.py:260), [prepare_data.py:976](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:976), [render_depth.py:713](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:713). Problem: S3’s attestations interfere and are under-validated. Rendering stores its marker at the runtime root; a later preparation transaction stages that root and blindly renames the render marker aside, although it does not regenerate depth. Moreover, `verify_publication` accepts an empty `roots` object as published and has no expected-root/room set. Thus a successful rerun can orphan valid depth evidence, and partial/Furnished-only markers still look valid to the verifier. Fix: namespace transaction markers or use one combined preparation-plus-depth marker; reject zero, duplicate, or unexpected roots and require the exact canonical room/root set. Add prepare→render→prepare and crash/composition tests.

5. T5 — blocking — [prepare_data.py:321](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/prepare_data.py:321), [render_depth.py:103](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:103), [render_depth.py:423](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:423), [test_raf_render_depth.py:844](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_render_depth.py:844). Problem: S5’s receiver sightline is not gauge-independent. The same `RAF_TO_PIPELINE` matrix transforms mesh, tx, and rx. Any consistently wrong signed permutation preserves distances, containment, and line visibility, so the inequality still passes; the test instead transposes only rx after rendering the correctly transformed mesh, testing an inconsistent pipeline. The 3×3 maximum and 0.10 m slack only make acceptance looser, while FurnishedRoom failures never gate. Fix: use independently registered raw-world bearings/landmarks tied to pipeline axes, and test candidate errors by changing the transformation for both mesh and poses. Enforce the EmptyRoom evidence through the canonical room-set gate.

6. T6 — blocking — [metric_callback.py:293](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:293), [metric_callback.py:355](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:355), [metric_callback.py:474](/home/yixunhu/codespace/exp-19-raf-finetune/src/metrics/metric_callback.py:474). Problem: S4’s scene gate accepts tuples, but attribution indexes only lists. A valid-length tuple therefore becomes one tuple-valued scene key for every item, collapsing the macro to one pseudo-room. Unknown nonempty labels also pass, and final computation never requires exactly `EmptyRoom` and `FurnishedRoom`. Fix: normalize list/tuple inputs and always index them; validate labels against the RAF room set and require exactly both rooms before emitting canonical macro metrics.

7. T7 — blocking — [render_depth.py:325](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:325), [render_depth.py:334](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:334), [render_depth.py:363](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:363). Problem: S2’s “re-derivation” accepts forged reports. For example, `{"miss_count": 0, "within_cap": true}` passes without coordinates, hash, declared ray count, or original miss evidence because `coordinates is None` makes both consistency checks true. A self-consistent forged coordinate list likewise cannot be tied to the already-inpainted depth map. Fix: derive misses directly from raw pre-inpaint ray hits; require unique, in-bounds coordinates, exact count/ray-count consistency, and the canonical hash even for the empty set.

8. T8 — should-fix — [test_raf_metrics.py:316](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_metrics.py:316), [test_raf_metrics.py:360](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_metrics.py:360), [test_raf_metrics.py:381](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_metrics.py:381). Problem: the macro oracles are not genuinely independent. `_single_room_reference("RAF", ...)` runs the same callback and its result is overwritten by the same `by_scene` macro path, contrary to its “global accumulators only” claim. Separately, GLOBAL `L1_STFT` is not unchanged from pre-fix arithmetic across unequal update batch sizes: the old code weighted batch \(B\) by \(B^2\), while the new code weights it by \(B\). The sole global regression uses one update call and cannot expose this AR/HAA behavior change. Fix: use hand-derived or pinned numeric goldens outside `AcousticMetricsCallback`, and add unequal-size multi-update tests; explicitly accept/version the corrected legacy result or scope the behavior change.

9. T9 — should-fix — [test_raf_render_depth.py:869](/home/yixunhu/codespace/exp-19-raf-finetune/src/tests/test_raf_render_depth.py:869). Problem: the reported suite is not checkout-reproducible. Two S5 tests require `HAA`, which is currently an untracked symlink to `/media/diskstation/yixunhu/HAA_processed`; a clean worktree lacks it. Static expansion is consistent with 394 collected cases—393 passes plus the deliberate skip—but green status depends on unversioned external data. Fix: use committed synthetic depth fixtures for unit tests and make the real-HAA check an explicit hashed integration test.

10. T10 — nit — [readback_audit.py:21](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:21), [readback_audit.py:199](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/readback_audit.py:199), [render_depth.py:227](/home/yixunhu/codespace/exp-19-raf-finetune/data/RAF/render_depth.py:227). Problem: S8 remains incomplete: usage still demonstrates `wxyz`, quaternion diagnostics still say the release does not state the order, and depth QA still claims “full hit rate.” Fix: update all stale text to the `xyzw` pin and cap-plus-inpaint policy.