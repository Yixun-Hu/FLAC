# exp_19 r4 Coder report (Opus 5 subagent, 2026-08-20 ~01:25 EDT) — archived by the Planner; deviations ACCEPTED

r4 complete on `raf-finetune-exp`: T1–T10 closed per Amendment 6's bounded dispositions; **462 passed, 1 opt-in skip** (+69); metric-consumer suites green after every metric-touching commit.

Commits: T8 `ad631e9` — **allegation CONFIRMED**: pre-S4 arithmetic weighted a size-B batch's items B× (B² entries), so the r3 fix had silently re-weighted AR/HAA global L1_STFT under `drop_last=False` tail batches; restored bug-compatible for AR/HAA, corrected per-item for RAF; per-scene stays per-item for all datasets (cross-room legacy has no defensible semantics); goldens re-implemented independently of the callback. T6 `6bff9c7`; T7 `4742954` (QA from raw pre-inpaint hits); T4 `6fdda0e` (fail-safe direction: older attestation reads unpublished); T2 `c68d11b` (canonical set incl. the registered allow_nonuniform for the 72-group, into marker identity); T1 `5f40ebc` (read-once digest; full sub-verdict validation; pose-digest corpus binding — pinned record predates the block, gate falls back to capture counts with the fact recorded; RE-PIN required, Planner's call); T3 `d3381d9` (opt-in `RAF_REQUIRE_PUBLICATION=1` first-load marker gate — canonical run scripts MUST set it; registered as the one operator switch); T5 `ae9d308` (vertical nadir gate joins publication; `DETECTABILITY_BOUNDARY` recorded in every QA file; horizontal permutation provably render-undetectable — test asserts it — pinned by derivation, RESIDUAL for Yixun); T9 `cde1083` (committed synthetic fixtures + hash-pinned real-HAA integration test, 0.5038–11.5523 m); T10 `0e100f0`; ledger `dfd440e`.

Deliberately NOT implemented (beyond the registered threat boundary, recorded): full-corpus hashing; per-item loader hashing; fstat/inode TOCTOU hardening; cryptographic marker signing; external-landmark horizontal-gauge evidence (would require a surveyed feature/compass bearing no project artifact supplies).
