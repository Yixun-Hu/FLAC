# bf_fa_cartesian_params_set_up.md — exp_21 configuration (written at the SMOKE/pre-launch stage; final values pinned)

## Arm
- **Method:** `cond_method: "fa_cartesian"` — full C4 frame averaging with Cartesian poses (Yixun Query 2, verbatim in the query file): per frame g ∈ {0°,90°,180°,270°}, depth + all four pose keys rotate jointly; rotated `source`/`context_poses` feed the UNCHANGED DistEmbedders as Cartesian xyz; all four pose/geometry conditioner outputs averaged; `context_audio` single-pass.
- **Config:** `FLAC_AR_BFC.json` = byte-copy of exp_07 `FLAC_AR_BF.json` except: `training.cond_method` fa_invariant→fa_cartesian; ADDED `training.frame_avg_max_fwd_samples: 32` (D5: one angle per chunk = B-F's legacy per-angle draw schedule; at K=8, 4×(1+8)=36 DINO draws per conditioning step). `frame_avg_angles [0,90,180,270]` kept. Optimizer AdamW 5e-5/(0.9,0.999)/wd 1e-3, InverseLR(1e6, 0.5, warmup 0.99), EMA on, cfg_dropout 0.1, metrics AGREE_fullAR — all untouched.
- **Init:** from scratch, seed 42. Seed-42 init-identity audit: BFC ≡ BF state-dict sha `44a2f6aa…` (753 tensors, 64.50M params), verified in the launcher gate.

## Training recipe (REGISTERED mode, pinned in bfc_launch.sh, non-overridable)
2×A6000 co-tenant (per-GPU free-VRAM floor 20 GiB); `--batch-size 32 --num-gpus 2 --accum-batches 1` (BN batch 64 via SyncBN — string-pinned rung), `--strategy ddp_find_unused_parameters_true --sync-batchnorm true --precision bf16-mixed --num-workers 6 --seed 42 --max-steps 40000 --checkpoint-every 2500 --logger wandb`, NO val loader (B-F parity: validation would advance the RNG stream), no resume, VAE `weights/FLAC/VAE.safetensors`, ViT grad-ckpt via config, `HF_HUB_OFFLINE=1`, `CUDA_VISIBLE_DEVICES=0,1`. Save-dir `outputs_FLAC/exp21_BFC`.
- Chunk plans (announcement 06) — the THREE declared partitions, each recomputed from `_orbit_average_batched`'s `angles_per_chunk = max(1, cap // batch)` over the 3 nonzero C4 angles:
  1. **TRAIN** — micro-batch 32/rank, cap 32 → `max(1, 32//32) = 1` → chunks `[1,1,1]`, i.e. one DINOv3 RoPE draw per angle, matching B-F's training-era schedule (D5).
  2. **EVAL, full batch** — batch 64, cap 64 → `max(1, 64//64) = 1` → chunks `[1,1,1]`.
  3. **EVAL, tail batch** — 6337 % 64 = 1 sample, cap 64 → `max(1, 64//1) = 64` → all 3 nonzero angles share ONE chunk, `[3]`.
  ⚠ CORRECTION (r5 prelaunch fix): this file previously said the full-eval rung was **2** angles/chunk. It is 1. Two-per-chunk arises only at batch 32 with cap 64 — the rung-4 PROBE's configuration, not the eval protocol's. No published number changes (eval mode draws no RoPE, so the partition is numerically inert — measured bit-exact, below), but a mis-declared chunk plan is exactly what announcement 06 exists to prevent.
- Train tail: 291,210 records at effective batch 64 (32/rank × 2 ranks) leaves **10 samples globally, 5 per rank**; `drop_last=True` discards them, so no partial batch ever reaches BN or the orbit. (The earlier "one-sample train tail" wording confused this with the EVAL split's 6337 % 64 = 1 tail, which is a different rung.)

## Eval protocol (announcement 05, generated from exp21_protocol.py only)
`--cond-method fa_cartesian --frame-avg-angles 0,90,180,270 --frame-avg-max-fwd-samples 64 --rotate-mode fixed --rotate-deg <angle> --cond-autocast bf16 --batch-size 64 --cfg-scale 1.0 --steps 1 --record-per-scene --record-stream --expected-stream-count 6337`, full unseen configs (K8 `acousticroom_unseeneval.json`, K1 `..._1.json`), seeds 42–46, `--eval-name exp21_BFC_S40000_K{K}_s{seed}[_rot{deg}]`. EVAL cap 64 (1 angle/chunk on full batches; the split's one-sample tail groups all 3 nonzero angles — disclosed). Cap-32-vs-cap-64 parity on real DINOv3 (rung 4, batch 32): **bit-exact 0.000e+00 at BOTH the registered bf16 autocast and fp32**. D6 comparators: BFre (`fa_invariant`, ckpt sha pinned `5319feb4…`) and P1re (`vanilla`, `c4c67882…`), same flags per arm, eval-names `exp21_{BFre,P1re}_S40000_K{K}_s{seed}`.

## Admission (gen_model_comparison + exp21_validate_cell)
Per cell: full split (n_samples 6337), 10 room-family per-scene payload, seeds exactly {42..46}, EMA, bf16 autocast, batch 64, cfg 1.0, steps 1, rotate 0, real angles, eval cap 64, `trained_cond_method` receipt, well-formed `ckpt_sha256` (fullmatch), exact parsed step 40000. Cross-arm: SIX-row transaction (BFC/BFre/P1re × K1/K8), one `source_sha`, one digest per arm across K (comparators pinned to the reviewed digests), per-(K,seed) input identity recomputed from sidecar preimages; historical B-F/P1 rows CONTEXTUAL ONLY.

## Pre-registered readout (plan §5)
Paired per-seed deltas BFC−BFre and BFC−P1re; primary T60+EDT K=8; all six metrics + K=1 reported. Invariance: C4 spread ≤ T60 0.005 / C50 0.0005 / EDT 0.006 / R@1 0.15; 45° REQUIRED to exceed on T60 or EDT. 5 eval seeds = sampling variability only; ONE training seed. BFC/BFre ≈ matched compute; BFC/P1re matched steps NOT compute (FA ≈3.5×/step).
