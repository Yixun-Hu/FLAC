# exp_14 yaw_gen — lab notebook (append-only)

## 2026-08-10T23:09:07-04:00 — Scaffold + understanding verification

- **Goal** — Open exp_14: yaw-shift generalization of vanilla vs C4/C8/C16/C32 frame-averaged arms. Scaffold the folder, record the query, verify understanding with Yixun before planning.
- **Result** — `passed`. Folder scaffolded; `yaw_gen_yixun_query.md` written. Yixun confirmed via AskUserQuestion: (1) yaw shift = physically-consistent conditioning rotation (`--rotate-deg`; depth roll + pose rotation, GT unchanged); (2) grid = FULL (2 K × 5 seeds, announcement-04 table grade).
- **Analysis** — All five matched-recipe arms exist at step 40,000 on this machine (8×8, training seed 42): `outputs_FLAC/exp11_{VANL,C4L,C8,C16,C32}`, sha-pinned in exp_11's committed `arm_launch_registry.json` (jobs 3661520 / 3648694–97). Eval machinery already exists: `eval_FLAC.py --rotate-deg --cond-method {vanilla,fa_invariant} --frame-avg-angles`, `src/data/yaw_rotation.py`, and the exp_11 screen kit (`fa_orbit_screen.sbatch` / `fa_orbit_screen_submit.sh`) with `ROTATE_DEG` / `EVAL_ORBIT` plumbing.
- **Next** — R3 forensics (below), then `plan_yaw_gen.md`.

## 2026-08-10T23:09:07-04:00 — Forensics: why exp_11 R3 (yaw-flatness) produced zero rows

- **Goal** — Before planning, establish what exp_11's R3 round already did and why it has no results, so exp_14 doesn't re-plan a solved problem or re-hit a known failure.
- **Result** — `passed` (diagnosis complete, zero new compute):
  - R3 design (exp_11 plan §R3, review B9): rotations {0 (ref), 5.625, 11.25, 22.5, 45}° at s42 K8 per arm, own protocol, in-group/off-group labels. Sound design; scope was single-seed, K8-only, no 90°, no VANL, and C32 wasn't trained yet.
  - 17 R3 screens launched 2026-08-08 19:46 (jids 3657730–3657746; C4L/C8/C16 × 5 angles + partial C8 resubmission). **Every one died identically**: `FileNotFoundError: weights/FLAC/VAE.ckpt` while the metric callback built the AGREE audio tower inside the measure-worktree.
  - Root cause (infrastructure, NOT a code bug in the eval path): the R3 jobs' pinned measure-worktree `.measure_worktrees/75f59342…` was provisioned 19:45 under a provisioner that symlinked only `weights/AGREE` + `weights/FLAC/VAE.safetensors` as leaf assets. That pin's tracked tree also carries committed metric JSONs under `weights/FLAC/` (announcement-04 raw evidence), so `weights/` materialises as a real tracked directory with JSONs and **no `VAE.ckpt`**. Commit `16b6a20` ("VAE.ckpt in the leaf assets") + `868e93a` fixed the provisioner; the conf worktree `0c6e9ffb…` re-provisioned 23:22 the same night has both VAE symlinks, and every subsequent conf screen COMPLETED (e.g. C16 conf jids 3658834–41; C32 conf 3672838–47).
  - R3 was never resubmitted; exp_11 closed 2026-08-10 with the θ=0 orbit trend complete and yaw-flatness untested.
- **Analysis** — Classification per SOP: *infrastructure* failure with the fix already merged ancestor-of-HEAD. exp_14 screens at a fresh campaign pin (HEAD descendant) will not encounter it; the smoke cell will re-verify the leaf-asset provisioning explicitly (regression check written into the plan's validation ladder).
- **Next** — Write `plan_yaw_gen.md` (supersedes R3 at table grade: + VANL, + C32, + 90°, 2 K × 5 seeds, absolute + paired-Δ readouts), then Codex plan review.

## 2026-08-10T23:12:22-04:00 — Protocol amendment from Yixun: random per-sample yaw replaces the fixed angle ladder

- **Goal** — Record and absorb Yixun's mid-session directive (query 2 in `yaw_gen_yixun_query.md`) before the plan is written.
- **Change** — Design pivot, no code yet. Headline protocol: for each eval sample, draw an independent random panorama-column roll (θ quantized to exact 360°/512 columns — interpolation-free by construction, same quantization `rotate_scene_metadata` already applies), rotate depth + poses together, GT unchanged; report robustness across arms. The fixed C4/C8/C16/C32 angle ladder is dropped as the headline readout (a handful of fixed-angle cells survive only as internal validity controls in the plan draft, subject to review/approval).
- **Analysis** — Feasibility checked against the code: `eval_FLAC.py` already applies the rotation per-sample (`metadata` is a list of per-sample dicts; `rotate_scene_metadata(md, alpha_rad, img_w)` in a comprehension), so random mode = draw one quantized offset per sample from a seeded generator in dataloader stream order (shuffle=False ⇒ deterministic order ⇒ arms can share identical assignments at a given rotation seed, making cross-arm contrasts rotation-matched). Current `--rotate-deg` is one fixed angle per run, so a small guarded CLI extension (`--rotate-mode {fixed,random}`) is required; fixed mode must stay byte-identical to legacy. Grid collapses from 300 cells (angle ladder) to ~105 (50 random-yaw + 50 θ=0 reference + ~5 fixed-angle validity controls) ≈ 25–45 GPU-h at measured per-cell times (13–36 min on one L40).
- **Next** — `plan_yaw_gen.md` under the random-yaw design; then Codex plan review.

## 2026-08-10T23:26:24-04:00 — Yixun confirms three plan-level design decisions (AskUserQuestion)

- **Goal** — Lock the user-level choices in plan Rev 1 before the review lands, per Yixun's invitation to surface alignment questions.
- **Result** — `passed`; all three as recommended:
  1. **Primary criterion = absolute metrics under random yaw (`m_R`)** — the headline "higher order outperforms" verdict and Holm-corrected inference attach to `m_R` (deployment reading). Degradation Δ = m_R − m_Z is reported as the secondary/mechanism readout. (Plan §4's H3 becomes PRIMARY; H2 flatness demoted to mechanism.)
  2. **Rotation seed = eval seed** (5 independent yaw assignments; seed variance includes sampling + yaw-draw components; cross-arm rotation matching unchanged).
  3. **V-cells kept as QA gates** (s42/K8, 90° in-group floor × 4 arms + C4L@45° positive control; worklog/validity section only, never headline).
- **Analysis** — Decisions recorded here rather than edited into `plan_yaw_gen.md` immediately: the Codex reviewer (background task `blj46d9ch`, started ~23:16 EDT) is reading the plan file right now — never edit an artifact while it is being consumed (same principle as "never edit a running script"). The decisions go into plan **Rev 2** together with the review findings.
- **Next** — Review lands → address findings + fold in these confirmations → Rev 2 → Yixun approval.

## 2026-08-10T23:37:11-04:00 — Codex plan review round 1 returned (REVISE) → plan Rev 2 written

- **Goal** — Close the plan-review loop: absorb the Reviewer's findings and Yixun's three design confirmations into a revised plan.
- **Result** — `fix_ready`. Review (`yaw_gen_codex_plan_review.md`, gpt-5.6-sol xhigh, `codex exec -s read-only` — accepted on this host this run, unlike exp_11's; 194k tokens): verdict **REVISE**, 8 BLOCKING + 2 NIT, zero method-fatal findings — the estimand, Z-reference design, and drawing scheme were judged sound; the blocks are provenance/statistics/compat rigor. All 10 addressed in `plan_yaw_gen.md` **Rev 2** (per-finding changelog = plan §10). Highest-value catches: (B1) dataset item-substitution + stochastic context selection make an offset-only hash insufficient — replaced with per-position `(target, contexts, img_w, offset)` canonical hashes + substitution guard; (B2) validity gates now executable formulas (G1 floor ≤ 0.5·σ̂_Z, G2 VANL@90 positive control ≥ 5·σ̂, G3 golden assignment, G4 hash equalities), VANL@90 added (grid 106); (B4) fixed-mode byte-compat contradiction resolved (conditional provenance, snapshot matrix); (B7) `gen_model_comparison.py` needs an explicit exp_14 row contract — Q9 VANL spec replaced + relabeled, within-pin fa-vs-vanilla delta delivered in exp_14 results (supersedes the never-run Q9 estimand).
- **Version Control** — no code yet (plan phase); working tree verified clean of reviewer writes (only known untracked paths + the concurrent session's new `exp_15_yaw_aug_claude/` scaffold).
- **Analysis** — One environment note: the auto-mode classifier blocked the exp_11-style `--sandbox danger-full-access` invocation; the SOP-standard `-s read-only` ran to completion this time despite `max_user_namespaces=0`, so future reviews here should try `-s read-only` first. Concurrent-session discovery: **exp_15_yaw_aug** (yaw-augmentation training) scaffolded mid-review by the other session; its query doc consumes exp_14's random-yaw protocol. Seam ownership recorded in plan §9 (exp_14 lands `eval_FLAC.py` + `yaw_rotation.py` helpers first; exp_15 builds on them; no parallel edits).
- **Next** — Present Rev 2 to Yixun for approval. On approval: Coder round 1 (§5.1–5.3 TDD) → per-round Codex review → rounds 2–3 → ladder → launch.

## 2026-08-10T23:41:15-04:00 — Rev 2 APPROVED by Yixun → Coder round 1 opens

- **Goal** — Begin implementation per approved plan Rev 2: round 1 = §5.1 (`yaw_rotation.py` helpers) + §5.2 (`eval_FLAC.py` random mode) + §5.3 (TDD suite), Coder = Opus 5 subagent at max effort.
- **Version Control** — `git pull --rebase` clean: local exp_11 anchor commit `89f24cd` (other agent, this checkout) rebased onto `f579777` (A6000 push, exp_10 A4 pre-registration). **Round-1 base = `89f24cd`.** Rule in force: pull --rebase before every commit; exp_11/exp_15 folders untouched.
- **Acceptance criteria (round 1, written before the Coder runs)** — (1) golden fixed-mode fixtures captured from base `89f24cd` BEFORE any edit; (2) all §5.3 tests exist and the suite runs red→green per cycle; (3) fixed-mode snapshot matrix proves byte-identical records/paths/dispatch; (4) `--rotate-mode random` draws per-sample offsets from a dedicated generator (global RNG untouched), applies per-sample rotation, accumulates §3.3 tuples, computes `input_hash`/`assignment_hash` with the canonical serialization, hard-errors on the two guard cases; (5) `_rotrand<seed>` naming injective; (6) commits < 200 changed lines each off `89f24cd`, no pushes, no GPU, no env changes, existing eval/yaw tests still green; (7) Coder reports any metadata gaps (esp. context-source identity availability for the fingerprint) rather than silently improvising.
- **Result** — `launched` (subagent dispatched; report to follow).
- **Next** — Coder report → Codex round-1 code review → fixes → close round.

## 2026-08-11T00:10:59-04:00 — Coder round 1 delivered; three Planner rulings; Codex review firing

- **Goal** — Absorb the Coder's round-1 report, rule on its three flagged gaps, dispatch the round-1 code review.
- **Change** — Commits `66e6ca5 → 16d7d13` (5 exp_14 commits; ledger updated). Effective base drifted `89f24cd → 44788e6` (concurrent session, worklog-only; both source files byte-identical) — fixtures provably captured at `44788e6` per the fixture's `_meta.captured_from_sha`. Acceptance criteria: all 7 met (red-first evidenced 17/15/21/27; new suites 98 passed; combined subset 394 passed; fixed-mode snapshot matrix green; no pushes/GPU/env changes; fingerprint gap reported not improvised). Three commits exceed 200 lines on test code only (source deltas 83/106/170/153) — accepted, disclosed.
- **Planner rulings on the Coder's flagged gaps (pre-registered before any cell runs):**
  1. **G4 Z-side hash (BLOCKING, fix in round-1 closure):** add `--record-stream` opt-in to `eval_FLAC.py` — writes a separate `<stem>.stream.json` sidecar (input_hash, per-position target ids, count) and never touches the metrics record, so the fixed-mode byte-compat contract is preserved. The kit passes it for **all** cell classes (Z/R/V); G4's `Z.input_hash == R.input_hash` check reads the sidecars.
  2. **R@k confound (plan §4 amendment):** in R cells the retrieval gallery embeds the *rotated* point cloud through a non-yaw-invariant AGREE, so absolute R@k mixes model robustness with AGREE's own sensitivity (cross-arm contrasts stay internally valid — galleries are rotation-matched — but sensitivity may collapse). **Co-primary #2 swapped R@1 → FD** (audio-embedding-only, rotation-clean per code inspection) for H-P/H-M/H-S; R@1/R@5/R@10 remain fully reported as descriptive with the common-gallery disclosure. Metric-callback input semantics stay legacy (rotated metadata), consistent with exp_02/exp_11 rot cells. Amendment made before any R-cell data exists; Yixun can veto at launch sign-off.
  3. **Substitution guard (fix in round-1 closure):** strengthen count-only guard with the Coder's own suggestion — per-position `idx == i` assertion (direct substitution detection; red test first).
  - Context fingerprint: the derived `context_poses`-based fingerprint (6-decimal canonical strings, pre-rotation) is **accepted** for this campaign — it identifies the stochastic context draw exactly, which is what G4 needs; the `context_audio`-hash upgrade is noted as future-proofing, not required.
- **Command / Validation** — Codex round-1 review dispatched over the five commits + Coder report + these rulings (prompt in session scratchpad; `-s read-only`).
- **Result** — `in_progress` (review running).
- **Next** — Review verdict → Coder fix batch (rulings 1+3 + any blocking findings) → re-verify → close round 1 → round 2 (screen kit).

## 2026-08-11T00:33:42-04:00 — Codex round-1 code review: REVISE (3 blocking, 2 nits); Planner ruling 2 OVERTURNED

- **Goal** — Absorb the round-1 code review (`yaw_gen_codex_code_r1_review.md`) and dispatch the fix batch.
- **Result** — Verdict REVISE. Reviewer independently re-derived the seed-42 offsets and both canonical hashes (all matched), re-executed pre-change helpers against every golden fixture (zero mismatches), reran the new suites (98 passed), and confirmed fixture capture predates all source edits. Findings: **B1** count enforcement tautological (`len(stream)==len(dataset)` passes even for an empty dataset) → require the campaign's explicit expected count with rejection tests; **B2** the ruled `--record-stream` sidecar payload was underspecified → must carry the full canonical input tuples, offsets/assignment tuples, both hashes, schema version, count, written only post-validation; **B3** **my ruling 2 (R@1→FD co-primary swap) REJECTED with code evidence** — the reported table R@1 is `RIR_to_GT_RIR_R@1` (audio-to-audio; `Retrieval.py`), only `RIR_to_geom_R@k` embeds the rotated point cloud, FD confirmed audio-only; **N4** context fingerprint is float32-pinned but not dtype-stable (measured: 2 six-decimal strings change under float64, 5,032 under float16) → fail-closed dtype/finite/shape assertions + schema version; **N5** add a real multi-worker DataLoader ordering test.
- **Analysis** — **Planner ruling 2 reverted**: co-primaries remain **T60% + R@1** exactly as in approved plan Rev 2 (which was never edited for the swap — no plan change needed); the confound disclosure narrows to `RIR_to_geom_R@k`, which will be reported as confounded-descriptive in R cells. My ruling rested on the Coder's mis-attribution of the retrieval branch, which I failed to verify in code before ruling — recorded as a Planner error; the cross-model review loop worked as designed. Rulings 1 and 3 stand as refined by the reviewer (full-payload sidecar; idx==i accepted as substitution detection, now with dtype pin added).
- **Command / Validation** — Fix batch dispatched to the round-1 Coder (same agent, context intact): (F1) explicit `--expected-stream-count` enforcement + wrong-size/empty rejection tests; (F2) `--record-stream` full-payload sidecar per B2, both modes, opt-in, post-validation write; (F3) per-position `idx == i` assertion; (F4) fingerprint dtype/finite/shape fail-closed assertions + `fingerprint_schema` version field; (F5) multi-worker in-memory DataLoader ordering test. No plan-document changes required.
- **Next** — Fix commits → Planner verification (diffs + tests) + focused Codex re-verify of B1/B2 closure → round 1 CLOSED → round 2 (screen kit).

## 2026-08-11T01:02:49-04:00 — Round 1 CLOSED (fix batch verified twice); round 2 dispatched

- **Goal** — Close round 1 per SOP (code → review → fixes → re-verify → log), open round 2.
- **Change** — Fix commits `a6d2e26` `769710e` `59be9ff` `6efbcb7` `6e7616c` (ledger updated). Coder deviations both accepted after review confirmation: fixed-mode stream-fill contract change (the point of ruling 1) and the `yaw_column_shift` extraction (single source of truth for the quantization rule; bit-exact pins incl. off-grid angles).
- **Command / Validation** — Planner verification: F1/F2 diffs read (validation at `eval_FLAC.py:987–989` precedes sidecar write at `:1036`); suites rerun by Planner (134 passed, 52.8 s). Codex focused re-verify (`yaw_gen_codex_code_r1_reverify.md`, 103k tokens): **ROUND 1 CLOSED** — B1/B2/ruling-3/N4/N5 all confirmed with file:line evidence; snapshot-suite blob IDs identical base↔head; scope exactly the three declared files; suites independently rerun 134 passed.
- **Result** — `passed`. Round 1 loop complete: 10 commits total (`66e6ca5..6e7616c`), eval-side random-yaw mode + full assignment-integrity machinery, fixed path frozen byte-identical.
- **Next** — Push round-1 commits (pull-rebase first); dispatch Coder round 2 (screen kit + wave submitter + `exp14_validate_cell.py`, prompt staged in scratchpad); then round-2 review.
