## Verdict: REQUEST-CHANGES

Do not approve the 12-day launch yet. Ranked by compute-waste risk:

1. **BLOCKING — the proposed treatment misses the implicated 3/3 regime.** The claim that micro-32 “can only reach 2/3” is false ([L16](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:16)): `96 // 32 = 3`. Replace cap 64 with cap 96, subject to a real micro-32 DDP forward+backward+checkpoint-recompute fit probe. Cap 32 versus 96 reaches 1/3 versus 3/3 for the same two arms/cost. A cap-64 NULL does not test the exp_11 topology and cannot support [L34’s](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:34) “clears implementation” conclusion. Micro-8×2×accum-4 is not an alternative: SyncBN sees `8×2=16`, not 64; accumulation never feeds BN.

2. **BLOCKING — this is a cap-policy intervention, not pure draw-sharing.** Changing cap also changes forward count, GEMM shape and global RNG consumption; conditioning runs before timestep/noise draws. A positive result identifies the operational chunk plan, not RoPE covariance alone. Either narrow DS1 accordingly or implement the costlier fixed-shape control: identical one-angle forwards with prescribed mappings `[a,b,c]`, `[a,a,b]`, `[a,a,a]`.

3. **BLOCKING — one training seed cannot support the stated causal tiers.** Five evaluation seeds estimate generation noise conditional on two checkpoints; they do not estimate training-seed variability. Thus “primary, causal” ([L30](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:30)) and `CAUSE-CONFIRMED/NULL` based on `2σ_c` ([L34](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:34)) overclaim. One pair supports only “cap effect for the seed-42 trajectory.” General causality needs independent paired training seeds, a single primary endpoint/SESOI, and a paired CI; ≥3 pairs is a bare variance-estimation minimum, 5+ preferable, with final power derived from pilot training-seed variance. That cannot fit 12 days at the stated rate. Also, NULL requires an equivalence CI inside a meaningful margin—not merely an observed difference below `2σ_c`.

4. **HIGH — reject the import-time environment override as designed.** [L20–26](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:20) makes the treatment ambient, absent from the checkpoint config, easy to lose on resume, and capable of contaminating other sessions. Cap 32 leaked into the default eval batch 64 fails at [`yaw_rotation.py:495`](/home/yixunhu/codespace/FLAC/src/data/yaw_rotation.py:495). Prefer a validated `training.frame_avg_max_fwd_samples` config key, default 64, passed explicitly into conditioning and embedded in the checkpoint. Evaluate both arms under one common eval cap and record training/eval caps separately. Do not edit `yaw_rotation.py` in a checkout used by a live cluster process; wait for a safe boundary or use an isolated pinned worktree.

   TDD must cover default compatibility; strict integer/>0 validation; B32/C4 partitions for caps 32/64/96; factory/wrapper propagation without global mutation; cap below batch; resume/config identity; DDP rank agreement; common-cap evaluation; provenance; full regression suite; and real cap-96 fit/VRAM.

5. **HIGH — DS2 is not a reproducibility floor.** [L31](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:31) bundles source drift, batched-versus-legacy execution, RNG/data order, environment, hardware and calendar/co-tenancy. It cannot “bound ANY cross-arm difference” in contemporaneous DS1. Call it a cross-era replication check and require a July-launch-commit parity audit. DS3 ([L32](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:32)) is likewise contextual, not causal. A5 v2 remains a conditioning-level, initialization-only pilot; it must not set training-outcome sufficiency or NULL claims.

6. **HIGH — launch/protocol/resource plan is incomplete.** `f_arm_launch.sh` in this repository is a resume-required fine-tune launcher; [L26](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:26) should instead specify a new guarded from-scratch launcher based on `bf_scratch_launch.sh`, with unique outputs/W&B identities and resume-cap checks. [L30–33](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:30) also omit the mandatory explicit `--frame-avg-angles`, `--rotate-deg`, and `--cond-autocast` flags. The “band-level” screen claim lacks a registered trajectory statistic.

   Before DS-CS/96: finish DS-PA, run its 40k confirmation/parity audit, then hold if the July bridge fails. Define hard aborts—wrong SHA/config/cap/world size/BN, OOM/nonfinite, storage floor—and no metric-driven stopping. The 11.8-day point estimate in [L38](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/plan_fa_drawshare.md:38) leaves essentially no review, evaluation or infrastructure buffer and is unqualified for cap 96.

Also required before approval: create the missing experiment `_worklog.md` and enumerate every file/test/launcher artifact in the plan per SOP. No files were modified.