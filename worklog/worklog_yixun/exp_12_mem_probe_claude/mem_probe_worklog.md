# Lab notebook — exp_12_mem_probe

## 2026-08-05T16:05:00-04:00 — scaffold + probe authored (Planner-written one-off; review pending)

- **Goal** — measure peak VRAM of the paper training configuration (1 GPU, micro-batch 64, accum 1, bf16-mixed, no DDP/SyncBN, no ViT grad-ckpt, canonical `FLAC_AR.json`) on a Slurm-allocated L40; 5 optimizer steps. Fail-honest on OOM per Yixun's verbatim mandate (no batch/ckpt/accum/GPU fallback).
- **Hypothesis** — the paper recipe needed H100-class memory (exp_07 audit: release got BN-64 via micro-64 on one GPU without SyncBN); on a 46 GB L40 the no-checkpointing micro-64 forward is at real risk of OOM. Either outcome is decision-grade (paper-exact recipe viable on L40 vs SyncBN-64 workaround justified).
- **Change** — new files only, all inside this folder: `FLAC_AR_exp12_memprobe.json` (byte-copy of canonical `src/configs/model_configs/FLAC/AR/FLAC_AR.json`, sha256 `f3eafef4…7ec9` both — **config diff vs canonical: NONE**) and `mem_probe.sbatch` (thin Slurm wrapper: canonical-byte-identity gate, paper-config asserts incl. grad-ckpt absent/False in both ViT conditioners, wandb identity gate with disclosed fallback to `logger=none`, 0.5 s nvidia-smi peak poller, `--max-steps 5`, isolated names `FLAC_exp12_memprobe`/`exp12_memprobe`/`outputs_FLAC/exp12_memprobe`).
- **Command / Validation** — static rung green: `bash -n` OK, config JSON parses, `--max-steps` plumbing confirmed (train.py:46,78). Defaults audit: `defaults.ini` already carries batch 64 / bf16-mixed / sync_batchnorm false / num_gpus 1 / accum 1 / seed 42 / workers 6 — CLI passes them explicitly anyway for the record; `--sync-batchnorm` flag deliberately ABSENT (train.py keeps kwargs byte-identical when off). checkpoint-every 10000 > 5 steps ⇒ storage-light.
- **Acceptance criteria (pre-launch)** — Slurm job on partition `all`, `gres=gpu:l40:1`, reaches train.py; EITHER ≥1 optimizer step × 5 with rc=0 and a recorded peak-VRAM number, OR a caught CUDA OOM with the exact error text + peak-at-failure recorded. Job 3637217 (p1-rerun, neu303) untouched — probe only ever submits its own allocation. No config fallback of any kind.
- **Result** — `in_progress` (Codex review of the probe script next; launch after findings addressed).
- **Analysis** — n/a yet.
- **Next** — Codex code review (SOP universal coverage) → address → `sbatch` → monitor → results/analysis.

## 2026-08-05T17:05:00-04:00 — round 1 review (Codex REJECT: 5 BLOCKING + 2 NIT) → Rev 2 fixes → Planner verification → round CLOSED

- **Goal** — SOP review loop for the probe round.
- **Command / Validation** — review: `codex exec` gpt-5.6-sol @ xhigh (danger-full-access, bwrap unavailable, read-only instruction; tree clean post-review); saved `mem_probe_codex_code_probe_review.md`. Fixes applied to `mem_probe.sbatch` (Rev 2): B1 UUID-bound fail-closed measurement (single-L40 assert, per-UUID query, POLLFAIL fatal, ≥1 valid row, dynamic memory.total, invalid peak overrides rc=0); B2 literal-SHA pin `f3eafef4…7ec9` on BOTH canonical + probe copy, `unset PYTHONOPTIMIZE`, no bare asserts, `use_ema is True` gate; B3 `EXPECT_SHA` commit binding + tracked-surface drift gate (`--untracked-files=no` on train.py/defaults.ini/src/expdir) + explicit CLI for every defining setting + `--nodes=1 --ntasks=1` + runtime shape asserts; B4 wandb fixed fail-closed (no logger fallback), isolated `WANDB_DIR`; B5 synchronous `| tee` with `PIPESTATUS[0]`, PL completion marker required (`\`max_steps=5\` reached` — wording verified in installed PL 2.1.0 fit_loop.py:167), OOM classified only from the flushed train log; N6 job-id-suffixed truncated artifacts + duplicate-run guard on the run dir; N7 params file re-headered PRE-LAUNCH DRAFT + unambiguous acceptance criterion.
- **Result** — `fix_ready`; `bash -n` green. **Round closure via Planner verification of the fixes** (SOP-sanctioned alternative to re-review; each fix checked line-by-line against its finding). Codex re-verify can be run post-hoc on request.
- **Next** — commit round (binds `EXPECT_SHA`) → `sbatch` → monitor → results.
