# Params — exp_16 ARE-V
Config `FLAC_AR_ARE.json` = BVp1 + training.are_lambda=1.0 + training.are_anchor{delta_hat:0.0, a_g:0.394574} (calibration: 2,059 LOS paths, R1 fired → δ̂=0; ℓ2 A_g @H=32). Recipe: from scratch, DDP 32×2×1 eff-64, SyncBN(BN=64), ViT grad-ckpt, seed 42, bf16, 40,000 steps, ckpt/2500, wandb FLAC_exp16_AREV, env flac. Anchor: mean-encode (no sampling) via frozen VAE, /scale, minus Enc(0), frames 0–2 kept (worst case 2 ≤ 3, headroom to 47.1 m), LOS gate from listener panorama, RandomTimeShift published into t*. Anchor cost ≈6.9%/step → ~1.91 d. Eval: --are-lambda from embedded config; λ=0 control = P1@40k (historical, plan-registered; no contemporaneous λ=0 arm — disclosed).
