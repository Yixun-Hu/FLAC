**Reviewer:** OpenAI Codex (GPT-5, Codex API workspace agent, read-only sandbox) · **Date:** 2026-08-14  
**Reviewed:** HEAD `9bebb15`, plan blob `d7cb3a9`

**Verdict: REQUEST-CHANGES.** The residual-flow algebra is sound, but the current plan is not safe to code or launch.

1. **BLOCKER — endpoint transformation is underspecified, and the documented dispatch does not exist on this branch.** Define `r=z−λA`, then use it in both `x_t=(1−t)r+tε` and `u=ε−r`; changing only `targets` would train an inconsistent field. Current code constructs both from unmodified `z` ([training L593–603](/home/yixunhu/codespace/FLAC/src/training/diffusion.py:593)), and inference always starts Gaussian ([eval L1037–1049](/home/yixunhu/codespace/FLAC/eval_FLAC.py:1037)). Contrary to [CLAUDE.md L123–128](/home/yixunhu/codespace/FLAC/CLAUDE.md:123), current factory/wrapper code has no `flow_source` argument ([factory L166–194](/home/yixunhu/codespace/FLAC/src/training/factory.py:166)). Resolve the base: port the nearest-ref lineage first, or implement ARE as an orthogonal target-space mode and correct the documentation. [Plan L9](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:9).

   EMA and CFG do not break the algebra: EMA tracks only model weights ([training L243–251](/home/yixunhu/codespace/FLAC/src/training/diffusion.py:243)); CFG dropout removes cross-attention but retains query-global conditioning ([DiT L302–312](/home/yixunhu/codespace/FLAC/src/models/dit.py:302)). Add-back must nevertheless be bound to the checkpoint’s embedded config.

2. **BLOCKER — P1 is not a bit-identical matched training control.** Algebraically, λ=0 can be bit-identical only if anchor construction is skipped before any RNG or CUDA operation. Historically, P1 crossed four legs and explicitly did not restore RNG/dataloader position ([P1 results L33](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_07_fa_scratch_claude/fa_scratch_results.md:33)); the repo also states resumes are not bit-exact ([CLAUDE.md L161](/home/yixunhu/codespace/FLAC/CLAUDE.md:161)). Same-seed P1 reruns differ materially—K8 EDT 35.955 versus 38.845 at 87.5k ([comparison L12–15](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/model_comparison.md:12)). Therefore P1@40k is a historical recipe comparator, not the λ=0 matched arm claimed at [plan L11/L21](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:11). Run a contemporaneous λ=0 arm for causal attribution, or weaken the claim. Add a full one-step weights/gradients/EMA/RNG-state parity test for key-absent and λ=0.

3. **BLOCKER — the proposed VAE anchor path is stochastic and architecturally misplaced.** Normal `pretransform.encode()` samples the VAE posterior through `torch.randn_like` ([bottleneck L27–46](/home/yixunhu/codespace/FLAC/src/models/bottleneck.py:27)); it cannot satisfy [plan L15’s](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:15) “determinism/no RNG.” Implement a posterior-mean encode for both skeleton and silence, and apply the same pretransform scale (`encode` divides; `decode` multiplies) ([pretransform L51–71](/home/yixunhu/codespace/FLAC/src/models/pretransforms.py:51)). Current scale happens to default to 1.0, but that must be asserted.

   “Metadata-path on-the-fly VAE encode is cheap” is unproven: the dataloader is built before the model/VAE weights are loaded ([train L112–160](/home/yixunhu/codespace/FLAC/train.py:112)), so workers cannot reuse the loaded pretransform without new plumbing and would hold separate model copies. Prefer the design source’s deterministic dense table/full-length encoding ([anchor source L735–763](/home/yixunhu/codespace/rir2rir/worklog/exp_15_anchor_claude/plan_anchor.md:735)); otherwise the 15-step DDP probe must gate measured throughput, VRAM and worker memory.

4. **BLOCKER — training augmentation silently misaligns the anchor.** AR training enables augmentations ([train dataset L13–15](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/AR/train/acousticroom_train.json:13)); `RandomTimeShift` moves half the RIRs forward by 1–10 samples without returning the shift ([utils L147–156](/home/yixunhu/codespace/FLAC/src/data/utils.py:147)). Geometry remains unshifted, so `t*` will be wrong on those examples. Record and apply the existing draw without consuming another RNG value, or disable augmentation for both newly trained arms—doing the latter invalidates historical P1 as control.

   Calibration is also too vague. Pin the design-source contract: LOS-only train cohort, local ±45-sample search, median `δ̂`, H=32 unit-ℓ2 kernel, matching `A_g` energy convention, clamp, and escalation rules ([source L508–562](/home/yixunhu/codespace/rir2rir/worklog/exp_15_anchor_claude/plan_anchor.md:508), [L346–354](/home/yixunhu/codespace/rir2rir/worklog/exp_15_anchor_claude/plan_anchor.md:346)).

5. **HIGH — frame coverage passes; short-waveform encoding does not.** Scanning all 302,925 AR metadata records gives worst `r=27.104669 m` ([record](/home/yixunhu/codespace/FLAC/AcousticRooms/metadata/Cafe/Cafe_idx_0/S0045_R0034.json:1)): `t*=1742.443` samples at 22,050 Hz. Even H=32 ends at 1774.443, below sample 2048, so every AR direct packet lies in nominal frames 0–1; retaining frames 0–2 is safe. But encode the full 10,240-sample skeleton and then truncate. A 3,072-sample encode has a different convolution boundary and is not established equivalent.

   AR LOS is observable from the listener panorama: `source` is listener-relative ([AR metadata L31–37](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/AR_md.py:31)) and depth is captured for that receiver ([L47–52](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/AR_md.py:47)). HAA correctly reverses both origin and vector semantics ([HAA L28–48](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/HAA_md.py:28)). Pin radial norm, 0.95 threshold, 3×3 minimum, seam wrap and pole clamp; the plan’s single-pixel `depth<r` rule omits these load-bearing conventions ([source L831–838](/home/yixunhu/codespace/rir2rir/worklog/exp_15_anchor_claude/plan_anchor.md:831)).

6. **HIGH — K wording and AR3 interpretation are wrong.** FLAC does not transport K latents and average them: K reference embeddings are tokens ([AR metadata L42–45](/home/yixunhu/codespace/FLAC/src/configs/dataset_configs/custom_metadata/AR_md.py:42), [conditioner L160–178](/home/yixunhu/codespace/FLAC/src/models/conditioners.py:160)), followed by one Gaussian sample and one decode. Therefore add `A_query` exactly once; delete “K-reference averaging unchanged” at [plan L10](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:10).

   AR3’s `{0,.5,1}` sweep on a λtrain=1 model is an **add-back dose**, not “train-time versus add-back-time” ([plan L24](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:24)). Add the complementary P1+inference-anchor sweep, or narrow the claim.

7. **HIGH — AR1 statistics and EDT mechanism are underdefined.** Define `σ_c` explicitly—prefer paired seed-wise deltas and a fixed CI/tier rule. Make EDT the primary endpoint, C50 secondary, and T60 non-inferiority/safety; allowing a win on either EDT or T60 is flexible and T60 is not structurally targeted by an early anchor ([plan L21–25](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:21)). An EDT win alone does not prove direct-path correction because `Enc(skel)−Enc(0)` is only a coordinate origin, not an additive direct component ([source L630–634](/home/yixunhu/codespace/rir2rir/worklog/exp_15_anchor_claude/plan_anchor.md:630)); preregister LOS/distance/direct-arrival mechanism readouts.

8. **CODE-ROUND BLOCKER — scope, protocol, launcher, and TDD plan must be reconciled.** The worklog says ARE-V only and defers ARE-FA/CYL ([worklog L5–8](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/are_port_worklog.md:5), while the plan still schedules three arms yet budgets one ([plan L27–43](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_16_are_port_claude/plan_are_port.md:27)). State ARE-V-only now; 1.8/3.5–4 days applies only to that arm. A required matched λ=0 arm makes it roughly 3.6 days exclusive.

   Reuse exp_14’s pattern, not its hard-coded launcher: explicit `PROBE/FULL/RESTART` and isolated namespaces ([launcher L115–188](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/dsarm_launch.sh:115)); parsed single-delta config contract ([L211–276](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/dsarm_launch.sh:211)); type-strict full-state resume ([L333–435](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/dsarm_launch.sh:333)); invocation-bound checkpoint readback ([L508–588](/home/yixunhu/codespace/FLAC/worklog/worklog_yixun/exp_14_fa_drawshare_claude/dsarm_launch.sh:508)). Extend treatment fingerprints to calibration/table/LOS/VAE hashes. The plan must also enumerate every edited file and named per-function red test before implementation, per SOP.

No files, environments, or jobs were modified.