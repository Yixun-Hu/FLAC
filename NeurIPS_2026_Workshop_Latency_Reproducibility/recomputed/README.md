# Recomputed output directory

Run `python3 code/calculation/recompute_all.py` from the bundle root. The generated repeat-1 summary and final single-repeat table will be written here. Canonical publication outputs are preserved separately under `expected/`.
